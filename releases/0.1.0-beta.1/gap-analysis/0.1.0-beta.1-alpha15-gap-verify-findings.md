# MISSING findings (independently-derived gap absent from the doc)

## slice: host-runtime-cli-daemon  (8 missing)
- **DB-RPC IPC boundary (worker owns no DB handle)**
  - note: No mention of DB-RPC or 'the worker owns no DB handle' anywhere. §13 covers the external git-dir + VACUUM INTO but not the typed DB-RPC channel interposed between the credential-free worker and the DB (worker reaches the DB only via brokered egress + DB-RPC).
- **Derived-store location: in-vault SQLite vs external data dir**
  - note: The relocation of the DB (and payloads JSON) OUT of the synced vault to a per-device OS data dir (~/.local/share/memoria/<vault-id>/memoria.db) is not stated. §2 keeps discussing the in-vault Memoria/ tree; §13 only covers the external git-dir + VACUUM, not the DB's own location.
- **Version handshake across plugin/daemon/CLI (refuse on mismatch)**
  - note: No cross-component version/capability handshake, refuse-on-mismatch, or installed-build standing deny-tests appears anywhere in the doc.
- **Write-event push (daemon→plugin) over WebSocket vs FS polling**
  - note: The daemon→plugin write-event push over loopback HTTP + WebSocket (motivated by Obsidian's watcher provably missing external writes / stale badge = honesty bug) is not mentioned; §10 covers surfaces/view-spec but not the push channel, and there is no discussion of A15's inbound FS-polling serve --watch as its counterpart.
- **Cognition-bound throttle on machine work**
  - note: The cognition-bound back-pressure is not stated: a full awaiting-review queue throttling new machine work, the system being cognition-bound (scheduled runs bounded by triage batch size, not wall-clock/spend), and no propose-only surface shipping without a precision floor + volume cap. §5 covers the Review surface but not the throttle.
- **Crash-isolation-from-Electron rationale**
  - note: The Electron crash-isolation rationale is not stated: compute (PDF extraction, embeddings, NLI, egress, DB) deliberately kept OUT of Electron in a separate OS process for crash isolation + one network choke point. The doc notes the daemon exists but never gives this rationale (the word 'Electron' does not appear).
- **Second-client (VS Code) portability story**
  - note: The recorded VS Code 'client #2 of the same daemon' second-client / M×N-dividend rationale (host-agnostic daemon + shared ViewSpecRenderer/DaemonClient) from Appendix A of the B1 design is not mentioned anywhere in the doc.
- **Offline behavior explicitly specified**
  - note: The explicit offline operating contract — catalog / search / notes / reading fully work; LLM jobs queue and fail cleanly, never half-write (all machine writes staged) — is not stated; the word 'offline' does not appear in the doc.

## slice: vault-layout-concept-model  (7 missing)
- **Deliverable artifact types (outline/section/figure/code) collapsed**
  - note: The doc never names the A15 deliverable-artifact targets outline/section/figure/code, nor states they collapse to draft/export or that outline folds into project.md. Only 'draft'/'export' appear in the §3 enum, with no delta statement.
- **Lifecycle field: archived vs superseded semantics**
  - note: Only the word 'archived' appears (as an A15 field in §4's universal-frontmatter list). The entire delta — rename archived→superseded, plus the retrieval-semantics inversion from stays-retrievable to Ask-excluded-by-default with export refusal — is not stated anywhere.
- **OKF v0.1 bundle conformance framing (explicit in beta.1)**
  - note: The doc contains no OKF terminology at all — no okf_version, no OKF §9.2 class enum, no resource: key, no OKF §7 log, no tolerate-unknown-producer-keys. The entire OKF-conformance exchange-format framing is absent.
- **reading disposition field (skip|cite-only|read) — new**
  - note: §7/§16 discuss Zotero-first reading, but the new researcher-set works frontmatter field reading: skip|cite-only|read is never mentioned.
- **New work provenance/OKF frontmatter keys (source, resource)**
  - note: The new works frontmatter keys source: (zotero|inbox|doi|bibtex|url|repo) and resource: (DOI/pinned repo URL) are not listed among the mem-* keys in §4 or anywhere else.
- **Per-type gated/category metadata removed**
  - note: §2 mentions folders.yaml categories=[knowledge] only in the bundle-root discussion; the doc never states that per-type schema category: knowledge / gated: false metadata is dropped.
- **note/work citations list field dropped**
  - note: The optional citations: list frontmatter field on note/work/hub is never mentioned; §4's A15 field enumeration omits it, and the 'replaced by DB edges' delta for citations is not stated.

## slice: provenance-frontmatter  (8 missing)
- **file churn on checking (content-hash-is-the-file invariant broken)**
  - note: No statement anywhere that checking rewrites a machine block/marker (m→mc promotion), causing file churn on check and breaking A15's 'files never churn on checking; the content hash is the file' invariant. §5's pre==post byte gate concerns human blocks not churning — the opposite point; machine-block churn on check is unaddressed.
- **supersession encoding: typed link + DB composition vs flat frontmatter flag/pointer**
  - note: No mention anywhere of the new flat frontmatter keys superseded: true and superseded-by: <citekey>. 'supersed' appears only in §8 as a retrieval-coverage gap cause, not as a supersession-encoding change. The typed-link-to-flat-keys re-encoding delta is unstated.
- **green/checked semantics: warrant-resolution vs citation-entailment (mc)**
  - note: The semantic redefinition of green/checked — from A15's 'checks passed AND warrants resolve' to B1's mc = citation-correct = NLI entailment that the cited anchor supports the sentence (explicitly not grounded/derived-from, with a documented faithfulness gap) — is not stated. §5 line 46 covers green's reconciliation mechanism (a different gap) and §6 mentions NLI only as contradiction detection within the retrieved set, not as the definition of the checked state.
- **terminology rule divergence: "verified" re-admitted (qualified)**
  - note: No mention of the terminology-gate change. 'verified' does not appear in the doc; A15's rg gate forbidding approved/verified/trusted and B1 re-admitting 'verified against anchors' (forbidding only safe/approved) is unstated. §5's 'checked ≠ approved' is the semantic principle, not this lexicon/gate delta.
- **new researcher-set reading disposition frontmatter key**
  - note: No mention of a reading: skip|cite-only|read frontmatter disposition key ('reading:' and 'cite-only' both absent from the doc). §7/§16 discuss reading surfaces, not this frontmatter key.
- **per-file local-only: true frontmatter key**
  - note: The doc references only the folder-level local-only/ marker in the Memoria tree (§2 line 22). The per-file local-only: true frontmatter flag enforced in prompt assembly is not mentioned.
- **OKF v0.1 conformance framing with resource:/description:/okf_version keys**
  - note: 'OKF', 'resource:', and 'okf_version' appear nowhere in the doc. The OKF-v0.1-bundle re-framing of the frontmatter contract, the OKF class enum / recommended-key semantics, and the resource:/okf_version keys are entirely unstated.
- **source-origin enum key (source:) in frontmatter**
  - note: No mention of a source: zotero|inbox|doi|bibtex|url|repo origin enum in work frontmatter ('source:' absent). The doc keeps intake-origin discussion at the DB/ingest level (§7, §9) and never notes this frontmatter enum.

## slice: store-catalog-snapshot-migration  (5 missing)
- **Citekey correction now renames the Work**
  - note: §3/§7 establish citekey=identity/PK, but the doc never states the consequence: that a citekey correction now renames the Work directory (reversing ADR-124's 'citekey change no longer renames the Work') and is a first-class revertible 'rename' run. The word 'rename' and the rename run-kind appear nowhere in the doc or appendix.
- **Derived DB moved out of the vault to a per-device external dir**
  - note: §13 covers the external GIT-dir, but the SQLite DB relocation is a separate delta the doc never states: moving from in-vault .memoria/memoria.sqlite to ~/.local/share/memoria/<vault-id>/memoria.db 'outside the synced tree, per-device,' with new <vault-id> namespacing and payloads beside it. '.local/share', 'vault-id', and the DB-out-of-vault move appear nowhere.
- **Tamper-evidence apparatus (hash-chained journal + git anchor) removed**
  - note: The doc nowhere states that B1 drops the row-hash-chained append-only journal (prev_hash/row_hash/GENESIS) and the git-anchored .memoria/journal-head tamper-evidence root that ADR-127 carried from ADR-104. 'tamper', 'hash-chain', 'row_hash', and 'journal-head' appear nowhere. §13's 'SQLite-journal cascade-rollback' phrasing is about rollback, not tamper-evidence, and does not state its removal.
- **Partial-success batch rule (completed_partial) is new**
  - note: The doc never states B1's deliberate exception to whole-run atomicity: inside a digest/extraction batch, per-item failures do not roll back the run (successes commit, failures stay retryable) with a first-class completed_partial runs.status, whole-run rollback reserved for invariant breaches. 'completed_partial' and 'partial-success' appear nowhere; §13's batch protocol is described as atomic without this carve-out.
- **overrides.jsonl human-disposition log and tiered DB-loss replay removed**
  - note: The doc never states that B1 removes the git-tracked append-only .memoria/overrides.jsonl and its directional DB-loss replay tiering (restrictive dispositions auto-reapply; permissive ones return as attention items), relying instead on frontmatter (superseded/reading) + exported decide-events CSV. 'overrides' appears nowhere, and the appendix removed-list omits it.

## slice: ingest-extraction-enrichment  (7 missing)
- **Semantic Scholar + scite.ai dropped (loses SPECTER/TLDR/citation-intents)**
  - note: No mention anywhere of Semantic Scholar, scite.ai, SPECTER embeddings, TLDRs, or S2 citation-intent contradiction candidates (grep: all 0). §6's contradiction discussion covers the Tier-2 LLM tension judge removal and NLI-only detection but never the loss of the S2/scite signal class. The whole provider/signal removal is unstated.
- **Background re-enrichment cadence rejected (calibrated cadence/TTL → attention-only)**
  - note: No mention of re-enrichment cadence, TTL for volatile fields, or attention-driven refresh (grep: 're-enrichment' 0, 'cadence' 0, 'TTL' 0). §6's calibration discussion is about NLI/shadow-first score calibration, not metadata re-fetch scheduling. The 'no background auto-refresh ever; refresh follows attention' delta vs A15's calibrated cadence + volatile-field TTL is absent.
- **Three-valued enrichable fields + as-of provenance rendering (new/explicit)**
  - note: No mention of the present|absent-in-source|not-yet-fetched trichotomy or mandatory provenance+as-of-date count rendering ('41 citations — OpenAlex, 2026-07-02') (grep: 'three-valued' 0, 'as-of' 0). This field-honesty model is entirely unstated.
- **Entity resolution: never-auto-merge (flag) → shared-ORCID auto-merge + lazy point-of-use**
  - note: No mention of entity resolution, ORCID/ISSN-L merge, auto-merge, or lazy point-of-use adjudication (grep: 'ORCID' 0, 'auto-merge' 0, 'entity resolution' 0). §9 notes authorship edges are dropped but says nothing about the resolution policy. A15's flag-as-attention-never-merge vs B1's shared-ORCID auto-merge + lazy adjudication delta is absent.
- **GitHub repo as a first-class work (new)**
  - note: No mention of GitHub repos, the CITATION.cff→codemeta→Zenodo-DOI→API metadata chain, pinned HEAD SHA, README→fulltext, code-files-inert, or the paper↔repo typed edge (grep: 'GitHub' 0, 'CITATION.cff' 0). §9's edges table description covers refs + capped cited-by but omits the paper-repo edge. The whole repo-as-work subsystem is absent.
- **DOI metadata channel (Crossref API primary → citation.doi.org registry-agnostic content negotiation)**
  - note: No mention of citation.doi.org, content negotiation, DataCite, registry-agnostic CSL-JSON, or redirects-disabled (grep: 'citation.doi.org' 0, 'DataCite' 0, 'content negotiation' 0). The doc frames OpenAlex as the fill source and Crossref for title/retraction, but never states that the canonical DOI/arXiv metadata author changed from Crossref-API-primary field merge to the DOI registry via content negotiation.
- **Peripheral provider set (arXiv/PubMed/ORCID/ROR/ISBN) narrowed**
  - note: No mention of arXiv-via-DataCite-DOI, PubMed/NCBI deferral, ORCID/ROR resolver removal, Software Heritage, or ISBN/book pipeline deferral (grep: 'arxiv' 0, 'PubMed' 0, 'ORCID' 0, 'ISBN' 0). §9 covers the OpenAlex graph fields but not the peripheral-provider narrowing/deferral delta.

## slice: ask-compose-discovery-gap-cost  (14 missing)
- **Gap severity scoring removed**
  - note: No mention anywhere of the {0,1,2} impact/confidence/actionability ordinals, the 0..8 score, the ≥4/≥2 bands, or why/next_actions/candidate_work_ids. §8's shift to retrieval-coverage does not state the removal of the ordinal severity/ranking model.
- **Year-spread temporal-coverage line**
  - note: No mention of a year-spread / temporal-coverage (TEMPO) line in gap output anywhere in §8 or elsewhere.
- **Snowball ranking model differs**
  - note: §8 mentions snowball depth ≤2 only. No mention of occurrence-count-across-seeds ranking (vs A15 steering.md), cap-stamped lines, the 10-20-item capped digest note, PRISMA-S event logging, or the discovery_seen/rejected ledger.
- **Deterministic probe derivation for stance**
  - note: No mention of stance-probe derivation (subject-aux inversion of yes/no questions, wh-questions 'state as a claim,' no-probe ⇒ all items 'mentions') anywhere.
- **Verbatim quote byte-substring assertion**
  - note: No Ask-time verbatim-quote extraction rule (best 1-3 contiguous sentences ≤60 words asserted as a byte-substring of the anchored block, fail ⇒ drop + re-index). §5's pre==post byte gate is about human-content immutability, a different mechanism.
- **Evidence-set ids + zero-survivor banner + second-hop**
  - note: No mention of the evidence set ≤25, ev-<8hex> block IDs, the searched-set-scoped zero-survivor banner, or the opt-in deterministic 'search deeper' second-hop entity re-query.
- **Superseded-chunk default-exclusion with per-Ask override**
  - note: The doc treats 'superseded' only as a gap cause in §8. It nowhere states that Ask excludes superseded-work chunks by default, counts them in the denominator, and surfaces them only via a per-Ask historical override — nor the contrast with A15 keeping superseded items first-class retrievable.
- **Synthesis grounding/output guardrails**
  - note: None of the grounding/flagging/instrumentation contract is stated: quotes+citekeys+sections-only (local-only material structurally absent), cites only [En], ⚠ uncited flag (flagged never blocked), stance chips + denominator carried through, Checker upgrading entailed sentences to mc, 'save to project' routing as t:u, or separate instrumentation.
- **`ruled-out.md` dead-end memory (new)**
  - note: ruled-out.md appears nowhere; the projects-folder file lists in §2 and §3 stop at project/evidence/gaps/draft/export. No mention of a human-written, never-machine-written, never-auto-promoted dead-end decision log (vs A15 folding this into generic working notes).
- **Superseded/retracted citation hard-blocks at export**
  - note: §9 mentions a Crossref retraction check at ingest and §5 mentions the export gate blocks generally, but neither states the specific delta that export is the one place the superseded/retraction flag must BLOCK (not down-rank) a citing draft — the elevation to a hard export-time block.
- **Local token estimator + safety margin**
  - note: §13 mentions 'preview' only as a breaker enforcement point, not the estimation approach. No mention of a local deterministic token estimator, a stated safety margin, or the 'production tokenizer not public, previews are honestly-labeled bounded estimates' rationale.
- **Single pinned model tier vs per-op test/live pins**
  - note: No mention of model tier pinning. §6's 'operation test (pinned prompt+model)' is per-operation shape, not the delta that B1 collapses A15's per-operation runner.test/runner.live pins into ONE beta-wide tier governed as a release marker.
- **prompt-template versioning + prompt-change revalidation**
  - note: No mention of costs.prompt_v prompt-template versioning on every run, nor the rule that any model or prompt pin change re-triggers fixture re-validation of the affected engine before it runs on the real vault ('a prompt edit is an unevaluated model swap').
- **Per-action confirmation threshold**
  - note: No mention of a spend-confirmation gate. The $1-single-action / projected-month-crosses-cap confirmation threshold is absent (§13 lists soft budget and breaker points but not this gate).

## slice: integrity-security-agent-judgment  (15 missing)
- **Theatre-check / calibrated-or-thinned checkpoint doctrine dropped**
  - note: No mention anywhere of the no-checkpoint-baseline comparison, the checkpoint-must-beat-its-un-checkpointed-baseline-or-be-thinned doctrine, mandatory thinning, or the ADR-128 evidence base (Jacobs 2021, verification asymmetry). §5 covers only the review-surface re-introduction, not the theatre-check discipline.
- **act/ask/log routing classes dropped**
  - note: §5 lists beta's apply/reject/defer (+accept-unedited vs accept-after-edit) dispositions, but the doc never mentions the alpha act/ask/log consequence-routing taxonomy or that this routing dimension was removed.
- **Apply-rate rubber-stamp detector removed**
  - note: No mention of apply-rate bounds (>0.9 or <0.2) or any quantitative rubber-stamp detector; neither the alpha numeric detector nor its replacement by qualitative design-smell heuristics is stated.
- **overrides.jsonl + tiered DB-loss replay removed**
  - note: No mention of the git-tracked append-only .memoria/overrides.jsonl human-disposition ledger or the direction-tiered DB-loss replay (restrictive dispositions auto-reapply, permissive ones return as attention items), nor beta's frontmatter-decisions + decide-event CSV replacement.
- **Consumption guard (read-time refuse+enqueue) → write-time stale-base check + amber render**
  - note: No mention of the alpha read-time consumption guard (hash-mismatch refuses-to-serve + enqueues scan, no write on read path) or beta's replacement — write-time stale-base skip/requeue + amber-on-divergence display that is still served. §5's hash checks concern human-span byte-immutability and green rendering, not this mechanism.
- **Semantic Scholar + scite.ai contradiction-candidate providers removed**
  - note: The doc never names Semantic Scholar or scite.ai (nor their reference union / citation-intent free Tier-0 contradiction candidates / TLDRs / SPECTER). §9 lists only OpenAlex/Crossref without noting these two providers and their signal were dropped.
- **Explicit threat model T1–T5 new in beta**
  - note: No enumerated five-item threat model (T1 injection-exfil via markdown URL fetch … T5 third-party content entering vault) and no 'injection classifiers excluded by charter — contain outputs, don't detect intent' stance appears anywhere; §12 keeps security distributed as prose.
- **Third-party plugin interference corruption class + startup detection (new, plugin-host-driven)**
  - note: No mention of the format-on-save-plugin corruption class (rewriting machine spans / flipping markers / tripping rollback loops), startup detection of hostile plugins, formatter-excluded path registration, or the data.json secret-shaped-field audit; §16 lists no discouraged-plugins content.
- **Prompt-assembly hygiene elaborated + 'ordering is not a security control' downgrade**
  - note: §12 has only A15's one-liner 'injection-as-untrusted-data.' The beta elaboration (nonce-delimited data blocks with escaped nonce, frontmatter/%%prov%%/taint stripped pre-prompt, instructions restated after every untrusted block, stop sequences, no-CoT-over-untrusted, existence-gating/abstain, fail-closed local-only exclusion) and the explicit 'ordering is NOT a security control — containment carries security' downgrade are absent.
- **'verified' copy permitted in beta vs alpha forbids it; rg-gate enforcement dropped**
  - note: §5 states A15's 'checked ≠ approved,' but the doc never states beta PERMITS the word 'verified' (qualified 'against anchors') or that the alpha rg-gate that forbade approved/verified/trusted terminology is dropped (enforcement rule survives only informally).
- **'Each tier must beat a cheap baseline or be disabled' rule dropped**
  - note: No mention of the per-tier discipline that each judgment tier must beat a declared cheap baseline (cosine/BM25) on its fixture or be disabled; only the adjacent silence-isn't-absence principle is implied elsewhere, not this beat-a-baseline-or-disable gate.
- **Human+machine epistemic-equality framing + evidence base not carried into beta**
  - note: The ADR-128 epistemic-equality thesis (human-authored notes and LLM digests epistemically equal — same envelope/checks/journal/rollback; who confers no trust) and its inlined evidence (Jacobs 2021 N=220, AutoResearchClaw HITL ablation, Feng & Liu verification asymmetry), and that beta does NOT carry them, appear nowhere. §4/§5 discuss the provenance/verdict-marker DISTINCTION, the opposite emphasis, but not this reversal's non-carry.
- **§13 flip conditions for agent/LLM allocation (new in beta)**
  - note: No recorded agent/LLM-allocation flip conditions appear (synthesis losing to ChatGPT → bounded multi-step compose; agent-host/MCP fragmentation → first-party worker-client; conversation de facto primary; scope leaving single-researcher-local → Memoria-owned pinned-model host).
- **ADR-11 vault-eval amendment (diagnostic-never-gates) not carried**
  - note: No mention of whole-system vault-eval, the ADR-129 amendment that vault-eval verdicts remain diagnostic and never gate work, or the eval select-models build-time-model-gating carve-out; §9 mentions per-engine fixture re-validation but not this amendment or its non-carry.
- **SECURITY.md doc (guarantees table, residual list, per-incident fixture ratchet)**
  - note: No SECURITY.md doc is specified anywhere — no guarantees table, data-flow statement, honest residual-risk list (compromised OS user, provider sees non-local-only text, semantic persuasion, no macOS/Windows OS egress enforcement), or the grow-by-one-per-incident poisoned-fixture CI ratchet.

## slice: surfaces-obsidian-onboarding-reading-platform  (10 missing)
- **Write-event push over WebSocket (vs polling file-watcher)**
  - note: No mention anywhere of write-event push, WebSocket, a daemon→plugin refresh channel, or the 'Obsidian's watcher provably misses external writes / stale badge = honesty violation' rationale.
- **Lockstep version handshake vs free-schema versioned --json**
  - note: No mention of a plugin/daemon/CLI version handshake, refuse-on-mismatch, lockstep single-version coupling, or the reversal of A15's free-schema stance ('--json is a versioned contract; the schema stays free behind it').
- **Agent view-specs land as data through the request envelope (new post-beta capability)**
  - note: §11 covers agents-as-clients under the read-API/request envelope (quarantined, read-scoped), but never the agent-submitted view-spec INSTANCE path (mandatory proposed-view container, provenance banner, engine-clamped loudness, enqueue-only vocab; needs its own adapter ADR + deceptive-layout fixtures).
- **reading disposition frontmatter (skip/cite-only/read) — new researcher-set key**
  - note: No mention of the researcher-set `reading` disposition key (skip/cite-only/read) settable via Zotero tag/color or on the work page; §3/§4 discuss frontmatter but not this 'only new frontmatter.'
- **VS Code extension variant (Appendix A) — new design artifact**
  - note: The doc's appendix is the three-lists, not a VS Code design. No mention of a VS Code second-client, the shared host-free ViewSpecRenderer+DaemonClient package, the four contributions, the DocumentLinkProvider resolving evidence anchors, or the M×N dividend.
- **Rejection→fixture ratchet on reviewed engine outputs (new)**
  - note: No mention of rejected engine outputs being auto-added as candidate regression-fixture cases (mirroring the per-incident security-fixture ratchet); §5 covers the review/disposition surface only as disposition, not fixture promotion.
- **Finding content-hash identity (suppress-while-open, re-emit on re-break)**
  - note: No mention of machine findings carrying a content-hash identity that is suppressed while an open item exists and re-emitted only after the state cleared and re-broke.
- **Model-generated markup rejected on Obsidian-no-sandbox / lethal-trifecta grounds (elaborated)**
  - note: The write-time exfiltration linter (§12) is a different mechanism. The doc never states the §15 rejection rationale: Obsidian has no plugin sandbox and can't add one; plugin holds full FS/network/Node access while the vault holds untrusted PDF text = lethal trifecta with a proven markdown-image exfil channel; revisit bar set deliberately high.
- **Nav rail / spaces markdown pages / how-to-guide links removed**
  - note: No mention of removing the _nav.md Navigator rail, the five spaces markdown pages, or the eranroseman.github.io how-to-guide link scaffolding in favor of Obsidian-native navigation + the wizard. §10's A15 side treats A15 as CLI-first and never names these shipped artifacts. ('spaces' in §10 refers only to the hub navigation-spaces concept.)
- **Workflow-audit (§12) and docs-history (§14) pointers — new process artifacts**
  - note: No mention of the beta.1 §12 workflow-audit pointer (scratch/workflow-audit/exec-plan.md) or the §14 docs-history borrow/adapt/reject archaeology whose outcomes are folded into the beta.1 body.

TOTAL MISSING: 74


# PARTIAL findings (theme touched, material nuance dropped)

## slice: host-runtime-cli-daemon  (17 partial)
- **Two-process supervisor/worker split with credential-free worker**  [doc §1]
  - §1 names 'supervisor + per-run worker' and §12 says 'engines get no sockets,' but the credential boundary itself is not spelled out: supervisor holds the key while the per-run worker is credential-free and socketless, spawned per run with a per-spawn token over a Unix domain socket, filesystem = staging + read-only vault.
- **Scheduled/background work: external cron vs daemon-resident scheduler**  [doc §1]
  - §1 contrasts A15's external scheduler-invokes-CLI (cron/systemd/Task Scheduler, --schedule-id) with B1's resident daemon, but does not state that scheduling moves INTO the daemon via a named JobRunner with heartbeat rows, idempotency keys and bounded retries (scheduled runs bounded by triage batch size).
- **ADR-95 nightly/scheduled discovery loop reversal**  [doc §8]
  - §8 mentions the 'scheduled citation monitor' as part of B1 discovery, but never frames it as the reversal of ADR-95's nightly-loop rejection (rejected in A15 precisely because an unattended always-on loop conflicted with no-daemon; the charter exclusion is now amended).
- **Persistent cross-session job queue + batch-API polling**  [doc §13]
  - §13 names the batch API only as a 50% price mechanism. The DB-persistent cross-session job queue, batch-API digest jobs polling across sessions, 'work survives restarts,' and the heartbeat-appended-only-after-clean-completion (missing-heartbeat = silent-non-execution detector) are not stated.
- **Runs-rule-time as ruling idea (operations vs runs)**  [doc §13]
  - §13 describes the 4-step batch protocol and one-click revert, and revert/cost/Review appear separately, but the 'runs rule time' ruling-idea reframe is not stated: every machine write belongs to exactly one run_id that keys snapshots/revert/cost/Review/incidents, whole-run atomicity, the completed_partial exception, and the operation→run rename.
- **VaultWriter single-writer ruling idea + linter ownership**  [doc §12]
  - §12 states the VaultWriter-owned exfiltration linter (linter-ownership covered), but the 'a single writer rules safety' ruling-idea elevation — exactly one code path (VaultWriter) ever writes vault files, sole machine writer, every invariant enforced there once, never per engine — is not stated.
- **Component roster / named services architecture**  [doc §12]
  - Named services appear scattered (Guard §12, Ledger/MigrationRunner §13, VaultWriter §12, ViewSpecRenderer §10), but the whole-system re-articulation as a discrete named-service roster with hard cross-process boundaries is never presented: Supervisor {JobRunner, Catalog, ProvenanceService, SnapshotService, DB-RPC}, Worker {Extractor, DigestEngine, AskEngine, Retriever, Checker, DiscoveryEngine}, Plugin {SurfaceViews, ProvenanceRenderer, DaemonClient}.
- **'Operations' vs 'Engines' as the machine-work unit**  [doc §6]
  - §6 captures operation-manifest/method_class → prose 'operation test,' and the appendix lists operations-as-product-code as removed, but the shift of the machine-work unit to distinct named 'engines' (Extractor/DigestEngine/AskEngine/Retriever/Checker/DiscoveryEngine), with 'operation' surviving only as the LLM-call litmus, is not stated.
- **Backup / restore machinery**  [doc §13]
  - §1 lists backup/restore and §13 mentions VACUUM INTO, but the restore --verify semantics and the per-store backup-status doctor on the read-only inspector (git / DB / payloads each configured-or-not, a report that warns before disk-death) are not stated.
- **LSP lifecycle: discovery, spawn, supervision, crash-loop give-up**  [doc §1]
  - §1 and the appendix name 'LSP lifecycle' and 'supervised by the plugin,' but the concrete client-owns-server lifecycle is not stated: port-file + per-install token discovery, Host-header/DNS-rebinding validation, spawn on onLayoutReady, initialize version/capability handshake, parent-death watch, and crash-loop give-up after five crashes in three minutes at block loudness.
- **Retrieval substrate: qmd vs in-DB FTS5 + sqlite-vec**  [doc §6]
  - §12 names qmd only as an A15 Node dependency and §6 mentions 'local embeddings/NLI' for contradiction detection, but the retrieval-substrate swap itself is not stated: qmd external retriever dropped, replaced by in-DB FTS5 + sqlite-vec in the single SQLite file, with bge-small int8 ONNX + NLI models installer-bundled and never downloaded at runtime, no external retrieval process.
- **LLM runner stack and provider model**  [doc §13]
  - §13 covers BYOK + batch API (50%) + price table, but the runner-stack change is not stated: pydantic-ai dropped as the typed runner, the two-provider local(Ollama)/gateway(OpenAI-compatible) split + per-operation --mode dropped, in favor of one pinned LLM host and a single pinned model tier for the whole beta.
- **Egress allowlist: workspace config surface vs compiled-in**  [doc §12]
  - §12 notes A15 'operation-scoped + allowlisted' egress → 'brokered egress via Guard,' and §9 notes Crossref-for-retraction, but the specific delta is not stated: providers.yaml config surface replaced by a compiled-in allowlist (no config surface), Unpaywall + Semantic Scholar + the cron-refreshed Retraction Watch CSV dropped, and citation.doi.org / api.github.com / Zotero-localhost added.
- **Distribution artifact / language runtime**  [doc §1]
  - §1 contrasts A15 pip-install with a B1 binary-acquisition flow, but the artifact/runtime delta itself is not stated: a single self-contained static binary with installer-bundled ONNX embedding/NLI models (pdfium chosen to match) vs a Python 3.12 app with a per-vault .memoria/.venv and no bundled local models.
- **Single artifact identity: `memoria` vs `mem` / `mem serve`**  [doc §1]
  - The doc uses 'memoria init/doctor' for A15 and 'mem'/'mem serve' for B1 throughout, but never flags the rename or the 'same artifact, daemon folded in as mem serve, no separate memoriad, one distribution story/one version' point as a delta.
- **Scoped MCP transport role**  [doc §11]
  - §11 demotes any agent (incl. the researcher's own Claude Code/Cursor clients) to a deferred client under ADR-130 ('chat additive, never primary'), which conveys the demotion, but it does not name that alpha.15's scoped MCP (memoria mcp --read-scope, FastMCP stdio) was one of the three primary A15 surfaces now removed from the centered host/surface model.
- **`hat` concept/command**  [doc §1]
  - §1 lists 'hat' among the B1 CLI commands, but the hat concept — events carrying a hat column alongside the closed verb enum, and its relation to (possible reframing of) A15's 'spaces' — is not stated.

## slice: vault-layout-concept-model  (11 partial)
- **Controlled vocabulary / vocab / ID-backed tags removed**  [doc §3]
  - §3 mentions 'ID-backed controlled-vocab tag' only inside the hub bullet. The distinct delta — vocab command removed, system/vocabulary.md SSOT removed, tags become freeform strings, membership-by-checked-tag gone — is never stated on its own.
- **fulltext.md materialization + anchor scheme change**  [doc §2]
  - §2 covers the fulltext DB→file materialization. The anchor-format half of the delta is dropped: the A15 W3C TextQuoteSelector side is never stated, and §16's '^pNNNN anchor bridge' gives only the B1 side without framing it as a change from W3C selectors.
- **report file class + reports/ folder (new)**  [doc §2]
  - §2 (adds reports/) and the §3 enum cover the new class and folder. The material retrieval-exclusion rule — reports are structurally never indexed for Ask and never citable as compose evidence — is dropped.
- **conflict file class (new)**  [doc §3]
  - 'conflict' appears in the §3 enum verbatim, so the class value is present, but it is lumped in the enum with no statement that it is a new file class originating from the batch/revert protocol (§13 discusses revert without connecting it to a conflict file class).
- **ruled-out file class (new)**  [doc §3]
  - 'ruled-out' appears in the §3 enum, but only lumped; the doc never states it is a new machine-scaffolded, body-stays-human class, nor the A15 'exploration traces → project working notes' contrast.
- **spaces/ navigation surface removed**  [doc §10]
  - §10/§2 give the B1 replacement (index.md/log.md projections) and discuss navigation, but the doc never mentions the A15 spaces/ dashboard folder (spaces/*.md embedding knowledge.base views); it characterizes A15 navigation as knowledge/_views/, so the specific spaces-surface removal is not stated.
- **catalog/{sources,entities} folders removed**  [doc §2]
  - The catalog-into-record.md folding is covered (§2/§7). But the doc characterizes A15 catalog as SQLite records, not as a catalog/ folder tree, and never mentions the entities/ folder or author/venue entity pages being removed ('graph is queries, never vault sprawl').
- **journal/ folder → root log.md projection**  [doc §10]
  - The B1 log.md projection is present (§10/§2), but the A15 journal/ folder (installer skeleton, git journal anchoring) is never mentioned as a vault folder being removed, so log.md is not framed as its replacement.
- **aliases field dropped**  [doc §4]
  - §4 lists 'aliases' among A15 universal-frontmatter fields and says B1 drops universal-frontmatter-on-every-concept, but the specific 'aliases field removed from the contract (citekey serves as human alias)' delta is lumped into the universal-frontmatter drop rather than stated individually.
- **inbox/ reframed as staging outside the bundle**  [doc §2]
  - §2 covers inbox/ re-introduced as staging, but the specific delta — inbox conceptually OUTSIDE the OKF bundle, with explicit no-silent-mint / awaiting-import-chip semantics — is dropped.
- **local-only/ folder + local-only marker (new)**  [doc §2]
  - §2's B1 layout lists the local-only/ folder, but the local-only: true frontmatter marker and its prompt-assembly enforcement are not stated.

## slice: provenance-frontmatter  (9 partial)
- **block-level %%prov%% trailing comment markers (new construct)**  [doc §3 (line 34), §4 (line 40), §5 (line 46)]
  - The %%prov:h|m|mc%% block marker is named in three places, but it is lumped into the single 'in-file provenance/verdict markers reintroduced' gap alongside the mem-* frontmatter keys. Dropped: the framing as an entirely new per-block in-body construct with no A15 antecedent, and the trailing-comment-per-block syntax with run:r_ and t:u fields.
- **human-vs-machine detection: absence-of-marker convention vs DB-recorded authorship**  [doc §4 (line 38)]
  - §4 line 38 quotes 'absence of all mem-* markers = human,' but uses it to support the universal-frontmatter-drop gap. Dropped: that this presence/absence convention is now THE human/machine detection rule replacing DB-recorded authorship, and that zero-migration hangs on it (zero-migration not mentioned anywhere).
- **x.* extension-key namespace dropped/deferred; bare mem-* keys used instead**  [doc §4 (line 39); appendix list 1]
  - §4 line 39 and appendix list 1 both state the x.* extension namespace is dropped. Dropped nuance: that x.* confinement was explicitly deferred and that Memoria's own mem-* producer-extension keys are therefore written bare in the meaning layer — the doc names mem-* keys (§4 line 40) and the x.* drop separately but never connects them as 'bare mem-* replaces x.* confinement.'
- **superseded retrieval behavior: never-excluded vs excluded-by-default with counted override**  [doc §9 (line 67); §8 (line 62)]
  - §9 line 67 names the Crossref retraction check (part of the delta) and §8 lists 'superseded' as a retrieval-coverage cause. Dropped: the core behavior shift — B1 excludes superseded chunks by default (reversing A15's explicitly-rejected hard-exclusion / always-retrievable stance), counts the exclusion in the denominator, offers a per-Ask historical override, and export refuses to cite.
- **monotone taint as an in-file never-green badge dimension (mem-taint / t:u)**  [doc §4 (line 40)]
  - mem-taint is named as one of the reintroduced in-file markers. Dropped: that taint is a monotone, permanent, never-removed, independent badge dimension that never turns green regardless of checking (set at creation, inherited by consumers), and the contrast that A15 had no in-file taint marker at all (taint was DB/attention-side). The t:u block-marker form is also not called out.
- **mem-flags deterministic distrust flags in frontmatter**  [doc §4 (line 40); §9 (line 68)]
  - mem-flags is named as a reintroduced in-file marker (§4) and deterministic distrust flags truncated/abstract_only are named at the extraction layer (§9). Dropped: the specific delta that these distrust flags are now surfaced into frontmatter (mem-flags: [abstract_only]) mirrored by a works.distrust_flags DB column, whereas A15 kept distrust/degradation as DB/attention-only signals — the two facts are never joined into this delta.
- **mem-run run-id written into frontmatter**  [doc §4 (line 40)]
  - §4 establishes the umbrella that mem-* producer keys are written to frontmatter, but its explicit enumeration lists only mem-prov/mem-taint/mem-flags and omits mem-run; 'run-id'/'run_id' appears nowhere in a file context. The specific delta — the authoring run-id written into frontmatter where A15 kept runs/journal exclusively DB-side — is not stated.
- **human-span manifest hash computed over in-file provenance blocks**  [doc §5 (line 45)]
  - §5 line 45 names the human-span hash manifest with pre==post byte gate and auto-rollback, but frames it purely as a byte-immutability integrity mechanism. Dropped: that the human span set is defined as blocks whose effective provenance is h (file default minus in-file %%prov%% block exceptions) and the manifest hash is recomputable from the file's in-file markers alone, rather than from DB-recorded provenance.
- **aliases and archived frontmatter keys dropped**  [doc §4 (line 38)]
  - aliases and archived are named, but only in §4 line 38's description of A15's universal frontmatter. Dropped: the delta that B1's frontmatter model omits both keys even on Memoria-created files (with retraction handled via superseded instead of archived) — their removal is subsumed under the broad universal-frontmatter drop, never stated as its own delta.

## slice: store-catalog-snapshot-migration  (11 partial)
- **Enrichment-gates-promotion removed; land-then-resolve, flags never block**  [doc §6]
  - §6 captures 'enrichment is empty-field-fill only' (JabRef rule) and §4 captures 'flags never block.' But the central ADR-123 reversal — that enrichment no longer gates promotion to checked/bibliography eligibility (land-then-resolve, 'phase 1 never fails,' bibliography complete across non-Zotero works without a checked gate) — is not stated. The doc frames enrichment as a merge-policy change, not as removal of the promotion gate.
- **Provider set and resolution channel changed; Unpaywall dropped; providers.yaml gone**  [doc §9]
  - §9 touches provider roles (OpenAlex fills empty fields; Crossref for retraction + title fallback). But the specific deltas are all absent: the citation.doi.org content-negotiation transport (and DataCite registry-agnostic CSL-JSON), Unpaywall being dropped, no OA-PDF fetcher, and provider policy moving from vault-config providers.yaml to a compiled-in allowlist ('no config surface'). None of 'Unpaywall', 'providers.yaml', 'citation.doi.org', or 'DataCite' appear anywhere.
- **references.bib renamed to bibliography.bib and split into two artifacts**  [doc §7]
  - The rename is captured implicitly: §7/§2 show A15's root references.bib → B1's Memoria/bibliography.bib. But the second half of the delta — a NEW per-export projects/<p>/export/references.bib containing cited works only, emitted at export time — is not stated anywhere. The doc has no export-artifact section covering it.
- **Work state model: binary checked/unchecked → scalar status lifecycle**  [doc §9]
  - §9 mentions the distrust flags (truncated/abstract_only) and one status value (extract_failed:needs_ocr), so the theme is grazed. But the core delta — replacing A15's binary checked/unchecked quarantine gate with a scalar resting-state lifecycle (new→cataloged→…→digested), with 'partial states legal,' 'indexed' as payoff, and 'unsearchable' as a derived predicate — is not described. 'works.status' appears nowhere; §5 still frames B1 in quarantine-and-verify terms.
- **Provider payloads: replayable blob store → operational-ledger, not-rebuilt**  [doc §6]
  - §6 mentions in passing that B1 keeps 'raw responses in a payloads store.' But the actual delta is dropped: reclassifying provider payloads from a replayable content-addressed-blob recovery class to a non-rebuildable operational-ledger table ('backed-up not rebuilt'), and relocating them from in-vault .memoria/blobs/provider-payloads/ to a per-device table beside memoria.db. The recovery-class reclassification and relocation are not stated.
- **Explicit derived-vs-operational-ledger DB-class split (new taxonomy)**  [doc §13]
  - §13 touches DB recovery via 'DB reconciliation' and §1 lists a 'rebuild' verb, but the doc never states B1's explicit two-class taxonomy — DERIVED state (chunks/FTS/vectors, links, anchors, edges, entities, works.status, checks verdicts) rebuildable via mem rebuild vs OPERATIONAL LEDGERS (runs/jobs/events/costs/incidents/guard_log/payloads) not rebuildable, preserved by VACUUM INTO + selective export — nor the new anchors-geometry-rebuildable-from-PDF claim. The reframe away from ADR-127's file/blob-replay/DB-accepted-loss triad is not captured.
- **DB rollback mechanism: DB inverse transaction → reconciliation from files**  [doc §13]
  - §13 captures the core: B1 revert uses 'DB reconciliation' distinct from A15's journal cascade-rollback (i.e. re-derive from files vs replay a stored inverse transaction/snapshot). But a material part is dropped: that B1 deliberately leaves the operational ledgers (runs/jobs/events/costs/incidents/guard_log) UNTOUCHED by revert (history appends), and 'no record.md ⇒ no work row.' That intentional carve-out is not stated.
- **One-click revert UX (inverse-diff preview, conflict diversion, byte-check) is new**  [doc §13]
  - §13 names 'one-click revert with inverse-diff,' §1 lists the revert-run verb, and §3's type enum includes 'conflict' — so revert-as-user-surface and the conflict file class are present. But the specific product mechanisms are dropped: per-file dry-run classification (clean-restore | delete-created | conflict), diversion of inverse hunks to <name>.conflict.md + an incident when a later human edit overlaps, and the runtime human-block byte comparison that aborts on any difference. Revert-being-a-run-itself and 'cost rows never deleted' are also not stated.
- **Backup contract: streaming replication (Litestream) → VACUUM INTO snapshots; doctor → inspector pane**  [doc §13]
  - The replacement pieces appear separately — VACUUM INTO in §13, a read-only inspector pane in §10 — but neither is tied to the backup contract. The actual delta is not stated: B1 drops SQLite streaming replication (Litestream) in favor of VACUUM INTO single-file snapshots + restore --verify, moves the backup-status report from a memoria doctor command to the inspector pane, and (§18) explicitly declines streaming-replication RPO for beta.1. 'Litestream'/'streaming replication' appear nowhere.
- **Migration ledger table vs bare user_version integer**  [doc §13]
  - §13 covers the migration runner as a blocker (gap 23) but not this distinct delta: A15 tracked schema evolution with a bare PRAGMA user_version integer gating .sql DDL, while B1 adds a structured migrations(n PK, name, kind, applied_ts, git_pre_tag, db_snapshot, status) ledger recording a per-migration git pre-tag + VACUUM INTO snapshot + status, plus meta.schema_version. Neither 'user_version' nor the migrations ledger table appears.
- **Run-kind enum and runs-rule-time keying vs operation model**  [doc §13]
  - §13 uses 'per run' / batch protocol and §1 lists 'revert-run', so the run vocabulary is grazed. But the doc never states the reframe itself: that B1 replaces the A15 'operation' unit with an enumerated run (fixed run-kind enum capture|extract|digest|import|ask-materialize|export|migration|merge|revert|discovery|rename), that every machine write belongs to exactly one run_id, and that a runs table keys snapshots/revert/cost/Review/incidents — elevating revert/migration/rename/discovery/export to first-class run-kinds. 'run-kind' and 'runs table' appear nowhere.

## slice: ingest-extraction-enrichment  (12 partial)
- **Zotero capture posture reversed (optional file-adapter → Zotero-first live API)**  [doc §7, §16]
  - §7 says 'Zotero import is day-1' and 're-centers Zotero as the primary reading/capture surface; A15 demoted it to optional.' That captures the optional→primary posture flip. But it drops the specific mechanism/delta: the live Zotero 7 Local HTTP API (127.0.0.1:23119, Guard-allowlisted) as a first-class LIVE capture channel, the A15 baseline of accepting only PORTABLE files, and that ADR-124 explicitly kept the live Zotero API OUTSIDE the core (this is a decision reversal, not just a re-weighting). zotero_key idempotent upsert also absent.
- **Required provider set / Unpaywall dropped**  [doc §6, §9]
  - §6/§9 enumerate the surviving providers ('OpenAlex singleton'; 'Crossref used for retraction check + title fallback'), which implicitly narrows the set. But the doc never names Unpaywall (grep: 0 hits), never states the A15 three-required-provider DOI gate (crossref+openalex+unpaywall in providers.yaml branches.doi.required) that blocked promotion, and never says the OA-locator/Unpaywall path was removed entirely. The specific 'required-provider gate collapsed and Unpaywall dropped' delta is absent.
- **Reference edges are single-source, not a DOI-deduped multi-provider union**  [doc §6, §9]
  - §6 names 'references DOI-deduped union' as an A15 feature and §9 says B1 'stores outgoing reference edges' (from the OpenAlex response). The union loss is thus lumped into the field-authority-merge gap in §6, but the doc never states the specific delta that reference EDGES are now single-source with no cross-provider DOI-dedup — a reader could miss that references lost their union.
- **Cited-by capping discipline (new, explicit)**  [doc §9]
  - §9 mentions 'capped cited-by edges (edges table)' but stops there. The discipline that makes this a gap — idle background backfill, top-N citers per work with a per-run ceiling, NO forward-of-forward (second-hop) expansion, and mandatory cap-stamping on every consumer ('among top-200 citers per seed') — is entirely dropped.
- **Memoria-side OA-PDF / full-text acquisition removed**  [doc §7]
  - §7 captures the replacement path ('PDF acquisition via a capture in Zotero handoff'). But it never states that A15 had auto OA-PDF acquisition (via Unpaywall/OpenAlex/S2 OA locations) as BASELINE that blocked the checked digest, that B1 REMOVES that fetcher on security grounds (auto-fetch = allowlist violation + zero-interaction beacon), the no_pdf resting state, or the deferred named-OA-host fallback (arXiv/Europe PMC).
- **Retraction check moved from capture-time promotion gate to weekly audit + export gate**  [doc §9]
  - §9 notes a retraction check exists ('Crossref used for retraction check'). But the actual delta — that it MOVED off the alpha.15 enrich-source promotion gate (leaves source unchecked + attention) to a scheduled WEEKLY citation audit plus an EXPORT-time refusal on cited retracted/superseded works — is not stated anywhere. Only the bare existence of a check is noted, not its relocation.
- **Blob storage location (in-vault .memoria/blobs → out-of-tree payloads + in-vault PDFs)**  [doc §2, §6]
  - §2 captures the in-vault PDF ('source.pdf (blob)' co-located in works/<citekey>/). But the location delta for PAYLOADS is missing: the doc never says provider payloads move OUT of the synced tree to sit beside memoria.db in the per-device data dir, nor that A15 stored both source-content and provider-payloads in-vault under .memoria/blobs. §6's 'payloads store' doesn't state where it lives.
- **Citation authority softened (catalog-is-authority → correct-at-source in Zotero)**  [doc §7]
  - §7 captures the catalog-authority→record.md shift and that bibliography.bib stays Memoria-generated. But the specific delta — that citation-facing fields owned by the researcher's Zotero are corrected AT SOURCE (deep link to the Zotero item; next sync confirms) rather than locked vault-side, partially reversing ADR-124's 'Memoria catalog is THE citation authority' — is not stated (grep: 'correct-at-source' 0).
- **Digest aspects: deterministic work_aspects (6 kinds incl assumption) → LLM digest claims (5 kinds)**  [doc §6, §2]
  - §6 lists 'digest' in the B1 LLM roster and §2 mentions 'digest.md (LLM digest),' touching the LLM-producer side. But the doc never establishes A15's deterministic work_aspects read-model (context/key_idea/method/outcome/limitation/assumption), so the actual delta — deterministic CSL/section-derived aspects (6 kinds) → LLM-extracted NLI-checked anchored claims (5 kinds, no 'assumption', projected_impact excluded) — is not stated (grep: 'work_aspects' 0, 'assumption' 0).
- **Digest/runner batch provider assumption (OpenAI-compatible → Anthropic Message-Batches)**  [doc §13]
  - §13 captures 'batch API (50%)' and BYOK as new cost machinery. But the transport-provider delta is not stated: A15's two OpenAI-COMPATIBLE runner providers (Ollama + OpenAI-style gateway, pinned per operation) → B1's Anthropic Message-Batches endpoint with one pinned model tier (grep: 'Message-Batches' 0). §6 discusses method_class/runner policy but not the provider swap.
- **Work lifecycle state machine restated ('indexed' payoff, distrust_flags array, unsearchable derived)**  [doc §9, §8]
  - §9 mentions 'deterministic distrust flags (truncated/abstract_only)' and §8 lists 'unsearchable/partial/superseded' as gap causes — touching two pieces. But the concrete lifecycle state machine (new→cataloged→{no_pdf|pdf_stored}→extracting→{extract_failed|extracted→INDEXED}→opt-in digest chain), 'indexed' as the Ask-payoff state at $0, the distrust_flags ARRAY, and 'unsearchable' as a DERIVED (not stored) predicate are not stated (grep: 'indexed' 0).
- **Visible staging inbox chip + dead-letter triage stub (no-silent-minting)**  [doc §2]
  - §2 captures the re-introduction of 'inbox/ (staging).' But the import-fragility discipline that is the real delta is dropped: files land freely in the drop folder, imports mint ONLY via the 'N files awaiting import' chip on researcher action (never silently), every input lands as an always-succeeds catalog stub, a failed import materializes a VISIBLE dead-letter triage stub (no captured source silently lost), and the post-bulk metadata-cache-idle wait before surfaces read.

## slice: ask-compose-discovery-gap-cost  (16 partial)
- **Coverage denominator + suspect accounting is new**  [doc §8]
  - §8 states the new 'printed denominator' and itemization 'by cause (unsearchable/partial/superseded)' — the headline is covered. Dropped: the truncated/abstract_only 'suspect-not-covered' accounting and the 'superseded excluded-but-counted' semantics, i.e. the specific searched-vs-total counting rules.
- **Scheduled citation monitor reintroduced**  [doc §8]
  - §8 names the 'scheduled citation monitor' as present in B1, and §1 covers the new resident daemon. But the actual delta — that A15 explicitly REJECTED scheduled/always-on discovery (ADR-95) and B1 re-introduces it (enabled by the daemon), one capped machine note per schedule — is not stated; the monitor is presented as plain B1 fact, not as a reversal.
- **Forward cited-by snowball + capped cited-by backfill**  [doc §9]
  - §9 mentions 'capped cited-by edges (edges table)' are stored — the storage side is covered. Dropped: that snowball now runs FORWARD from the cited-by cache (not just backward refs) and the idle background backfill job (top-N per work, per-run ceiling, no forward-of-forward).
- **Seeding relevance prefilter as a paid LLM call**  [doc §6]
  - §6's LLM roster lists 'seeding relevance prefilter' and §15 mentions an 'optional relevance prefilter,' so its existence as an LLM op is present. Dropped: the delta framing that A15's seeding relevance was DETERMINISTIC (steering.md) and B1 turns it into a priced/paid LLM call in the §13 cost roster.
- **Hybrid vector retrieval substrate mandated for Ask**  [doc §6]
  - §6's LLM roster lists 'local embeddings/NLI,' implying vector capability landed. But the doc nowhere states the substantive delta: that A15 deferred hybrid (kept qmd/FTS5) and B1 MANDATES a fully-specified BM25 top-100 ∪ vector top-100, RRF fusion, per-work cap 8, MMR-to-40 Ask retrieval pipeline. The retrieval-substrate change is not stated as such.
- **Pinned bundled local models specified**  [doc §6]
  - §6 acknowledges 'local embeddings/NLI' exist and §1 mentions a binary-acquisition flow generally. Dropped: the specific pins (bge-small-en-v1.5 int8 ONNX, deberta-v3-base-mnli-fever-anli int8 ONNX), installer-bundled/never-runtime-downloaded, pins recorded in meta, and load-time hash mismatch ⇒ refuse mem rebuild --embeddings.
- **Per-quote NLI stance chips in Ask**  [doc §6]
  - §6 states B1's 'contradiction detection = NLI within the retrieved set,' which touches the contrasts axis. Dropped: that Ask's read path computes per-quote 3-way stance chips (entail→supports / contradict→contrasts / else→mentions), probability-on-hover-never-aggregated, and the 'empty list is never absence-of-disagreement' framing — the Ask-time stance-chip affordance itself.
- **paper_plan / outcome_frame / frame-paper machinery absent**  [doc §3]
  - §3 covers that B1 drops the paper_plan/outcome_frame/thesis-role project concept. Dropped: the specific operation-level machinery removal — the `project frame-paper --frame-file` operation, structural paper-readiness gaps, and the `project export --ready-only` variant with paper-plan rendering (PR-O).
- **`draft.md` human-class local-only deliverable (new)**  [doc §3]
  - §2/§3 list draft.md as one of the project files. Dropped: that draft.md is human class, local-only:true by default, machines never write it, and its blocks still join the manifest — i.e. the protected human-only nature that is the actual delta.
- **Export readiness-gate criteria changed**  [doc §5]
  - §5 touches the export gate ('blocks, does not edit'), the no-write-time-oracle property. Dropped: the actual criteria rewrite — refuse on an unresolved citation, a cited superseded/retraction-flagged work, or a mapped project claim with no mc/Checker anchor AND no gap-analysis missing-evidence marker — replacing A15's paper-frame-completeness gate.
- **Export is a revertible run with snapshot**  [doc §12]
  - §12 notes the exfiltration linter runs 'at apply + export,' and §13 covers the general batch/revert substrate. Dropped: that export is itself a run-kind (`export`) needing a snapshot to be revertible, rewrites span links to [@citekey, para N], and emits export/draft.md + references.bib with a linter re-pass over both.
- **Batch-API 50% / cache-zero cost previews**  [doc §13]
  - §13 names 'batch API (50%)' — the batch-discount half is covered. Dropped: that cache savings are budgeted as zero (conservative cache accounting) in previews.
- **Circuit breaker (Ledger decides, JobRunner cancels)**  [doc §13]
  - §13 names the circuit breaker and its enforcement points. Dropped: the Ledger-decides (on streamed/polled usage) / JobRunner-executes-cancellation split and that remaining batch items are cancelled as cancelled(budget).
- **Soft/hard monthly budget**  [doc §13]
  - §13 states '$20/mo default soft budget' — the soft half is covered. Dropped: the researcher-set hard cap.
- **Unattributable-spend miss row + fail-safe breaker**  [doc §13]
  - §13 names the 'miss row for unattributable spend.' Dropped: the fail-safe rule that the breaker treats missing/unattributable usage data as over-budget evidence.
- **Pervasive dollar-preview UX**  [doc §13]
  - §13 mentions the price table and 'preview' as an enforcement point, so the preview concept is touched. Dropped: the delta that every paid operation surfaces a printable dollar figure BEFORE it runs ('Synthesize — $0.04'; 'Digest N works: est. $X (batch)') as a precondition of the call.

## slice: integrity-security-agent-judgment  (17 partial)
- **Two-tier demotion propagation + derivation graph removed**  [doc §5]
  - §5 second bullet references 'A15's propagation-only demotion' only as beta's stronger-gate claim. It never states the two-tier stale-vs-invalid model (depth-1 machine consumers demote / depth-≥2 stale-flagged-and-excluded / human descendants stay live ask-routed / retraction propagates one level deeper) nor the removal of the first-class consumed-at-verdict derivation/blast-radius graph.
- **Universal quarantine of all mutations → machine-writes-only staging**  [doc §5]
  - §5 names A15 quarantine-and-verify and §4/§5 note beta treats human content as trusted-by-default (absence of mem-* = human) and byte-immutable, but the doc never frames the delta as beta NARROWING the universal invariant so human/foreign edits (incl. references.bib/overrides.jsonl) are no longer non-consumable-until-verified and workspace-scan foreign-edit coverage is dropped.
- **Superseded/retracted: exclude-by-default (beta) vs never-hard-exclude (alpha)**  [doc §8]
  - §8 lists 'superseded' only as one retrieval-coverage-gap cause. It never states the default flip (alpha include-always-tagged/hard-exclusion-rejected → beta exclude-by-default with per-Ask historical override), that alpha explicitly REJECTED hard exclusion (TEMPO/cross-period), or the export-refuses-on-citing-superseded gate.
- **Operation-manifest / runner test-live modes / select-models machinery → single tier + operation-test litmus**  [doc §6]
  - §6 fourth bullet covers method_class/capability-manifest declaration → prose 'operation test' and 'no capability-manifest registry,' but drops the runner.test/runner.live dual-model pins, --mode test|live selection, eval select-models pinning the cheapest passing candidate, the manifest-as-hard-ceiling / untrusted-field-declaration aspect, and the single-pinned-model-tier-for-all-of-beta decision.
- **Journal row-hash-chaining + git tamper-evidence root removed**  [doc §13]
  - §13 covers the journal→git-snapshot substrate change and pathspec-limited commits, but never states the removal of the row-hash-chained SQLite journal + git-anchored .memoria/journal-head tamper-evidence root (nor the beta Mem-Run trailer replacement); the entire cryptographic-tamper-evidence dimension is absent.
- **2PC prepared/materialized journal states → 4-phase run batch protocol**  [doc §13]
  - §13 states beta's 4-step batch protocol (snapshot/apply/verify/commit) replacing 'A15's SQLite-journal cascade-rollback,' but does not name the formal 2PC with exactly two journal states (prepared/materialized), the write-ahead fsync-ordering contract, the one workspace write lock / workspace recover, or beta's run statuses + git pre-tags.
- **Deterministic argument-edge extraction + act-promotable allowlist removed**  [doc §3 (also §8/§9)]
  - §3 covers removal of the typed relation-links frontmatter map and §8 covers argument-graph gap-analysis removal, but the zero-LLM deterministic body-link→candidate-edge extraction on materialization and the act-promotable promotion allowlist ({mentions,related,cites,version-of}, argument-class supports/contradicts/extends never auto-promoted) are not itemized.
- **Gap-engine saturation block + {0,1,2} ordinal scoring removed**  [doc §8]
  - §8 covers removal of argument-graph/saturation/thesis-health gap analysis, but does not state the {0,1,2} ordinal gap-scoring removal or the specific ≥1-addressed-support-AND-≥1-counterpoint saturation-block-as-compose-phase-signal (advisory-never-gating) model.
- **Canary spec (MEMORIA-CANARY, weekly drill, engines-disabled) new in beta**  [doc §12]
  - §12 mentions 'canary drill' and the appendix lists canary as new-in-B1, but drops the fail-safe consequence (any hit ⇒ INC-canary incident + engines DISABLED) and the spec (MEMORIA-CANARY-<8hex> minted at install, weekly + post-migration, scans all artifacts/DB/logs/exports for it and key prefixes).
- **Compiled-in host allowlist enumerated in beta**  [doc §12]
  - §12 says A15 was 'operation-scoped + allowlisted' and B1 uses brokered egress, but never enumerates the concrete compiled-in host allowlist (pinned LLM host, api.crossref.org, api.openalex.org, api.github.com, citation.doi.org redirects-disabled, Zotero localhost; explicit no-OA-PDF-fetcher; deferred entries) or the per-entry audit and EgressDenied logging.
- **§13 operation-test decision framework + externalization of open-ended work to the user's agent**  [doc §6 / §11]
  - §6 states the 'operation test' litmus deciding which LLM calls ship and §11 states the agent role is externalized to the researcher's own client, but the unified in-product-vs-agent ALLOCATION framework — failing calls belong to the researcher's agent 'on their model and their bill,' synthesis 'deliberately redundant' with a frontier agent, envelope-writes-only — is not stated as a single framework.
- **Agent chat-transparency contract + per-agent read-scoping/poisoned-read fixtures not fully carried**  [doc §11]
  - §11 states beta's retained agent-as-client contract (read-scoped, quarantined, never file/terminal/exec/send, chat additive/never primary), but does not state that the full chat-transparency enumeration (plan/sources-read/assumptions/unknowns/pending-writes before enqueue), the poisoned-read + cross-channel credential-reuse fixture families, and the trusted-local-dogfood distinction are dropped/not carried.
- **HANS gate: runtime loud-degraded-mode (alpha) → launch-blocking acceptance test only (beta)**  [doc §6]
  - §6 third bullet mentions beta's HANS/PAWS launch-blocking acceptance test (as the calibration gate), but does not state that alpha's runtime loud-degraded MODE (Tier-1 HANS-failure routes cosine/BM25 candidates to ask, raises attention, never a silent no-op) is dropped — i.e., the runtime-fallback → one-time-launch-gate conversion delta.
- **surface-tensions standalone operation → contradictions folded into Ask**  [doc §6]
  - §6 says 'contradiction surfacing is NLI-only within the retrieved set,' which overlaps the fold-into-Ask idea, but does not name the removal of the dedicated corpus-wide surface-tensions OPERATION, its SUPPORTED/REFUTED/NOTENOUGHINFO contract / canonical-id pair dedup, or that contradiction detection now exists only as an in-Ask projection over the retrieved set.
- **Anchor re-anchoring: exact-then-fuzzy (alpha) → exact-only, fuzzy deferred (beta)**  [doc §16]
  - §16 mentions beta's exact-quote → ^pNNNN anchor bridge + orphan counting, but does not state alpha's re-anchor-never-silently-rebind/drop with exact-THEN-FUZZY re-anchoring or that beta drops fuzzy (explicitly DEFERRED until a measured orphan rate demands it); the narrowing delta is not stated.
- **'operations, never agents' naming vs beta 'engines' + Memoria-is-not-an-agent**  [doc §11]
  - §11 quotes 'Memoria is not an agent, and beta.1 adds none,' capturing the beta stance, but does not state the naming delta from the strict ADR-125 'operations, never agents' rule to beta's 'engines'/'operations' used interchangeably (the strict naming rule not being restated).
- **Automated Crossref retraction/EoC audit new in beta**  [doc §9]
  - §9 mentions 'Crossref used for retraction check' inside the enrichment-graph-narrowing bullet, but does not present it as a NEW automated weekly corpus-scoped Crossref retraction/expression-of-concern audit feeding proposed human-only-disposition flags (or the probe of whether Ask resurfaces flagged items unlabeled), nor the alpha-absent framing.

## slice: surfaces-obsidian-onboarding-reading-platform  (15 partial)
- **Two-process supervisor/worker daemon architecture (new)**  [doc §1, §12]
  - §1 names the 'two-process daemon (supervisor + per-run worker)' and §12 says engines get no sockets (credential-free worker, Guard brokers egress). Dropped: the IPC mechanics — per-spawn token over Unix socket/named pipe and exactly two ops (brokered egress + DB-RPC).
- **LSP daemon-lifecycle mechanics (new)**  [doc §1]
  - §1 names 'LSP lifecycle' but states none of the mechanics: port-file/per-install-token discovery, spawn-on-onLayoutReady, initialize handshake refuse-on-mismatch, parent-death watch, crash give-up after five-in-three-minutes at block loudness.
- **Surface decomposition: places/now spaces → action panes**  [doc §10]
  - §10 lists the five B1 surfaces (Capture/Reading/Ask/Compose/Review), but frames the A15 side as 'verb-first CLI + status --json,' not as the five markdown spaces (Now/Places: inbox/library/knowledge/project/maintenance) + _nav rail. The reorganization from place/queue-nav pages to interaction-mode surfaces is not articulated.
- **ViewSpecRenderer built as a shared renderer (vs view-specs as additive data only)**  [doc §10]
  - §10 + appendix name ViewSpecRenderer/view-spec.v1 and that B1 builds the renderer while A15 was CLI-first. Dropped: the A15-side origin (view-specs were an additive data-only field, no renderer) and the host-free TS-package + DaemonClient extraction for the VS Code variant.
- **view-spec.v1 vocabulary elaboration (per-row check_status, body_data, tripwire)**  [doc §10]
  - §10 names view-spec.v1 as the artifact but states none of the elaboration delta: per-block AND per-row check_status, untrusted text only as tagged body_data, and the ~two-dozen-kinds vocabulary-creep tripwire (retreat to view-spec islands).
- **Chrome-authored / projection-server-driven split (generative-UI doctrine)**  [doc §10]
  - §10 says B1 builds pane UI + a generative-UI renderer and (§10) Bases for meaning-only, loosely touching both halves. But the governing doctrine — 'the unit of generativity is the block, never the pane,' hand-built typed pane shells vs $0 deterministic server-driven content — is not articulated.
- **CLI binary rename memoria → mem**  [doc §1]
  - The doc uses both names (A15 'memoria init/doctor' vs B1 'mem engine'/'mem serve'), so the rename is implicit, but it is never called out as a delta, and the 'one artifact, no separate memoriad executable' decision is absent.
- **work interview (discuss-a-paper) command removed/replaced**  [doc §1]
  - Only implicitly covered by the generic CLI-demotion / 'verb-first CLI removed' items (§1, appendix); interview is never named, and the specific fold of discuss-a-paper into the Ask pane + Zotero-first Reading is not stated.
- **Reading elevated to a first-class surface (Zotero PDF reader + CM6 anchored-note)**  [doc §10, §16]
  - Reading is named as a surface (§10) and Zotero-first primary read is stated (§16), but the CM6 select-then-anchor 'New anchored note' gesture (also the full path for non-Zotero works) and fulltext.md-rendered-as-evidence-substrate are dropped.
- **Capture surface: inbox drop-folder + awaiting-import chip + commands**  [doc §2, §10]
  - §2 names inbox/ (staging) and §10 names the Capture surface, but the mechanics are dropped: the 'N files awaiting import' chip that mints imports on the researcher's action (never silently), the 'Add by DOI / Import Zotero' palette commands, and progress on the inspector strip / no dedicated Activity pane.
- **Plugin distribution/acquisition + code-signing + BRAT channel (new)**  [doc §1]
  - §1 bullet 4 covers catalog-clean plugin + consented/package-manager binary acquisition + code-signed Windows builds. Dropped: the BRAT beta channel, and the rust-analyzer-model detail (explicit-consent, checksummed, version-matched download; catalog ships only main.js/manifest.json/styles.css).
- **Read-only inspector pane replaces dataviewjs-polling dashboards**  [doc §10]
  - §10 names the 'read-only inspector pane,' but the delta is dropped: that it REPLACES the dataviewjs 30s-polling dashboards (board-state/audit-log/eval-trend) and removes the Dataview dependency, that it is write-event-push-fed, and its contents (daemon liveness, queue depth, recent runs, integrity verdict, budget, per-store backup).
- **Native Bases split (meaning-only Bases vs verdict-bearing read-API)**  [doc §10]
  - §10 names both lanes (Bases for meaning-only views; ViewSpecRenderer/read-API), but the formalized rationale is not stated: meaning-only frontmatter means native Bases structurally cannot carry check_status, so anything verdict-bearing must route through the read-API path, with Bases as the keep-test browsing layer.
- **Policy-gate plugin + audit.jsonl surface replaced by VaultWriter/Guard/Ledger**  [doc §2, §12]
  - VaultWriter, Guard, the exfiltration linter (§12), and manifest.jsonl in visible system/ (§2) are named. But the replacement delta isn't drawn: that these supplant the A15 MCP policy-gate plugin (.memoria/plugins/memoria-policy-gate/) and its audit-log.md dataviewjs dashboard; Ledger-as-event-log and deny-rate-spike-as-injection-signal are not stated.
- **Scheduled citation monitor runs under the daemon (reverses no-daemon rationale)**  [doc §1, §8]
  - §8 names the 'scheduled citation monitor' (under discovery) and §1 notes A15's external cron/systemd scheduler model, but the specific delta — that this scheduled work now runs INSIDE the resident daemon (with batch-API digest polling across sessions), reversing the no-daemon rationale that pushed scheduled work to external schedulers — is not connected.

TOTAL PARTIAL: 108


# CRITIC VERDICTS

## critic: cross-cutting-completeness
OVERALL: The gap doc is strong on the big architectural pivots (host, catalog authority, type model, integrity gate, cost/security machinery) but under-mines the shipped alpha.15 vault-template end-state and a few kept-accepted/single-source mechanisms: it misses the Librarian persona and the steering.md discovery-weighting substrate, mischaracterizes inbox/ and system/ as new-in-B1 when both already ship in A15 (with different semantics), omits the spaces/navigation layer (ADR-101, kept-accepted), the .memoria/plugins policy-gate hook system, ADR-127's demotion-propagation derivation graph, and the overrides.jsonl tiered-replay recovery. Not complete — ~9 material gaps, two of them high-severity inaccuracies.
- [high/missing-gap] Alpha.15's shipped end-state carries a named agent persona — 'The Librarian' — that reads steering.md at session start and weights/surfaces discovery, and the shipped eval bundle scores tasks on the Librarian's behavior. Beta.1 ships no agent at all (§13 'Memoria is not an agent, and beta.1 adds none'). The gap doc's agent section (§11) frames the loss purely as 'no resident Co-PI agent' under ADR-130, and never names the Librarian persona/session-loop that the vault-template actually shipped, so the reader misses that a concrete, user-facing agent role was removed.
    evidence: vault-template steering.md: 'The Librarian reads it at the start of every session and weights discovery by how well candidate papers match what's here … an empty or stale file produces an unfocused Librarian.' system/eval/find-transformer-paper.md and find-residual-learning.md scoring rubrics are written around 'The Librarian retrieves the catalog record …'. beta.1 §13 excludes any resident agent; the roster is digest/prefilter/synthesis + local models only. The gap doc §11 speaks only of 'no resident Co-PI agent … externalized to the researcher's own client' and cites ADR-130, never the Librarian.
    doc-loc: §11 Agent / autonomy — add the Librarian persona (the session-start, steering-weighted discovery agent shipped in vault-template/steering.md + system/eval/) as a distinct A15→B1 removal beside the Co-PI point.
- [high/missing-gap] Alpha.15 ships steering.md as a first-class human-authored 'program memory' substrate (priorities / open questions / synthesis gaps / papers-to-prioritize) that is the PI's main lever over what discovery surfaces; ADR-129 discovery-relevance scoring (PR-AA #1195) landed 'via deterministic steering.md candidate relevance with ranked/exploration channels.' Beta.1 has no steering.md and no PI-priority-weighted discovery: discovery is bounded citation-graph ranked by transparent occurrence counting across seeds, and the only relevance signal is a per-batch yes/no prefilter against 'the live question.' The gap doc's discovery section (§8) covers the graph narrowing but never registers the loss of the steering.md priorities substrate or the collapse of priority-weighted ranking.
    evidence: vault-template steering.md: 'steering is program memory, the program-wide steering substrate'; 'the Librarian pulls from this list to set discovery targets.' disposition-map keeps ADR-92 landed 'via deterministic steering.md candidate relevance with ranked/exploration channels'; calibration.yaml hybrid_scores.candidate_rank exists as the shadow-mode ranking knob. beta.1 §7 discovery ranks 'by transparent occurrence counting across multiple seeds' and §7 seeding uses a 'yes/no relevance prefilter … irrelevant to the live question' — no steering.md, no priority weighting. ADR-126 relocates 'steering → the instructions user config,' but the shipped template kept steering.md as a distinct discovery lever.
    doc-loc: §8 Discovery / gap analysis (or a new sub-point) — record the removal of the steering.md program-memory substrate and PI-priority-weighted discovery ranking; note the calibration.yaml candidate_rank shadow knob goes with it.
- [high/inaccuracy] The gap doc asserts A15 has 'no inbox/' and that inbox was 'retired at alpha.11,' and lists 'inbox/ staging' as 'Removed in A15, re-introduced in B1.' The shipped A15 vault-template contradicts this: inbox/ is a live, first-class transient home for machine-emitted attention projections/prompts, and virtually every alpha.15 attention PR emits 'stable inbox/ … prompt/attention.' The real A15→B1 delta is a semantic repurposing (attention-projection inbox → capture-staging drop folder), not a re-introduction of a retired concept — which is a materially different characterization.
    evidence: folders.yaml transient_prefixes: [inbox/]; AGENTS.md: '`inbox/` transient attention projections'; spaces/inbox.md is the 'Inbox' space ('Needs me', 'Notes to check'); exec-plan PR-V/W/X/Y/Z each 'emit one stable inbox/ … prompt/attention.' beta.1 §3 inbox/ is a 'staging drop folder — imports mint only via the awaiting-import chip.' Gap doc §2 A15 bullet: 'no inbox/ … (Inbox retired at alpha.11)'; Appendix: 'Removed in A15, re-introduced in B1: … inbox/ staging.'
    doc-loc: §2 Vault structure (A15 bullet) and the Appendix 'Removed in A15, re-introduced in B1' list — correct to: A15 inbox/ exists as the attention-projection home; B1 repurposes the same folder name as capture staging.
- [medium/inaccuracy] The gap doc states A15 has 'no system/ workspace tree' and frames the B1 visible system/ tree as an addition. The shipped A15 template already carries an extensive system/ tree (templates, dashboards, eval, metrics, logs, exports, patterns). The true delta is that system/'s contents changed (A15: templates/dashboards/eval fixtures/patterns; B1: manifest.jsonl/incidents/metrics), not that system/ is new — and B1 drops the A15 system/templates, system/dashboards, and system/eval subtrees entirely.
    evidence: vault-template tree: system/templates/{note,project,hub}.md, system/dashboards/{board-state,eval-trend,audit-log}.md, system/eval/*.md + alpha15-seeded-errors.json, system/metrics, system/logs/sessions, system/exports/assets, system/patterns/_preamble.md; folders.yaml skeleton lists all of these. beta.1 §3 system/ holds only manifest.jsonl, incidents/, metrics/. Gap doc §2 A15 bullet: 'no … system/ workspace tree.'
    doc-loc: §2 Vault structure (A15 bullet) — correct to: A15 has a rich system/ tree (templates/dashboards/eval/metrics/logs/exports/patterns); B1 repurposes system/ to manifest/incidents/metrics and drops the templates/dashboards/eval/patterns subtrees.
- [medium/missing-gap] Alpha.15 ships a whole navigation layer the gap doc barely registers: a spaces/ tree (library, knowledge, project, inbox, maintenance) with projection/dashboard/surface frontmatter and cssclasses, a _nav.md 'Navigator' (Now / Places / Actions), and a 'three places' mental model (Library / Knowledge / Project) in home.md. This is ADR-101 ('spaces vs gate vocabulary'), which the disposition map explicitly KEEPS ACCEPTED, UNCHANGED. Beta.1 replaces the entire spaces/navigator model with five surfaces (Capture/Reading/Ask/Compose/Review) and has no spaces/, no navigator, no three-places framing. The gap doc §10 only mentions 'navigation-spaces' in a parenthetical about hubs and never treats the spaces layer (a kept-accepted ADR) as a dropped concept.
    evidence: vault-template spaces/{library,knowledge,project,inbox,maintenance}.md with frontmatter projection: space|queue|maintenance, dashboard, surface, cssclasses: memoria-space; _nav.md 'Navigator' with Now/Places/Actions; home.md 'The three places: Library / Knowledge / Project'; disposition-map 'Keep accepted, unchanged': '101 (spaces vs gate vocabulary).' beta.1 §1/§16 five surfaces, no spaces/. Gap doc §10 only: 'B1 also has no navigation-spaces/hub concept at all.'
    doc-loc: §10 Surfaces / navigation — add the spaces/ + _nav Navigator + three-places model (ADR-101, kept-accepted) as an explicit A15→B1 removal, not just a parenthetical tied to hubs.
- [medium/missing-gap] Alpha.15 ships a .memoria/plugins/ hook-plugin mechanism — the memoria-policy-gate plugin providing pre_tool_call/post_tool_call hooks that route any external-adapter vault write through the runtime policy core, fail-closed. This is A15's concrete mechanism for making a BYO agent/adapter's writes pass the envelope at the tool-call boundary. Beta.1 has no .memoria/plugins/ tree and no per-tool-call hook plugin: machine writes go through the daemon's single VaultWriter, and agents are 'clients under the ADR-130 contract' whose envelope writes VaultWriter applies. The gap doc's agent/security sections (§11/§12) cover the ADR-130 contract and brokered egress but never note the removal of the policy-gate plugin / hook system.
    evidence: vault-template .memoria/plugins/memoria-policy-gate/{plugin.yaml,__init__.py}: 'provides_hooks: pre_tool_call, post_tool_call'; __init__.py evaluate_pre → decision 'block'; 'policy gate fails closed; any error blocks the write.' plugin.yaml description: 'Route workspace writes through the Memoria policy gate (fail-closed); block deny/dry_run zones in every run mode.' beta.1 §2 VaultWriter is 'the sole machine writer'; agents write via the request envelope (§13). No .memoria/plugins/ in beta.1 layout (§3).
    doc-loc: §11 Agent / autonomy or §12 Security — record the removal of the .memoria/plugins/ policy-gate hook system (pre/post_tool_call fail-closed adapter-write gate) as A15's adapter-write enforcement mechanism, superseded by single-VaultWriter + envelope.
- [medium/missing-gap] ADR-127's central integrity mechanism — a first-class DB derivation graph (consumed-at-verdict on every edge) driving two-tier demotion propagation: depth-1 machine consumers demote, depth-≥2 machine descendants go 'stale' (still served with stale framing, excluded from correctness-bearing consumers), human descendants get ask-routed flags and are never auto-destroyed, retraction propagates one level deeper — has no beta.1 analog and is not captured anywhere in the gap doc. Beta.1's model is per-block hash divergence (edit under an mc marker → amber + re-check, §4) plus footprint-scoped DB reconciliation on revert (§6); there is no source-changes-so-consumers-demote cascade and no derivation graph. The gap doc §5 covers 'checked ≠ approved' and the review-surface removal but omits the demotion-propagation/derivation-graph mechanism entirely.
    evidence: ADR-127 Decision: 'Demotion propagates, in two tiers … Depth 1: machine-derived direct consumers demote; human-authored descendants stay live with an ask-routed flag … Depth ≥2: machine descendants are stale-flagged … Retraction-driven demotions propagate one level deeper.' exec-plan PR-C2 landed this. beta.1 §4 handles staleness per-block via hash divergence; §6 revert does 'footprint-scoped DB reconciliation'; no consumer-demotion cascade. Gap doc §5 discusses quarantine-and-verify/review surface but not propagation.
    doc-loc: §5 Integrity / review / gate model — add the loss of ADR-127's derivation-graph + two-tier stale/invalid demotion propagation (depth-1 demote vs depth-≥2 stale, human-descendant protection, retraction-deeper) as a distinct removed mechanism.
- [medium/missing-gap] Alpha.15's recovery model (ADR-127) has a git-tracked, append-only .memoria/overrides.jsonl human-override log with a tiered DB-loss replay discipline — restrictive dispositions (archive, contested-block) auto-reapply, permissive ones (degraded acceptance, retraction resolution) return as attention items requiring re-confirmation because the journal witness is gone — plus a mandated three-store backup contract including SQLite streaming replication (Litestream-class). Beta.1 has neither: researcher decisions live in frontmatter (superseded/reading) + exported decide-events CSV, and §18 explicitly DECLINES DB streaming-replication for beta.1. The gap doc §13 covers the git-snapshot/VACUUM INTO substrate but never mentions overrides.jsonl, the tiered restrictions-reapply/permissions-re-earn replay, or the dropped streaming-replication backup leg.
    evidence: ADR-127: '.memoria/overrides.jsonl … tiered by direction — restrictions reapply, permissions re-earn'; Consequences: 'SQLite streaming replication — Litestream or equivalent.' exec-plan PR-B/PR-E1 track overrides.jsonl. beta.1 §5 researcher decisions are frontmatter; §18 item 5: 'DB streaming-replication RPO is declined for beta.1.' Gap doc §13 lists 'cross-store recovery + journal' for A15 and git/VACUUM for B1, no mention of overrides.jsonl or the tiered replay or the dropped replication leg.
    doc-loc: §13 Cost / snapshot / migration machinery — add the removal of .memoria/overrides.jsonl + tiered DB-loss replay, and note B1 §18's explicit decline of the ADR-127 streaming-replication backup leg.
- [low/scope-source-underused] Two smaller layout/continuity inaccuracies rooted in the vault-template. (a) The gap doc §2 says A15's 'catalog … [is] not [a] workspace folder,' but the shipped skeleton has catalog/, catalog/sources/, catalog/entities/ as workspace folders with a generated catalog/index.md projection (records are SQLite, but the folder tree and nav home exist) — and B1 drops the top-level catalog/ tree entirely (works co-locate record.md under works/). (b) The gap doc treats Bases/view rendering as a B1-era stack (§10 'Bases for meaning-only views', Appendix 'New in B1: generative-UI view-spec/ViewSpecRenderer'), but A15 already ships Obsidian Bases (knowledge/views/knowledge.base, embedded via ![[knowledge.base#Works]]) plus Dataview queries — so Bases is continuity, and the B1 delta is specifically routing verdict-bearing views off Bases onto the read-API/ViewSpecRenderer.
    evidence: folders.yaml skeleton includes catalog, catalog/sources, catalog/entities; catalog/index.md is a generated projection; AGENTS.md: '`catalog/` source and entity records.' vault-template knowledge/views/knowledge.base + spaces embed ![[knowledge.base#...]]; maintenance.md uses a ```dataview block. beta.1 §3 layout has no catalog/ folder; §15/§16 keep Bases for meaning-only views and route verdict-bearing views through the read-API.
    doc-loc: §2 Vault structure (catalog folder claim) and §10/§15 (Bases continuity) — correct the 'catalog not a workspace folder' claim and note Bases is inherited from A15, with the real delta being verdict-views-off-Bases-onto-read-API.

## critic: accuracy-of-claims
OVERALL: Mostly accurate: ADR content, type-class enum, CLI roster, host/daemon/catalog/frontmatter/enrichment claims, and the '§11 = alpha.16 scope' citations all check out against the sources. Three defects found — one high-severity mischaracterization (the review/disposition surface was NOT removed in a15; beta.1's own §11 says the span-first review surface 'converges with alpha.15'), one wrong ADR number (quarantine-and-verify is ADR-127, doc says ADR-128; ADR-127 is never cited), and one minor a15-side conflation (Tier-2 judge admissible on 'hard' cases, not 'HANS-degraded').
- [high/inaccuracy] The doc frames a review/disposition surface as REMOVED in alpha.15 and RE-INTRODUCED in beta.1. Alpha.15 did not remove the disposition surface — it removed only the *approval* semantics of the old review queue (plus the inbox-as-Obsidian-category mechanism) while explicitly retaining a consequence-routed attention worklist with batch worklists, span-first Elicit presentation, and one-click --apply. beta.1's Review surface is the Obsidian rendering of that same alpha.15 attention surface, not a re-introduction of something a15 dropped.
    evidence: a15 ADR-128 lines 80-82: 'The review queue as an approval surface is gone; the attention surface is a consequence-routed worklist that must keep the honesty-card obligations (arguments, not verdicts; batch worklists; graded loudness).' a15 ADR-130 lines 85-94 describe that attention surface with 'span-first with source evidence adjacent (the Elicit table pattern), and --apply is one click' plus batch worklists. Decisively, the beta.1 design itself (0.1.0-beta.1-design.md §11, lines 970-972) lists 'the Elicit-style span-first review surface' under '**Converges** with alpha.15 on' — beta.1's own design says a15 already has this surface, contradicting the doc's 'removed in A15' framing.
    doc-loc: §5 line 44 delta ('B1 re-introduces a review/disposition surface that A15 had removed'); repeated in Appendix line 109 under 'Removed in A15, re-introduced in B1: ... a review/disposition surface'
- [medium/inaccuracy] Wrong ADR number: the doc attributes 'quarantine-and-verify' to ADR-128. Quarantine-and-verify is ADR-127 ('Quarantine-and-verify integrity engine'); ADR-128 is 'No write-time correctness oracle — checked ≠ approved'. ADR-127 is never cited anywhere in the gap doc, while its mechanism is misattributed to 128.
    evidence: a15 adr/127-quarantine-and-verify-integrity.md title line 14: 'ADR-127: Quarantine-and-verify integrity engine'; a15 adr/128-no-write-time-correctness-oracle.md title line 14: 'ADR-128: No write-time correctness oracle'. Gap doc §5 line 44 writes 'quarantine-and-verify (ADR-128)', while the adjacent 'no-write-time-oracle (ADR-128)' on line 45 IS correct — so one number is applied to two different ADRs' content. Grep of the gap doc shows ADR-127 cited zero times. Separately, the doc header (lines 3/5) scopes to 'ADRs 125–130' yet relies on ADR-124 at lines 23 and 57.
    doc-loc: §5 line 44 ('quarantine-and-verify (ADR-128)'); header lines 3/5 ('ADRs 125–130')
- [low/inaccuracy] Mischaracterizes when the a15 Tier-2 LLM judge is admissible. The doc says 'admissible only on Tier-1 abstain/HANS-degraded cases'. Per ADR-129 the Tier-2 judge is admissible on Tier-1 abstain/**hard** cases; HANS-degraded is a distinct mode where Tier-1 NLI is disabled and candidates route to cosine/BM25 → ask (attention raised), NOT to the Tier-2 LLM judge. The doc's headline delta (B1 has no Tier-2 judge) is correct; only the a15-side qualifier is off.
    evidence: a15 ADR-129 line 42: 'Tier-2: an LLM judge, admissible only on Tier-1 abstain/hard cases, retrieval-grounded, abstain first, temperature 0'. Lines 39-40: 'Tier-1 failing HANS is a **loud degraded mode**: cosine/BM25 candidates route to ask, attention is raised — never a silent no-op.'
    doc-loc: §6 line 50 ('admissible only on Tier-1 abstain/HANS-degraded cases')

## critic: appendix-and-internal-consistency
OVERALL: The section→appendix mapping is mostly sound (the bulk of the 16 sections land in exactly one correct list, and buckets I cross-checked against ADR-124/125/130 are defensible), but it is not complete or exclusive: §11's agent gap is orphaned from all three lists AND mischaracterizes A15's ADR-130 agent contract (which B1 actually converges with); the resident daemon is double-bucketed across lists B and C; and several §2 (digest.md restore, reports/, project-file-split) and §9 (pdfium/no-OCR) sub-gaps are dropped from every list, while list C's 'paid synthesis' lacks a source section.
- [high/inconsistency] Section 11 (Agent / autonomy) is the only one of the 16 numbered sections whose gap appears in NONE of the three appendix lists. Its stated gap — 'B1 ships no resident Co-PI agent; the agent role is externalized to the researcher's own client' — has no counterpart in list A (present/absent), list B (reintroduced), or list C (new). Every other section maps to at least one appendix entry; the 'operations are product code' fragment of the same section does map to list A ('operations-as-product-code / method_class / capability manifests'), but the agent/Co-PI core of the section is orphaned.
    evidence: Gap doc §11 (lines 77-78) states the Co-PI/agent-externalization gap. Appendix lists (lines 107, 109, 111) contain no agent, Co-PI, read-API, or ADR-130 entry — grep of all three lists finds nothing agent-related. Contrast every other section (§1-10, 12-16), each of which lands in a list.
    doc-loc: Appendix (lines 105-111) — an agent-boundary entry is missing; if it belongs anywhere it is list A (present-in-A15 read-API+Co-PI, changed in B1), but see the separate accuracy finding below.
- [medium/inaccuracy] The §11 gap is itself mischaracterized, which is likely WHY it has no clean appendix bucket. It claims A15 had a 'Co-PI' (resident first-party agent) that B1 drops. But the cited A15 source, ADR-130, defines the agent identically to B1: 'any delegated agent — Memoria's worker or the user's own (Claude Code, Cursor, ChatGPT)... Universally quarantined... never granted file/terminal/exec/send tools... chat is additive, never the primary path.' That is verbatim the B1 §11/§13 contract. 'Co-PI' appears only in the ADR-130 *filename* (130-read-api-surfaces-and-copi.md), never in any A15 ADR or design body. B1 §13 explicitly grounds itself in ADR-125-130. So the agent boundary is a CONVERGENCE, not a divergence — making §11's framing as a gap questionable, and its appendix omission arguably correct while its presence as a gap section is the actual error.
    evidence: ADR-130 body (alpha.15/adr/130, lines 46-63) = quarantined client contract, no resident agent. grep for 'co-pi/copi' across all alpha.15 ADRs + design returns only the filename (design line 13) and 'CopilotKit' (line 112). B1 design §13 (lines 1006-1019) mirrors ADR-130 and cites it. B1 §11 'Converges with alpha.15' does not, but §13 treats agent boundary as inherited, not new.
    doc-loc: Gap doc §11 (lines 77-78) — the 'A15 had Co-PI' premise overstates a resident agent that ADR-130 never describes.
- [medium/inconsistency] The resident daemon is double-bucketed: it appears in list B (reintroduced) as 'a resident daemon' AND in list C (new) as 'Obsidian plugin + mem serve daemon (LSP lifecycle)'. The three lists are meant to be mutually exclusive buckets for a single gap. §1 bullet 2 is one gap ('B1 adds a resident daemon; A15 explicitly had none'), yet it lands in two lists. The reintroduced-vs-new call is genuinely ambiguous (a 'board daemon' existed pre-A15 per §10, was removed, and the specific mem-serve/LSP form is new), but the doc resolves the ambiguity by listing it twice rather than once.
    evidence: ADR-125 line 34: 'Memoria is a standalone local CLI + engine, single-user, no daemon' confirms A15 had none. Gap doc §10 line 72 notes A15 'no board daemon' (implying a prior daemon). Appendix line 109 (list B) 'a resident daemon'; line 111 (list C) 'mem serve daemon (LSP lifecycle)' — same underlying §1 gap in two buckets.
    doc-loc: Appendix lines 109 and 111 — pick one bucket (or state the concept-vs-specific-form split explicitly) instead of listing the daemon in both.
- [medium/inconsistency] Several §2 (vault structure) sub-gaps are stated in the section but dropped from all three appendix lists. (a) Restored per-work 'digest.md': A15 folded digest into the 'work' concept ('knowledge/digests removed'); B1 splits it back out to works/.../digest.md — a textbook removed-in-A15-reintroduced-in-B1 item, but it is in neither list B nor anywhere. (b) The 'reports/' machine-report tree (new in B1) is in no list. (c) Project-artifact split into separate files (evidence.md/gaps.md/draft.md) is in no list (only the removed argument-graph *concept* is captured, via list A). The appendix does capture inbox/ (list B), knowledge/ single-bundle (list A), and record.md (list B), but these §2 items fall through.
    evidence: Gap doc §2 line 24 ('B1 restores a per-work digest.md'; 'B1 splits project artifacts into separate files'), line 25 ('adds reports/'). Appendix lists (107/109/111) contain no digest.md, no reports/, no project-file-split entry.
    doc-loc: Appendix — digest.md belongs in list B (reintroduced), reports/ in list C (new); project-file-split either list C or noted under the argument-graph entry in list A.
- [low/inconsistency] The §9 extraction gap (pdfium-only, no OCR) is stated in the section but is in none of the three lists. A15 had 'extraction via the ported ingest subsystems' (multi-extractor capability); B1 fixes a single extractor (pdfium) and explicitly ships no OCR. The narrowing/removal of multi-extractor + OCR capability is a present-in-A15-absent-in-B1 item that never reaches list A (nor is the pdfium fixation logged as new in list C).
    evidence: Gap doc §9 line 68 ('B1 fixes a single extractor (pdfium) and explicitly ships no OCR'). B1 design §7 lines 632-636 (pdfium alone, no OCR, extract_failed:needs_ocr). Appendix has no extraction/OCR/pdfium entry.
    doc-loc: Appendix — add to list A (multi-extractor/OCR capability absent in B1) or list C (pdfium-only fixation).
- [low/under-covered] Reverse direction: list C entry 'paid section-expanded synthesis' has no clear home among the 16 sections. It is only mentioned inside §6's B1-state roster ('LLM call roster = digest, seeding relevance prefilter, paid synthesis') but is never called out as its own gap in any section; §8 (the doc's discovery/gap-analysis section) does not cover synthesis at all. So an appendix entry lacks a corresponding section-level gap, the inverse of the other findings.
    evidence: Appendix line 111 (list C) 'paid section-expanded synthesis'. Gap doc §6 line 50 mentions 'paid synthesis' only as part of the B1 roster describing the Tier-2-judge gap, not as its own delta. B1 design §8 lines 798-816 is where paid synthesis actually lives.
    doc-loc: Gap doc §8 (or a synthesis sub-bullet in §6) — add the paid-synthesis delta so the list C entry has a source section.
- [low/inconsistency] The §10 Canvas gap ('B1 mentions no Canvas/spatial projection') is stated as a section gap but is in none of the three lists. It is weak (the section itself concedes A15 only 'referenced Canvas historically', so it may be correct to omit), but as written it is an asymmetry between a section bullet and the appendix.
    evidence: Gap doc §10 line 74. Appendix (107/109/111) has no Canvas entry.
    doc-loc: Appendix, or drop the §10 Canvas bullet since A15 did not actively ship Canvas — either way make section and appendix agree.

## critic: source-coverage
OVERALL: The gap doc thoroughly mines the six consolidation ADRs (125-130), ADR-124, and the beta.1 design's headline mechanisms, but systematically under-mines the shipped vault-template's knowledge substrate (steering.md/Librarian discovery-steering and system/vocabulary.md controlled-vocabulary faceting are both 0-mention omissions) and several exec/release-plan tactical facts (qmd->sqlite-vec substrate swap mischaracterized as convergence; `work interview` step dropped; ADR-95 nightly-loop rejection reversed by the daemon+monitor; shipped MCP/HTTP transport layer).
- [high/scope-source-underused] The shipped vault-template's `steering.md` and the discovery-steering / "Librarian" mechanism it drives are never mined. A15 ships `steering.md` as a first-class researcher-authored priorities file (program memory) that "the Librarian reads at the start of every session and weights discovery by," i.e. discovery is STEERED by researcher priorities/open-questions/synthesis-gaps/papers-to-prioritize. B1 has no steering.md, no Librarian, and no priority-weighting lever: its discovery is deterministic occurrence-counting over the citation graph with no researcher-intent input. This whole steering substrate being dropped is a real beta.1 gap the doc omits.
    evidence: vault-template `steering.md` lines 3-43 ("weights discovery by how well candidate papers match what's here… the main lever you have over what gets surfaced"; "steering is program memory"); exec-plan PR-AA #1195 (`analyze-gaps` annotates candidates with deterministic `steering.md` relevance metadata, ranked vs exploration channels). B1 design §7 discovery is snowball occurrence-counting + citation monitor, identifiers-only, no steering input. Gap-doc grep: 0 occurrences of "steering" or "Librarian".
    doc-loc: §8 (Discovery / gap analysis) — currently frames the delta only as topic/concept-graph vs citation-graph-only; should add a steering-lever gap. Appendix "Present in A15, absent in B1" list.
- [high/scope-source-underused] The controlled-vocabulary knowledge substrate is not mined. A15 ships `system/vocabulary.md` as "the single source of truth for the controlled values" with faceted vocabularies — `research_area` (~30 curated terms), `methodology` (architecture + specific-method terms), and note `topics` drawn from research_area — plus a `vocab` CLI command that mints/manages ID-backed tag IDs, the calibration.yaml `classify` operation that silently applies a research_area from OpenAlex topic scores (floor 0.6 / near-tie 0.15), and the `_enrichment` namespace for reference taxonomies. B1 drops the entire controlled-vocabulary model: tags are minimal/freeform, no research_area/methodology facets, no `vocab` command, no classify operation. The gap doc only glances at this via hub-owns-one-vocab-tag (§3) and never surfaces the vocabulary system itself.
    evidence: vault-template `system/vocabulary.md` (research_area 27 terms, methodology two groups, topics facet; "Keep `research_area` to ~30 terms"); note.yaml `topics: list`; calibration.yaml `classify:{confidence_floor:0.6, near_tie_margin:0.15}`; ADR-126 `vocab mints and manages tag IDs`. Gap-doc grep: 0 occurrences of "vocabulary", 0 of "classify", 0 of "research_area".
    doc-loc: §3 (Concept / type model) and §9 (Ingest) — §3 mentions the hub's one controlled-vocab tag but not the facet system; §9 mentions OpenAlex topics not imported but not the classify→research_area mechanism.
- [medium/inaccuracy] The retrieval-substrate delta is mischaracterized. A15's SHIPPED substrate is qmd — the exec-plan/release-plan §10 spike verdict explicitly recorded that sqlite-vec + local embeddings did NOT clear the bar and "qmd remains the alpha.15 baseline" (FTS5 cleared lexical only). B1 §5/§8 ships FTS5 + sqlite-vec + bge-small-en-v1.5 embeddings + deberta NLI, i.e. it adopts exactly the substrate A15 declined to ship and drops qmd. This is a concrete substrate swap, but the beta.1 design §11 lists it as CONVERGENCE ("the alpha.15 §10 spike is exactly the Ask substrate") and the gap doc inherits that framing — qmd appears only once, as a security aside (§12 "Node ≥22 + qmd"), never as the retrieval substrate B1 replaces.
    evidence: exec-plan PR-A #1162 ("qmd remains the alpha.15 baseline; FTS5 clears lexical cases but does not clear semantic/hybrid replacement without packaged sqlite-vec + local embeddings"); release-plan G2/G3 ("spike-selected retrieval substrate (qmd runtime *or* FTS5+sqlite-vec)"); vault-template `.memoria/index/qmd/`. B1 design lines 361-416, 754-756. Gap-doc grep: qmd appears once (line 82).
    doc-loc: §8/§10 (or a dedicated retrieval-substrate row) — no row captures qmd→sqlite-vec+embeddings; §12 uses qmd only in the stack aside.
- [medium/missing-gap] The `work interview` operation is dropped and unmentioned. A15's canonical minimum loop is `work add → work enrich → work interview → work digest`: an interactive elicitation step that builds the subjective work before digesting. B1 has no interview — the subjective layer comes from a single-pass pinned digest LLM call plus Zotero annotation sync. Losing the interview elicitation flow is a real beta.1 delta the doc never records.
    evidence: exec-plan lines 112-116 and release-plan lines 49-55 (minimum loop lists `work interview`); dogfood note "`work digest` requires 5-15 hub topics". B1 design §7 DigestEngine (single pinned `digest_prompt_v1` pass, no interview) and §7 Reading (Zotero annotations). Gap-doc grep: 0 occurrences of "interview".
    doc-loc: §9 (Ingest / enrichment) or §16 (Reading/annotation) — neither notes the interview step's removal.
- [medium/missing-gap] The adr-disposition-map records ADR-95 (nightly discovery loop) as REJECTED specifically because an "unattended always-on loop conflicts with the no-daemon architecture." B1 reintroduces BOTH halves of what that rejection rested on — a resident two-process daemon (§1) AND a scheduled citation monitor that writes report notes on a schedule (§7). The doc covers the daemon (§1) and the monitor (§8) as separate additions but never surfaces that this jointly reverses an A15 rejection whose stated reason (no daemon) B1 removes — exactly the retired/rejected-transition the task flags as a gap.
    evidence: adr-disposition-map §Rejected: "95 nightly discovery loop — unattended always-on loop conflicts with the no-daemon architecture." B1 design §1 (resident `mem serve` daemon) + §7 ("Citation monitor (scheduled): one capped machine note per schedule"). ADR-125 supersession of ADR-22 records "there is no internal daemon."
    doc-loc: §1 (host/daemon) and §8 (discovery) — should cross-reference the ADR-95 rejection reversal.
- [medium/under-covered] The shipped spaces navigation model is under-mined. The vault-template ships five `spaces/*.md` dashboards (inbox, knowledge, library, maintenance, project) as markdown files with `projection: space` / `dashboard: space` frontmatter and embedded Bases (`![[knowledge.base#Works]]`), plus a weekly "Friday ritual" maintenance cadence. B1 replaces this with a different 5-surface model (Capture/Reading/Ask/Compose/Review) — no spaces, no maintenance/Friday ritual, no library space. The gap doc mentions "navigation-spaces" only once in passing (§10) and never enumerates the concrete five-space dashboard end-state that B1 discards.
    evidence: vault-template `spaces/{inbox,knowledge,library,maintenance,project}.md` (each `projection: space`, `cssclasses: memoria-space`, embedded knowledge.base sections); folders.yaml transient inbox/, system/dashboards/. steering.md references "Friday weekly review." B1 design §1/§16 five surfaces. Gap-doc: single passing mention of "navigation-spaces" (§10).
    doc-loc: §10 (Surfaces / navigation) — expand the one-line navigation-spaces mention into the concrete five-space delta.
- [low/under-covered] A15 actually SHIPPED scoped-MCP-stdio and loopback-HTTP as read-API transports (ADR-130; exec PR-R/S/T/AD: `serve --http`, `mem mcp --read-scope`, engine read-scope guard). B1 reshapes the transport story into a plugin↔daemon loopback HTTP + WebSocket link with write-event push, and MCP appears only as prior-art/deferred ("agent transports stay local/stdio"). The gap doc's §10/§11 cover surfaces and the agent externalization but never note that the concrete scoped-MCP + loopback-HTTP transport layer A15 built is not carried as-is (WebSocket push added, MCP not centered).
    evidence: ADR-130 Decision ("Alpha.15 implements CLI, scoped MCP, and loopback HTTP"); exec-plan PR-R #1186 / PR-S #1187 / PR-T #1188 / PR-AD #1198. B1 design §1 ("plugin ↔ daemon over loopback HTTP + WebSocket"; "pushes write events") and §13 ("Agent transports stay local/stdio").
    doc-loc: §10 (Surfaces) / §11 (Agent) — add the shipped-transport-layer delta.

