# 0.1.0-alpha.11 design assumption verification results

Date: 2026-06-28

Scope: `docs/releasing/0.1.0-alpha.11/tmp/0.1.0-alpha.11-design.md`.

This is a local verification pass over the design assumptions previously flagged
from the alpha.11 reset design. The pass tested only what is testable from this
worktree and the installed local tools. Live Obsidian/Zotero/Local REST API checks,
external OKF tooling, model calls, and Hermes workflow runs were not fabricated;
they are marked blocked where they are required.

## Evidence status update

This table is historical M0 evidence, not the current implementation ledger.
Later same-day disposable spikes supersede the `not-implemented-local` /
`failed-local` rows below only as feasibility evidence, and later
`wp*-results.md` files supersede them only for the exact implemented slice they
verify. For example, the later WP2/WP7/WP-Gate result files are the current
evidence for the trusted-writer, cascade-rollback, and seeded-error slices. The
later scope correction also removes migration, upgrade, and backwards-
compatibility work from alpha.11 because no non-sandbox install exists.

Current broad gate update: on 2026-06-29, `scripts/test.sh all` passed with
594 pytest tests plus L0 static/schema/docs/provenance/shell checks. This
supersedes the older failed `scripts/test.sh all` row below only as current
worktree gate evidence; the table remains the historical M0 assumption ledger.

## Commands run

| Command | Result |
| --- | --- |
| `scripts/test.sh all` | Failed: 545 pytest tests passed, 1 failed. L0 also failed at `scripts/agents_doctor.py`. Failure: `.agents/system/docs-manifest.yaml` is stale and needs `scripts/agents_doctor.py --write`. This is unrelated to the alpha.11 design assumptions and was not changed. |
| `env PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_cluster_mcp.py tests/test_policy_gate_completeness.py tests/test_eval_score.py tests/test_qmd_filter_mcp.py tests/test_memoria_inspector.py tests/test_ingest_mcp.py tests/test_ingest_paper.py tests/test_project_structural_impact.py -q` | Passed: 63 tests. Covers current policy hard-deny, read-only inspector contract, ingest pieces, graph/canvas staging allowlist, supersession filtering, eval scoring, and current project structural impact. |
| `find . -path './.git' -prune -o -iname '*okf*' -print` | Only `docs/adr/107-okf-interchange-bundle-format.md` found. No OKF serializer/importer implementation in this worktree. |
| `find . -path './.git' -prune -o -path '*ai-catalog*' -print` | No `ai-catalog` files found. |
| `find src -path '*staging*' -o -path '*quarantine*' -o -path '*capabilities*' -o -path '*journal*'` | No alpha.11 staging/quarantine/capabilities/journal paths found in shipped `src/`. |
| `find /home/eranr/Memoria-test -maxdepth 3 -path '*staging*' -o -path '*quarantine*' -o -path '*capabilities*' -o -path '*journal*'` | No matching runtime paths found in the test vault. |
| `hermes --version` | Hermes Agent v0.17.0 is installed. |
| `qmd --version` | qmd 2.5.3 is installed. |
| `qmd search "rollback"` | Failed: local project qmd store/index is not usable here (`SQLITE_CANTOPEN` / sqlite-vec probe failure). |
| `pgrep -a obsidian` | No Obsidian process running, so live Local REST API/native MCP behavior was not tested. |

## Status legend

- `verified-local`: tested and passed for the current implementation.
- `partial-local`: existing tests/mechanisms cover a narrower older form, not the
  alpha.11 reset claim.
- `not-implemented-local`: no local implementation found to test.
- `failed-local`: the local implementation contradicts the alpha.11 assumption.
- `blocked-live`: requires live runtime, external tool, model, or source corpus.
- `open-design`: design/acceptance ambiguity, not a runtime behavior.
- `superseded-scope`: the assumption was removed from alpha.11 scope.

## Findings

| # | Assumption tested | M0 status | M0 evidence and reasoning | Next verification |
| --- | --- | --- | --- | --- |
| 1 | Three nested OKF-compatible bundles can round-trip as the storage/export substrate. | partial-local | M0 found no local OKF fixture or validator. Later WP1 evidence supersedes this row for the local directory/git substrate: a minimal `catalog/`, `knowledge/`, and `capabilities/` fixture now validates as OKF-core and Memoria-profile shape before export, after copy export, and after copy import, with byte-for-byte markdown preservation. External OKF serializer/importer tooling remains unimplemented. | Run the approved external OKF interchange toolchain when ADR-107 moves from proposal to implementation. |
| 2 | `.memoria/staging/` plus `.memoria/quarantine/` enforces the read barrier. | superseded-local | M0 found no staging/quarantine skeleton and only the old review-gated prefix model. Later WP1/WP2 evidence supersedes this row for local feasibility: the skeleton now has staging/quarantine roots and worker-owned staging/promote/quarantine tests. | Keep live runtime proof in the G4/G8 gate evidence; this M0 row is historical, not current implementation status. |
| 3 | Trusted writer commits Concept file plus derivation event atomically. | superseded-local | M0 found no alpha.11 journal path, derivation-event shape, trusted writer, or coupled commit implementation. Later WP2 evidence supersedes this row for the local slice: worker-owned staging, promotion, PI-edit backfill, foreign-write quarantine, and selected-path commits are now covered by tests. | Keep multi-device/concurrency proof in the later live/simulation evidence; this row is historical, not the current implementation ledger. |
| 4 | Cascade rollback can revert derived outputs per Concept and flag human-directed descendants. | superseded-local | M0 found no rollback DAG/inverse-commit implementation. Later WP7 and WP-Gate evidence supersede this row for the local slice: cascade rollback quarantines machine-derived descendants, flags PI-derived descendants, and records traced inverse events in seeded fixtures. | Keep semantic/live checkpoint-cost proof in WP-Gate follow-up evidence; this row is historical. |
| 5 | Gateless integrity is safe if trace/check/rollback recall meets a seeded-error bar. | partial-local | M0 found no seeded-error gold set, batch runner, recall threshold, or rollback-completeness measurement. Later WP-Gate evidence supersedes this row for the current local deterministic slice: the frozen bundle and worker verdict run structural, ambiguous-entity identity, marker-based injection, provenance-checkpoint, unwarranted-claim, stale-as-current, wrong-extraction, and rollback-completeness cases against the built loop. Semantic detection and live PI checkpoint-cost calibration remain open. | Extend WP-Gate with semantic cases and live checkpoint adjudication evidence before making any non-sandbox safety claim. |
| 6 | The structural-check suite is feasible. | partial-local | Current source gate and targeted tests prove existing checks for policy, lint pieces, supersession filtering, eval scoring, project structural impact, trace-integrity quarantine, and checked-only read-barrier paths for qmd, Ask, gap analysis, and project argument views. The scheduled `integrity-sweep` now includes trace-integrity quarantine before the structural checks. These tests still do not cover alpha.11 semantic span entailment, NLI, or counterfactual faithfulness. | Prototype the remaining semantic checks in shadow mode with thresholds and false-positive accounting. |
| 7 | The wiki compiler can update 5-15 digest/hub nodes per source with acceptable quality. | partial-local | M0 found no alpha.11 `digest` type, wiki SCHEMA, wiki compiler integration, or fixture. Later WP5 evidence supersedes this row only for the local contract: checked `digest` records, hub suggestions, model-call logging, source-grounding smoke tests, mocked embedded-Hermes adapter construction, and an import-only local Hermes `run_agent.AIAgent` smoke exist. A live Hermes model-quality run remains open. | Run the Hermes `llm-wiki` / candidate compiler on a small source set and measure traceability, unsupported claims, duplication, and edit burden. |
| 8 | `digest`, `note`, and `hub` replace current note-family types cleanly. | superseded-local | M0 saw separate alpha.10 note-family schemas and no unified alpha.11 `note` or `digest` schema. Later WP1/WP6 evidence supersedes this row for the local schema slice: the alpha.11 skeleton now carries `digest`, `note`, and `hub` Concepts and schema/tests cover the reset type map. | Keep live fresh-vault initialization proof in G3/G4 evidence; this row is historical. |
| 9 | Gap analysis over both catalog and knowledge mismatch is actionable. | partial-local | `tests/test_project_structural_impact.py` covers current project argument graph saturation/gap behavior. Later WP6 fixtures now cover catalog-vs-knowledge mismatch locally: new-topic, source/digest-dense note-thin, note-dense source-thin, balanced, below-threshold, unchecked, stale, and uncurated-candidate states. The G3 harness now runs a worker-boundary source-to-gap cycle that starts under-warranted, captures a source, compiles a digest/hubs, accepts and links a note, closes the gap, answers Ask, rebuilds checked qmd, renders Canvas, and exercises cascade rollback from the source. Live actionability in Obsidian remains unproven. | Run the same cycle in `Memoria-test` through the plugin surface and record whether the proposed seed is useful to the PI. |
| 10 | Ask retrieval design beats cheap baselines. | partial-local | qmd is installed, qmd supersession filtering tests passed, and the repo-local qmd BM25 index now builds/searches when `scripts/qmd-codebase-index.sh` pins qmd config/cache under `.qmd/`. No alpha.11 Ask benchmark has compared BM25, hybrid union, query expansion, LLM rerank, and long-context whole-read. | Run retrieval benchmarks with recall@k, support correctness, latency, and cost before claiming any retrieval layer beats BM25. |
| 11 | The Obsidian plugin surface can carry Co-PI conversation, trace rollback, flags, and graph browsing. | partial-local | M0 saw only the read-only Inspector. Later WP8 evidence supersedes that for the local surface: the Inspector is now an enqueue-only control panel for capture, checks, Ask/query, gap analysis, digest, note curation, trace rollback, attention decisions, queue state, integrity flags, and checked-graph browsing, with static tests forbidding direct vault/journal/projection writes, fetch, and shell access. The 2026-06-29 start-blocker verifier proves the refreshed `Memoria-test` `memoria-inspector` plugin files match the alpha.11 template, retain enqueue-only control-panel markers, avoid direct vault/network/shell APIs, execute `memoria-inspector:open-memoria-inspector` with HTTP 204, and produce `workspace_view_present=True` for `memoria-inspector-view`. It also proves a disposable alpha.11-shaped worker queue can drain an Inspector-style `answer-query` operation through the checked policy boundary and return only checked Concepts, then drains one harmless live `Memoria-test` `answer-query` job into `.memoria/queue/done/`. Direct note editing is not a plugin feature; PI edits happen in Obsidian and are observed/backfilled by the worker evidence in WP2/WP6. Live visual rendering by screenshot/human inspection and attended Co-PI use remain unproven. | Run visual Obsidian activation in `Memoria-test` and exercise the Co-PI/trace rollback controls against the worker. |
| 12 | Local REST API/native MCP plus Zotero Local API support the new capture path. | partial-local | ADR-31 documents an accepted Local REST API native MCP path and prior live validation. The 2026-06-29 local probe reached Obsidian Local REST at `127.0.0.1:27124`, read the smoke plugin manifest, listed commands, and executed the smoke-pane command. Zotero Local API port `127.0.0.1:23119` was open, and a disposable alpha.11 workspace smoke fetched one live Zotero item key through `capture_zotero_local_source()` into a checked `source` Concept with raw `.zotero.json` and journal commit. The current scope correction treats Zotero as item/source import only; Zotero annotation import is not an alpha.11 capability or release gate. | Run native MCP read/write smoke if it differs from Local REST command execution; keep Zotero runtime proof focused on item/source import. |
| 13 | `capabilities/` and generated `ai-catalog.json` can be trusted as a supply-chain surface. | superseded-local | M0 found no `capabilities` path or `ai-catalog` file. Later WP1/WP2/WP5 projection evidence supersedes this row for the local manifest slice: capability Concepts and generated `ai-catalog.json` are covered by projection tests, every worker-dispatched operation has a checked operation Concept, checked operation Concepts must satisfy `load_operation_policy()`, and the worker fails closed before dispatch when the operation Concept is missing or unchecked. Later local capability tests also prove unsigned imported operation Concepts are copied to `.memoria/quarantine/`, omitted from the live catalog, and not executable through `load_operation_policy()`. Signed promotion and live approval UI remain outside this row. | Define and test signed promotion only after alpha.11 has an approved signing/approval format. |
| 14 | Clean rebuild migration can preserve the current record while regenerating machine layers. | superseded-scope | The later scope correction states there is no alpha.11 system installed outside the sandbox, so alpha.11 needs fresh initialization only: no migrator, upgrade path, backwards compatibility, or data-preservation checklist. | Remove migration from the ExecPlan and design SSOT; initialize only the sandbox skeleton and seed data through the alpha.11 pipeline. |
| 15 | Git-only, single-user, multi-device operation is safe enough for alpha. | partial-local | Later WP2 tests now simulate two clones against a bare remote: sequential single-writer commits from two machines converge by pull/push, write separate per-machine journals, and rebuild trace state from both journals. A simultaneous same-Concept edit simulation now proves the stale device push is rejected and `git merge FETCH_HEAD` leaves a visible file conflict instead of silently overwriting either device's write. Live Obsidian Git behavior and live-runtime merge recovery remain unproven. | Run live Obsidian Git pull/merge behavior before treating multi-device operation as release-ready. |
| 16 | Proposed library/tool choices fit the reset design. | partial-local | Current tests cover existing use of NetworkX graph behavior, eval scoring, ingest parsing, and policy tooling. Later start-blocker evidence supersedes the old PDF parser gap for the local fixture: the Memoria-test venv's PyMuPDF parser ran a tiny real-PDF capture through `capture_pdf_source()` and preserved page/quote/bbox selectors. A later local tool-fit smoke found repo-Python availability for Pydantic JSON schema (`2.13.4`), RapidFuzz token matching, and `sqlite_vec` load into `sqlite3` (`v0.1.9`), plus `/usr/bin/pandoc` `3.1.3`; the Memoria-test venv has `fitz`/`pymupdf4llm` but not RapidFuzz or `sqlite_vec`. These smokes prove local availability only. Alpha.11 still does not implement Pydantic schema emission, Pandoc/CSL export, RapidFuzz dedupe, SQLite-vec fallback routing, or larger PDF corpus-quality checks. | Wire a tool only when the alpha.11 path actually uses it; otherwise keep it out of scope. Larger PDF corpus quality and actual Pydantic/RapidFuzz/sqlite-vec/Pandoc integrations remain future proof points, not release claims. |
| 17 | Stable `source_id` with citekey as generated alias works. | superseded-local | M0 found ingest and docs centered on `citekey`, with no alpha.11 `source_id` field/path model. Later WP4 evidence supersedes this row for the local slice: capture writes stable `source_id` paths, treats citekey as an alias, and tests PI citekey correction without renaming the source Concept. | Keep cross-bundle/export identity proof in later packaging evidence. |
| 18 | Writing/export are deferred, but the functional loop still names draft/verify/export. | superseded-local | The current design, ExecPlan, and release plan now agree: draft/verify/paper export, draft/export compatibility, and the coding/autonomy lane are post-alpha.11 work. Remaining mentions are identity/future-boundary language, not alpha.11 acceptance. | Keep release close-out focused on the basic knowledge cycle; do not add draft/export stubs in alpha.11. |
| 19 | This file's lifecycle/source-of-truth claim is accurate. | superseded-local | M0 found the tracked release design scratch still claiming the old `_notes/` home. The current header now names `docs/releasing/0.1.0-alpha.11/tmp/` as tracked release design scratch and says to delete it before release close or promote durable decisions into ADRs. | Keep the release close-out sweep responsible for deleting or promoting this scratch before alpha.11 closes. |
| 20 | The alpha.11 milestone can ship the full basic knowledge cycle plus plugin as one increment. | partial-local | M0 found no alpha.11 release plan, task breakdown, dependency DAG, or implementation evidence beyond the tmp design note. Later evidence supersedes that row for planning and local implementation slices: this ExecPlan now sequences the work, the release plan names the gates, and WP result files cover several local mechanisms. The local worker-boundary basic cycle now includes trace-to-rollback, and the start-blocker verifier now proves a disposable alpha.11-shaped queue can drain a checked `answer-query` operation plus one harmless live `Memoria-test` queue drain after sandbox refresh. The full live Obsidian Co-PI cycle still needs runtime proof. | Complete the live plugin-cycle evidence and close WP-Docs against the final built behavior before treating the release as ready. |

## Bottom line

At M0, no load-bearing alpha.11 reset assumption was fully verified. The current
implementation state is tracked by the later `wp*-results.md` evidence files,
which supersede this historical table only for their verified slices; live
runtime checks still need their own fresh evidence.
