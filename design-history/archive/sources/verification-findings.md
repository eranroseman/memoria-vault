# Verification findings (raw harvest from wf_03487b83-235)

## A. Verify-phase findings (FALSE / UNSUPPORTED only)
```json
{"section":"verify:a1-pipeline (lines 505–630)","verdict":"FALSE","claim":"Line 603: \"By status in the README index: 48 accepted\"","evidence":"Counting the README index at 0e9ed9dd: `grep -cE '\\| accepted'` = 46 (45 pure `accepted` + 1 `accepted → ADR-28`). 46 + 15 deferred + 7 superseded = 68 numbered ADRs (checks out).","correction":"Accepted count is 46, not 48."}
{"section":"verify:a1-pipeline (lines 505–630)","verdict":"FALSE","claim":"Line 603: \"13 deferred (ADRs 34, 35, 38, 39, 40, 41, 58, 59, 60, 61, 62, 63, 64, 65, 66 — note some marked deferred)\"","evidence":"`grep -cE '\\| deferred \\|'` on the README index = 15. The doc's own parenthetical enumerates 15 ADR numbers (34,35,38,39,40,41,58,59,60,61,62,63,64,65,66), all of which are indeed status `deferred`, yet the stated count is 13.","correction":"Deferred count is 15, not 13. The enumerated list of 15 numbers is itself correct; only the leading count '13' is wrong."}
{"section":"verify:origins (Part I Origins + About, lines 5–67)","verdict":"FALSE","claim":"Line 50: '...by alpha.1 it was co-pi/engineer/librarian/peer-reviewer/writer/retriever-scout and one more.' (asserts a seven-profile alpha.1 roster including retriever-scout)","evidence":"The alpha.1 boundary commit 0e9ed9dd ships exactly FIVE profile packages: memoria-copi, memoria-librarian, memoria-writer, memoria-peer-reviewer, memoria-engineer (git ls-tree of src/.memoria/profiles/), and docs/reference/profiles.md at that commit states 'The five profiles.' There is no retriever-scout package and no seventh. retriever-scout was ADR-37 ('Retriever / Scout as a separate profile'), status superseded_by:[48] — ADR-48 folded 'find' into the single Librarian posture, so it never shipped. The genuine pre-consolidation SEVEN-profile fleet (ADR-02 'Seven specialist profiles') was: Librarian, Mapper, Socratic, Writer, Verifier, Coder, Linter — which also excludes retriever-scout, co-pi, engineer, and peer-reviewer.","correction":"State the alpha.1-baseline roster as the five shipped profiles (co-pi, librarian, writer, peer-reviewer, engineer), OR, if describing the pre-alpha.2 seven-profile fleet, name them per ADR-02 (librarian/mapper/socratic/writer/verifier/coder/linter). Drop retriever-scout and 'one more' — retriever-scout was a superseded proposal (ADR-37→48), not a shipped alpha.1 profile."}
{"section":"verify:origins (Part I Origins + About, lines 5–67)","verdict":"UNSUPPORTED","claim":"Line 62: blockquote presented as verbatim origin-notes quote — 'Layer 7, the vault, evolved into a mix of the original notes and additional data repositories, leading to a lack of clarity. This approach prevented us from selecting optimal solutions for specific problems.'","evidence":"Sentence 1 is a paraphrase of researcher-notes.md:33 ('Layer 7 (the vault) became a mix of original notes + extra data repositories -> lack of clarity') — substance matches. Sentence 2 ('This approach prevented us from selecting optimal solutions for specific problems') appears in NO available source: grep across all of sources/ finds it only inside the design-doc's own build artifact (build/part-0-front.md), not in researcher-notes.md or any notes/ file. researcher-notes.md is flagged as truncated at ~50k chars, so it may derive from the unseen full notes, but it cannot be confirmed against ground truth.","correction":"Either cite the exact source for the second sentence or downgrade the blockquote from a verbatim quotation to a paraphrase (e.g. drop the quote marks / mark it as reconstructed), since the second sentence is not attested in any available source."}
{"section":"verify:a1-gate — \"Write gate, policy engine, provenance & audit\" (lines 407–504)","verdict":"FALSE","claim":"Line 478: \"A missing `task_id` is itself denied and audited (`request.no-task-id`).\"","evidence":"In policy_mcp.py `PolicyEngine.check()` (at 0e9ed9dd) the very first branch is `if not task_id: return {\"decision\": \"deny\", \"policy_rule\": \"request.no-task-id\", ...}` — it returns immediately, BEFORE any `append_audit()` call. No audit entry is written for a missing task_id. (Contrast the traversal branch, which explicitly calls `_audit_traversal()` before returning.) So the request is denied with rule `request.no-task-id`, but it is NOT audited.","correction":"Drop \"and audited\": a missing task_id is denied (rule `request.no-task-id`) but no audit record is appended for it — unlike a path-traversal deny, which is audited via `_audit_traversal()`."}
{"section":"verify:a1-gate — \"Write gate, policy engine, provenance & audit\" (lines 407–504)","verdict":"UNSUPPORTED","claim":"Line 482: \"The FAMA-exposure detector (below) is a Borrow: making 'reuse of obsolete memory' a deterministic check\" — stated within the paragraph describing why-pattern-provenance.md's Borrow/Adapt/Reference/Ignore ledger.","evidence":"why-pattern-provenance.md at 0e9ed9dd contains no mention of FAMA or Memora (repo-wide `git grep -i fama` shows it nowhere in that file); its Borrow table has no obsolete-memory / FAMA entry. The FAMA borrow lineage is documented in ADR-10 (\"Memora's FAMA metric exists to penalize reuse of obsolete/invalidated memory\") and the quote \"reuse of obsolete memory\" comes from detectors.py / how-to guides (promote-a-claim.md), not from the pattern-provenance ledger the paragraph is discussing.","correction":"The fama-exposure detector and the borrow are real, but the source is ADR-10 (Memora/FAMA), not why-pattern-provenance.md. Attribute the Borrow to ADR-10 rather than folding it into the why-pattern-provenance ledger, whose Borrow column does not list it."}
{"section":"verify:alpha.9","verdict":"FALSE","claim":"Line 1344: 'New first-class claim.md template with an action-oriented body and a [!suggestions] callout + QuickAdd buttons (...), schema_version: 2, explicit links.supports/contradicts, and a superseded_by pointer field (src/system/templates/claim.md, #819).'","evidence":"git show b4d775ad:src/system/templates/claim.md (the alpha.8 boundary, i.e. BEFORE the alpha.9 window) already contains the full frontmatter — schema_version: 2, links.supports/contradicts, and superseded_by: \"\" — identical to 830ecd31. `git diff b4d775ad 830ecd31 -- src/system/templates/claim.md` shows the ONLY change in the window is +30 lines appending the `[!suggestions] Claim actions` callout and four QuickAdd buttons (Link claim / Capture source / Delegate task / Archive claim). The template is not new to alpha.9, and schema_version:2 / links / superseded_by were not introduced this version.","correction":"The template pre-existed alpha.8; only the [!suggestions] callout + 4 action buttons were added in this window. Reword to 'claim.md template gains an action-oriented [!suggestions] callout + QuickAdd buttons' and drop the schema_version:2 / links / superseded_by items (or move them to a note that they are pre-existing fields the new write-claim.js path now populates). The button names themselves are correct."}
{"section":"verify:alpha.4","verdict":"FALSE","claim":"Line 847 'Deferred out': '#379/#559 calibration spec + score-* skills, #344 diversity reserve, #412 (ADR-62 harnesses, split to #560/#561), #370 (ADR-38 ratchet — alpha.4 keeps only shadow/report-only; enforcement → #562).'","evidence":"Both §2.2 of the ExecPlan and the b1657d92 commit body say calibration/score-*/reserve = #379/#381/#344 (not #559); #412 (ADR-62 harnesses) is deferred whole with NO split; #370 (ADR-38 ratchet) is 'deferred — depends on the deferred calibration discipline (#379)', not partially kept. `git grep` for #559/#560/#561/#562 returns zero hits anywhere in the alpha.4 release dir or the exec-plan.","correction":"Deferred out: #527, #379/#381/#344 (calibration spec, score-* map skills, diversity reserve), #412 (ADR-62 harnesses, deferred whole), #370 (ADR-38 ratchet, deferred whole). Remove the fabricated issue numbers #559/#560/#561/#562 and the '#370 keeps shadow/report-only; enforcement → #562' claim."}
{"section":"verify:alpha.4","verdict":"FALSE","claim":"Line 847 'Why': 'the 2026-06-16 revalidation against live Hermes docs changed the facts — Hermes now documents ssl_verify for HTTP/SSE MCP (unblocking #527) and native Windows support...'","evidence":"The exec-plan contains no 2026-06-16 date (all decision dates are 2026-06-15), no 'revalidation' event, and zero occurrences of 'ssl_verify'. Far from being 'unblocked', #527 (HTTPS transport) is explicitly DEFERRED OUT: line 80 says 'deferred: keep ADR-31's documented loopback-HTTP residual; ... not a Hermes-only blocker'. The native-Windows carry-over/ADR-64 reframing and #296 stale-v0.3-label flag ARE supported (b1657d92 second commit).","correction":"Drop the '2026-06-16 revalidation / Hermes now documents ssl_verify / unblocking #527' narrative entirely. #527 stayed deferred as an ADR-31 residual. Keep only the native-Windows-as-alpha.3-carry-over and #296-stale-label points, which are supported."}
{"section":"verify:alpha.4","verdict":"FALSE","claim":"Line 929 describes test-env.md as a 'clean-sheet, all-Linux test harness' with 'two single-OS installers: install.ps1 (all-Windows) + install.sh (all-Linux = containerized/ephemeral)', a 'version-controlled golden-image Docker Compose stack (native Hermes + 5 profiles + headless Obsidian via xvfb-run + ... qmd)', 'local Gemma 4 12B (GGUF Q4_K_M) served by llama.cpp (--jinja)', 'full L0–L5 coverage plus new chaos/security/performance suites', deleting the 'Obsidian-on-Windows ↔ Hermes-in-WSL2 bridge / topology parity fragility', and 'revisits ADR-64'.","evidence":"The actual test-env.md (identical at authoring 0145dc3f and at b1657d92, 281 lines) is titled 'Test environment design — WSL2 process-testing env' — a WSL2 test env alongside a Windows production env, mapped onto ADR-29's L0–L5 pyramid, which explicitly KEEPS L3/L4/L5 on Windows and explicitly REJECTS headless Obsidian on WSL2 ('Option A xvfb-run is heavy/flaky ... doesn't gate L2b'). Term-presence audit: install.ps1=0, llama=0, GGUF=0, jinja=0, Docker=0, Compose=0, qmd=0, containeriz=0, ephemeral=0, golden-image=0, chaos=0, security=0, performance=0, ADR-64=0, 'topology parity'=0, 'mirrored'=0. ADR-29=20 hits.","correction":"Rewrite to reflect the real note: a WSL2 process-test env alongside Windows production, refining ADR-29's L2b placement, keeping GUI/L3/L4/L5 on Windows. Correct sub-facts to retain: 281 lines, Gemma 4 12B at Q4_K_M, supersedes the prior model decision (needs a MEMORIA_ENV ADR), moved to alpha.5 via #530. Remove install.ps1/install.sh, Docker Compose, llama.cpp/GGUF/--jinja, chaos/security/performance suites, the bridge-deletion narrative, and ADR-64."}
{"section":"verify:alpha.4","verdict":"UNSUPPORTED","claim":"Lines 869 and 929: 'Commander places the 16 QuickAdd command-palette macros'; 'a pytest orchestrator over the 16 command-palette commands'.","evidence":"At b1657d92 the QuickAdd data.json has 17 top-level command-palette commands (all command=true), 15 entries of type 'Macro', and the Commander cmdr/data.json places 12 named Memoria commands. No source states '16' (the only '16' in test-env.md is 'RTX 4060 Ti 16 GB'). create-linked-claim was added this release (#513), taking the count 16→17.","correction":"Use 17 command-palette commands (or 15 macros / 12 Commander placements, as appropriate); '16' is not supported at the alpha.4 checkpoint."}
{"section":"verify:alpha.11 (lines 1601–1765)","verdict":"FALSE","claim":"\"The type vocabulary collapsed from 27 types to 13\" (line 1629), cited to vault-template/.memoria/schemas/types/*.yaml vs src/.memoria/schemas/types/*.yaml","evidence":"Counting the cited yamls: `git ls-tree 383034b0 -- src/.memoria/schemas/types/` = 25 files; `git ls-tree 1b24453b -- vault-template/.memoria/schemas/types/` = 12 files (confirmed with wc -l: 25 and 12). The design note 0.1.0-alpha.11-design.md contains no \"27\" or \"13\" type-count statement (only 'Date: 2026-06-27'). The doc's own itemized lists also sum to 12 new (6 added + 6 kept), not 13.","correction":"Change to \"collapsed from 25 types to 12\" (equivalently: 19 removed + 6 kept → 6 added + 6 kept). The cited file glob is the authoritative registry and yields 25→12, not 27→13."}
{"section":"verify:alpha.8","verdict":"FALSE","claim":"Line 1288: 'ADR-74 (pinned Obsidian plugin provenance) accepted — repo now ships ... src/.obsidian/plugin-provenance-lock.json ... (commit 0bbfe1cf; ADR-74 diff)'","evidence":"0bbfe1cf's full stat touches only 4 files: ADR-100, ADR-76, ADR-84, and README — it does NOT touch ADR-74 at all. ADR-74's status changed deferred→accepted in commit ffed6dab (#687, 'docs: clarify ADR implementation mappings'), which is in-window. Separately, plugin-provenance-lock.json was first created in c79f659c (alpha.6), not this cycle.","correction":"Attribute ADR-74's acceptance to ffed6dab, not 0bbfe1cf. The provenance-lock.json file is real and current-state, but it predates this cycle (created c79f659c)."}
{"section":"verify:alpha.8","verdict":"FALSE","claim":"Line 1308: 'ADR-83 Direct PI relate control accepted ... (commit 0bbfe1cf)'","evidence":"ADR-83 status transitions: authored as 'deferred' in 4e593a4c (#692), then changed to 'accepted' in 89588b3d (#738, 'amend ADRs for implementation drift (81, 75, 83)'). By 0bbfe1cf it was already accepted; 0bbfe1cf does not touch ADR-83 (stat = ADR-100/76/84/README only). Note line 1266's parenthetical correctly says '0bbfe1cf accepts 76/84/100' (omitting 83), so line 1308 contradicts line 1266.","correction":"Attribute ADR-83's acceptance to commit 89588b3d, not 0bbfe1cf."}
{"section":"verify:alpha.8","verdict":"FALSE","claim":"Line 1259: 'Retired/disposed all carried-forward tmp/ scratch across alpha.4-7 release folders (deletions under docs/releasing/0.1.0-alpha.{4,5,6,7}/tmp/; commits be840217, aa02089f, 2593076e)'","evidence":"be840217 and aa02089f deleted files under docs/releasing/0.1.0-alpha.8/tmp/ (install-a-real-package-alpha5.md, test-env-design-alpha5.md, ui-architecture-*-alpha7.md, alpha7-docs-audit-report.md, deferred-adr-implementability-alpha6.md) — files named for alpha 5/6/7 but living in the alpha.8 tmp dir. 2593076e ('align ADR issue routing guidance') deleted nothing (no D-files). The only in-window deletion under an alpha.4-7/tmp path was in 30075fb3 (alpha.7/tmp/dashboards/*), a different commit not cited here.","correction":"The disposed scratch lived under docs/releasing/0.1.0-alpha.8/tmp/, not alpha.{4,5,6,7}/tmp/. Drop 2593076e (not a deletion commit); cite only be840217 and aa02089f."}
{"section":"verify:alpha.14","verdict":"FALSE","claim":"Line 1982 (and table row line 2056): the Zotero-specific import surface removed in #1123 (7ad88a84) included `regenerate-ai-catalog.md`.","evidence":"git show --name-status 7ad88a84 does not touch regenerate-ai-catalog.md at all (only capture-zotero-source.md is deleted, plus cli.py -73, worker.py -60, capture.py -179). regenerate-ai-catalog.md was RENAMED to regenerate-capability-index.md in #1066 (f0da1a98, `R050 .../regenerate-ai-catalog.md -> .../regenerate-capability-index.md`), and it is the capability-index generator, not a Zotero surface.","correction":"Drop `regenerate-ai-catalog.md` from the Zotero/#1123 removal list in both line 1982 and the table row at line 2056. It was renamed (not removed) in #1066 and is unrelated to Zotero."}
{"section":"verify:alpha.14","verdict":"FALSE","claim":"Line 2012 (and table row line 2058): `skill-state.md` (136 lines) was 'removed with the Obsidian payload #1124'.","evidence":"git log --diff-filter=D 6c2d5b04..88b7f0f5 -- vault-template/system/dashboards/skill-state.md returns exactly one commit: a3794fb6 'Remove installed profile runtime surface (#1119)'. It was NOT deleted in 5a93b142/#1124 (the Obsidian payload commit); that commit deletes the .obsidian tree and system/scripts/*.js only. The '136 lines' figure is correct (git show 6c2d5b04:...skill-state.md | wc -l = 136).","correction":"Attribute skill-state.md removal to #1119 (a3794fb6, profile runtime surface), not #1124. Fix both line 2012 and the table row at line 2058."}
{"section":"verify:alpha.12 (lines 1766–1837)","verdict":"FALSE","claim":"Line 1768: the release was 'driven by ... four disposable pre-implementation spikes whose three FAIL verdicts forced concrete contract decisions.'","evidence":"Two distinct spike artifacts exist. scratch/releases/0.1.0-alpha.12/preimpl_spikes/run_spikes.py has exactly FOUR spikes (1. lifecycle/crash fixture, 2. references.bib durability, 3. SQLite journal integrity, 4. rollback graph) and all PASS — running it prints \"failures\": []. The three FAIL verdicts the doc quotes throughout (Persistence, Catalog-recovery, Cross-store) come from a DIFFERENT artifact, alpha12-test-results.md / alpha12_test_harness.py, which has SEVEN results: 'PASS: 4, FAIL: 3'. No source has 'four spikes' with 'three FAILs' — the four-spike set had zero failures; the set with three failures has seven results.","correction":"Either 'seven disposable spike results (4 PASS / 3 FAIL, per alpha12-test-results.md)' or clearly separate the four all-PASS run_spikes.py spikes from the seven-result test harness. The 'four ... whose three FAIL' combination is internally contradictory."}
{"section":"verify:alpha.12 (lines 1766–1837)","verdict":"UNSUPPORTED","claim":"Lines 1770 & 1784: 'The design specifies an entities/catalog_links graph, field-provenance, enrichment runs...' and 'the design's entities, catalog_links, field-provenance, and enrichment tables did not ship.'","evidence":"grep of 0.1.0-alpha.12-design.md (and the exec-plan, and the whole releases/0.1.0-alpha.12 tree) finds ZERO occurrences of 'enrich' / 'enrichment' and no 'field-provenance' / 'per-field' provenance. The design's §3 catalog shape (line 84) specifies only sources / entities / catalog_links; 'provenance refs' is a single column on the sources table, not a field-provenance mechanism. entities, catalog_links, and recursive-CTE cascade-rollback ARE in the design (verified); field-provenance and enrichment are not.","correction":"Drop 'field-provenance' and 'enrichment runs/tables' from the design-only list, or cite where the design specifies them. The verified design-only items are entities, catalog_links, and recursive-CTE cascade-rollback (plus journal-heads.jsonl anchor and Turso adapter)."}
{"section":"verify:alpha.12 (lines 1766–1837)","verdict":"UNSUPPORTED","claim":"Line 1784: '(SCHEMA_VERSION not yet externalized — the standalone schema.sql file arrives in a later release).'","evidence":"At 7765040d there is no SCHEMA_VERSION constant in state.py (git grep finds only unrelated AUDIT_SCHEMA_VERSION in policy/audit.py and a capabilities schema_version). And schema.sql does not appear in any tree through alpha.15 (git ls-tree at 6c2d5b04/88b7f0f5/5b1ea8bc finds no schema.sql). The substantive in-window fact — the schema is inlined via executescript with no standalone file — is true; the parenthetical is what fails.","correction":"State only that the schema is inlined in state.py via executescript with no standalone schema.sql in this range; drop the 'SCHEMA_VERSION not yet externalized' phrasing (no such constant) and the unverified 'arrives in a later release' (no schema.sql through alpha.15)."}
{"section":"verify:alpha.10","verdict":"FALSE","claim":"Line 1444: the `index` document type was removed — 'index.yaml deleted, `24540227`/ADR-119'.","evidence":"`git rev-parse 24540227` → 'unknown revision'. The index schema/template/folders.yaml entries were actually deleted in `cb81a255` (#924, 'implement updated ADR-119 schema contract'): `git show cb81a255 --stat` shows deletion of src/.memoria/schemas/types/index.yaml, src/system/templates/index.md, and folders.yaml drops `index: notes/indexes` and the `- notes/indexes` skeleton entry. `28540227` (a plausible typo target) is #888 'Document sample vault load and removal', unrelated to index removal.","correction":"Change the SHA from `24540227` to `cb81a255` (#924). The described change (schema, template, folders.yaml index entry + notes/indexes skeleton all gone) is otherwise correct."}
{"section":"verify:alpha.10","verdict":"FALSE","claim":"Line 1475: the superseded-claim filter 'landed in `bafa92e2`/#851 \"filter superseded claims from qmd\"'.","evidence":"`git rev-parse bafa92e2` → 'unknown revision'. #851 is actually commit `baa92e61` 'fix(alpha9): filter superseded claims from qmd' (confirmed via `git log --oneline 830ecd31..383034b0 | grep 851`). The subject and PR number match; only the SHA is wrong.","correction":"Change `bafa92e2` to `baa92e61`."}
{"section":"verify:alpha.10","verdict":"FALSE","claim":"Line 1466: the `memory` toolset 'became profile-scoped to `memoria-copi` only (enforced in `policy_hook.py` via `PROFILE_SCOPED_DIRECT_TOOLS = {\"memory\": {\"memoria-copi\"}}`)'.","evidence":"No such constant exists. `git grep -n 'PROFILE_SCOPED' 383034b0 -- src tests` returns nothing; policy_hook.py (at src/.memoria/mcp/policy_hook.py) has no PROFILE_SCOPED_DIRECT_TOOLS. Enforcement is actually via the per-profile tool-registry allowlist inside evaluate_pre()/_allowed_registry_tools(). The substantive claim is TRUE though: tests/test_policy_gate_completeness.py asserts `evaluate_pre(_payload(\"memory\"), \"memoria-copi\") == {}` (allowed) while librarian/writer get decision=='block'; librarian config sets memory_enabled:false and lists memory in disabled_toolsets; copi config has memory write_approval:true and provider:''.","correction":"Remove/replace the fabricated constant. Memory is scoped to memoria-copi via the tool-registry allowlist evaluated in policy_hook.py's evaluate_pre(), verified by tests/test_policy_gate_completeness.py — not via a `PROFILE_SCOPED_DIRECT_TOOLS` constant."}
{"section":"verify:alpha.10","verdict":"FALSE","claim":"Line 1540: 'the seven old pipeline-ordered tutorials … were deleted and replaced by six (01-orient … 06-close-the-loop … 07-make-it-your-own)'.","evidence":"At boundary 383034b0, docs/tutorials/ has SEVEN numbered files: 01-orient, 02-bring-in-your-first-source, 03-build-claims-and-connect-them, 04-draft-a-section-from-your-claims, 05-verify-it-holds, 06-close-the-loop, 07-make-it-your-own. The parenthetical itself lists up to 07, and the arc enumerated (orient→capture→distill&connect→draft→verify→close→graduate) is 7 steps. ADR-112's arc has 7 beats. 'six' comes from the superseded ADR-111 spine ('6 tutorials, was 7'), not the shipped result.","correction":"Change 'replaced by six' to 'replaced by seven' (01-orient … 07-make-it-your-own)."}
{"section":"verify:alpha.15","verdict":"FALSE","claim":"§1 (line 2082): 'work.yaml has `work_id` required, `citations`/`evidence_set` optional'","evidence":"git show 5b1ea8bc:vault-template/.memoria/schemas/types/work.yaml — optional fields are ONLY {aliases, archived, description, x}; no citations, no evidence_set. Same at introduction (71ec0dc4). work_id required is correct, but citations/evidence_set never appear in work.yaml.","correction":"Drop the '`citations`/`evidence_set` optional' clause. work.yaml's optional fields are aliases/archived/description/x only. (citations/evidence_set existed on the deleted digest.yaml, not on work.yaml.)"}
{"section":"verify:alpha.15","verdict":"FALSE","claim":"§7 (line 2172) and removals table (line 2226): '`calibration.yaml` **deleted**' / 'The static calibration.yaml (entity_resolution/classify/clustering/hybrid_scores floors) was retired'","evidence":"git ls-tree 5b1ea8bc shows vault-template/.memoria/schemas/calibration.yaml still present; git show 5b1ea8bc:...calibration.yaml still contains entity_resolution, classify, and hybrid_scores sections. Only commit touching it in-window is 97a819af (#1204) which removed the CLUSTERING section only. The file was not deleted, and the entity_resolution/classify/hybrid_scores floors were NOT retired.","correction":"Change 'calibration.yaml deleted' to 'the clustering-config surface was removed from calibration.yaml (PR #1204)'. The file and its entity_resolution/classify/hybrid_scores floors survive at release."}
{"section":"verify:alpha.15","verdict":"FALSE","claim":"§5 (line 2138): enrichment merge landed via 'PR-F #1173, `ingest_paper.py` +114'","evidence":"git show 82d0a6a9 --numstat: ingest_paper.py = 1 insertion / 1 deletion. The enrichment logic actually landed in runtime/enrichment.py (+133/-13). No file in the PR has a +114 count.","correction":"The enrichment merge code is in runtime/enrichment.py (~+133), not ingest_paper.py (+1). Fix both the filename and the line count."}
{"section":"verify:alpha.15","verdict":"UNSUPPORTED","claim":"PR-number citations #1158/#1159 (§11 line 2212, ADR consolidation) and PR-A #1160/#1161/#1162 (§2 lines 2096/2098, §6 line 2152)","evidence":"git log --all shows NO PRs #1158–#1162 anywhere; the lowest-numbered PR in the 88b7f0f5..5b1ea8bc window is #1163 (c1d42690). The five earliest alpha.15 commits the doc pins to these PRs (1a8664da, a3b3f1f4, be2ea1b9, afa83639, d502ac20) carry no '(#NNNN)' in their subjects. The SHAs themselves are all real and correctly attributed.","correction":"Cite these landings by SHA only (1a8664da/a3b3f1f4 for ADR consolidation; be2ea1b9/afa83639/d502ac20 for the stack/schema/retrieval work) and drop the unverifiable #1158–#1162 PR numbers."}
```

## B. Recheck-phase verdicts (adversarial second pass)
```json
{"final":"CONFIRMED-ERROR","claim":"Line 603: \"13 deferred (ADRs 34, 35, 38, 39, 40, 41, 58, 59, 60, 61, 62, 63, 64, 65, 66 — note some marked deferred)\"","reasoning":"Independently re-derived against the README ADR index at the exact commit the doc cites (`git show 0e9ed9dd:docs/adr/README.md`). Counting the status column strictly (`grep -cE '\\| deferred \\|$'`) yields 15 deferred, and the ADR numbers of those 15 rows are exactly 34 35 38 39 40 41 58 59 60 61 62 63 64 65 66 — the very list the doc enumerates. So the enumerated list is correct and every one of the 15 is status=deferred; only the leading count \"13\" is wrong. I attempted to defend \"13\": (a) commit window is correct — I used the doc's own SHA 0e9ed9dd, whose ls-tree returns 70 files (68 numbered ADRs + README.md + _template.md) as the doc states; (b) no title-vs-status confusion — I matched the status column, not title text; (c) no alternate README — the current HEAD README doesn't even use the \"deferred\" status, so the doc must mean 0e9ed9dd, which I checked. The hedge \"note some marked deferred\" cannot yield 13, since all 15 listed ADRs carry status deferred. Full breakdown at 0e9ed9dd: accepted 46 (45 bare + 1 \"accepted → ADR-28\"), deferred 15, superseded 7, total 68. Note the doc also misstates accepted as 48 (actual 46); 48+13 and 46+15 both sum to 68, which explains how the off-by-two miscount passed a total-check. The first verifier's flag is upheld and its proposed correction (deferred = 15, list of 15 correct, only the count \"13\" wrong) is exactly right."}
{"final":"CONFIRMED-ERROR","claim":"Line 603 of memoria-design-history-alpha.1-to-alpha.15.md: \"By status in the README index: 48 accepted\" (for docs/adr/README.md at commit 0e9ed9dd, alpha.1 boundary).","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. Confirmed the right file/window: `git ls-tree -r 0e9ed9dd -- docs/adr/` = 70 md files (68 numbered ADRs 01–68 + README.md + _template.md), matching the same sentence's context, so 0e9ed9dd:docs/adr/README.md is the correct index. Counting the 68 table rows: rows matching `accepted` = 46 (grep -cE '\\| accepted' = 46; of these 45 end exactly `| accepted |` and 1 is ADR-27's `accepted → ADR-28`); rows ending `| deferred |` = 15; rows with `superseded → ADR` = 7. Sum 46+15+7 = 68 (checks out). The doc's \"48 accepted / 13 deferred / 7 superseded\" also sums to 68 but is achieved by mis-shifting 2 rows from deferred into accepted. Tried hard to defend 48: no valid reading of the README status column reaches 48 — including the lone arrow row gives 46, excluding it gives 45; reaching 48 would require improperly adding README.md + _template.md (which carry no status and are already counted as the +2 beyond the 68 ADRs). The doc is also internally inconsistent: its own parenthetical lists 15 deferred ADR numbers (34,35,38,39,40,41,58–66) yet the prose says 13, and it lists 27→28 among superseded although ADR-27 is actually marked accepted→ADR-28. The correct breakdown is 46 accepted, 15 deferred, 7 superseded. The first verifier's flag and correction (46, not 48) are upheld."}
{"final":"CONFIRMED-ERROR","claim":"Line 62 presents as a verbatim origin-notes quotation: 'Layer 7, the vault, evolved into a mix of the original notes and additional data repositories, leading to a lack of clarity. This approach prevented us from selecting optimal solutions for specific problems.'","reasoning":"I independently re-derived the ground truth and the flag holds. (1) Presentation: line 60 introduces the text as \"The origin notes name the flaw...\" and line 62 renders it as `> *\"...\"*` — the same italic + double-quote blockquote form the doc reserves for genuine verbatim quotations (cf. the vision.md quote at line 27, the Hermes quotes at line 41). It is unambiguously offered as a direct origin-notes quotation, not a paraphrase. (2) Sentence 2 (\"This approach prevented us from selecting optimal solutions for specific problems\") is unattested. An exhaustive grep across the full scratch tree (sources/notes, versions, prealpha-docs-design, old-skeleton, releases/0.1.0-alpha.11..15, releases/0.1.0-beta.1) and the main repo returns it in only two files: this design doc itself and its own build artifact sources/build/part-0-front.md:62. The single other occurrence of \"selecting optimal solutions\" is in an unrelated paper (Ren et al. 2025) about search-based planners — different context. (3) The truncation defense fails. researcher-notes.md is truncated at line 356 (\"item-note and paper-note are a mixture of a note\"), but the origin story spans lines 9–40 and is fully intact; the Layer 7 statement is at lines 32–34, and the sentence immediately following \"lack of clarity\" is \"Never conceptualized Memoria as an application, never implemented a proper agentic application stack\" — not the doc's second sentence. If the second sentence belonged to the origin narrative it would sit in this fully-preserved region; it does not. (4) Even sentence 1 is a paraphrase, not verbatim: notes l.33 reads \"Layer 7 (the vault) became a mix of original notes + extra data repositories -> lack of clarity\" vs the doc's \"evolved into a mix of the original notes and additional data repositories, leading to a lack of clarity\" (\"evolved into\"!=\"became\", \"additional\"!=\"extra\", prose vs the \"->\" shorthand). So the entire blockquote is a reconstruction: sentence 1 close-paraphrases the notes (substance matches), and sentence 2 has no attestation in any available source. Presenting the whole thing as a verbatim origin-notes quote is unsupported. The proposed correction — cite the exact source for sentence 2 or downgrade the blockquote from a verbatim quotation to a marked paraphrase/reconstruction — is the right fix."}
{"final":"CONFIRMED-ERROR","claim":"Line 50: '...by alpha.1 it was co-pi/engineer/librarian/peer-reviewer/writer/retriever-scout and one more.' — asserting a seven-profile alpha.1 roster that includes retriever-scout.","reasoning":"Independently re-derived against the alpha.1 boundary commit 0e9ed9dd and its shipped docs. git ls-tree of src/.memoria/profiles/ at 0e9ed9dd yields exactly FIVE profile packages (distinct-dir count = 5): memoria-copi, memoria-engineer, memoria-librarian, memoria-peer-reviewer, memoria-writer. No 'retriever' or 'scout' profile package exists; grepping the whole tree, 'retriever'/'scout' appear only in docs/adr/37-retriever-scout-profile.md and cross-references (ADR-61/65/README). alpha.1's own shipped docs corroborate five and explicitly reject seven: docs/reference/profiles.md is headed 'The five profiles — One conversational agent (the co-PI) plus four background agents,' and docs/explanation/profiles/README.md says 'one conversational agent — the co-PI — and four background agents... v0.1.0-alpha.2 ships all five profiles... Why one co-PI + four lanes, not seven specialists.' retriever-scout never shipped: ADR-37 at this commit has frontmatter status: superseded, superseded_by: [48], with the comment 'one Librarian posture spans catalog·extract·link·map, find included' — i.e. ADR-48 folded discovery/find into the single Librarian, so the split profile was killed as a proposal. I tried hard to defend the doc: the Old Skeleton source note does claim 'the alpha.1 baseline (which kept the seven profiles),' but this cannot rescue the claim because (a) the actual boundary tree and alpha.1's own reference/explanation docs say five and label seven the superseded design, and (b) retriever-scout is not even one of the Old Skeleton seven (co-pi/socratic, librarian, mapper, writer, coder, verifier, cmdr per sources/old-skeleton lines 38-39). So 'retriever-scout' appears in neither the Old Skeleton roster nor the shipped alpha.1 roster; it is solely a superseded ADR. The doc named the five real alpha.1 profiles correctly (co-pi/engineer/librarian/peer-reviewer/writer) but then wrongly appended 'retriever-scout' and 'one more' to force a count of seven. The first verifier's core evidence and proposed correction are accurate (state alpha.1 as the five shipped profiles; drop retriever-scout and 'one more'). Note the doc's separate Old-Skeleton clause (coder/writer/socratic/mapper/librarian/verifier/cmdr) is actually supported by the source, but that is not the flagged clause. The flagged alpha.1-roster claim is a confirmed factual error."}
{"final":"CONFIRMED-ERROR","claim":"Line 482: \"The FAMA-exposure detector (below) is a Borrow: making 'reuse of obsolete memory' a deterministic check\" — stated inside the paragraph describing why-pattern-provenance.md's Borrow/Adapt/Reference/Ignore ledger.","reasoning":"I re-derived ground truth against /home/eranr/memoria-vault/main at commit 0e9ed9dd and every component of the first verifier's evidence holds.\n\n(1) I read the full why-pattern-provenance.md at 0e9ed9dd. Its Borrow column has exactly 12 rows: Stage-gated pipeline, Explicit roles per agent, Strong schema with validated handoffs, Persistent knowledge graph, Reviewable organization artifacts, Persistent Kanban + worker lanes, Point-of-action discovery loop, File-as-Bus, Structured outputs at handoff boundaries, Cross-run knowledge accumulation, Paper↔code repository linking, and Claim-to-evidence chain. There is NO FAMA / obsolete-memory / Memora entry. `git grep -i \"fama|memora|obsolete|superseded\"` against that file returns zero matches, and a count of standalone \"Memora\" is 0. Memora (the FAMA-origin system) is not even in the file's \"Systems surveyed\" list.\n\n(2) The FAMA borrow lineage is documented in ADR-10 (10-claim-supersession.md), verbatim: \"Memora's FAMA metric exists to penalize reuse of obsolete/invalidated memory\" and \"the Linter gains a FAMA-style detector that flags any draft or answer citing a superseded claim.\" ADR-10 is the true source of the borrow.\n\n(3) The quoted phrase \"reuse of obsolete memory\" is verbatim from detectors.py (fama_exposure finding message, line 490), promote-a-claim.md (line 47), and linter.md — not from the pattern-provenance ledger the paragraph is discussing.\n\n(4) Corroborating: the doc's own source-file list for this section (line 503) cites ADRs 03,12,25,27,28,38 and why-pattern-provenance.md, but omits ADR-10 entirely. So the doc genuinely folded the FAMA borrow into the ledger and never cited its real source.\n\nI tried to defend the doc under a charitable reading (that \"Borrow\" is used only in a generic sense, not as the ledger's column). It does not survive: the sentence sits inside a paragraph whose topic sentence explicitly enumerates the ledger's Borrow/Adapt/Reference/Ignore columns, whose other claims (the Ignore-column refusals — SmartPause, CycleResearcher, NanoResearch, tournament loop) are all verifiably drawn from that ledger, so the capitalized \"Borrow\" reads as the ledger's column and implies the ledger classifies FAMA-exposure there. It does not — the ledger contains no such entry and does not mention Memora/FAMA at all.\n\nThe underlying design fact is true (FAMA-exposure IS a borrowed pattern turned into a deterministic check, per ADR-10 + detectors.py), but the claim's ATTRIBUTION — presenting the FAMA borrow as an element of the why-pattern-provenance.md ledger — is a genuine mis-citation. The flag is fully correct; the fix is to attribute the borrow to ADR-10 (Memora/FAMA) and the quote to detectors.py/promote-a-claim.md, not to the pattern-provenance ledger."}
{"final":"CONFIRMED-ERROR","claim":"Line 478: \"A missing `task_id` is itself denied and audited (`request.no-task-id`).\"","reasoning":"Independently confirmed against the code at 0e9ed9dd (the commit this section is pinned to per line 503). In src/.memoria/mcp/policy_mcp.py, PolicyEngine.check() begins (lines 505-507) with `if not task_id: return {\"decision\": \"deny\", \"policy_rule\": \"request.no-task-id\", \"message\": \"task_id is required on every request\"}` — a bare dict returned immediately, BEFORE any append_audit() or _audit_traversal() call. The traversal branch (lines 511-513) does the opposite: it calls self._audit_traversal(...) before returning. The normal-decision path audits at lines 532-548. So a missing task_id is denied but never written to audit.jsonl. The hook layer (policy_hook.py lines 213-217) likewise blocks fail-closed on a missing task_id without any audit and without even calling check(). No layer audits it. The first verifier's evidence and proposed correction (\"drop 'and audited'\") are exactly right: denied YES (rule request.no-task-id), audited NO. I tried hard to defend the doc. The only real mitigation is that the cited source docs/reference/policy-mcp.md @0e9ed9dd line 24 itself states \"A missing task_id or a path-traversal attempt ... is denied and audited\" — so the design-history author faithfully paraphrased the reference doc rather than fabricating; the inaccuracy was inherited. But the reference doc conflated the two cases and is itself wrong versus the code, which is the authoritative ground truth for this behavioral claim. The design-history doc is also internally inconsistent, since line 471 correctly describes the _audit_traversal() contrast. Net: the \"and audited\" portion is factually false; the flag stands."}
{"final":"CONFIRMED-ERROR","claim":"Line 929 (in the alpha.4 \"Design notes authored here, then deferred to alpha.5\" section) describes test-env.md, as authored at 0145dc3f and moved at 7dd0d09f, as a \"clean-sheet, all-Linux test harness\" (281 lines) with two single-OS installers install.ps1/install.sh, a golden-image Docker Compose stack (native Hermes + 5 profiles + headless Obsidian via xvfb-run + qmd), Gemma 4 12B (GGUF Q4_K_M) served by llama.cpp (--jinja), full L0–L5 plus new chaos/security/performance suites, deletion of the WSL2 bridge / \"topology parity\" fragility class, and revisiting ADR-64.","reasoning":"I re-derived the ground truth from git and the flag holds. The two commits the doc cites are in the alpha.4 window, but at BOTH of them the file is the WSL2 version, not the all-Linux one. At the authoring commit 0145dc3f (#528), docs/releasing/0.1.0-alpha.4/tmp/test-env.md is titled \"Test environment design — WSL2 process-testing env,\" 281 lines, a WSL2 test env alongside Windows production mapped onto ADR-29's L0–L5 pyramid; model is Gemma 4 12B via vLLM (\"local Gemma 4 via vLLM,\" ~7–8 GB at Q4_K_M). Term audit at 0145dc3f: install.ps1=0, llama=0, GGUF=0, jinja=0, Docker=0, Compose=0, qmd=0, containeriz=0, chaos=0, security=0, performance=0, ADR-64=0, \"topology parity\"=0 (ADR-29=21, ADR-31=5). 7dd0d09f (#530) is a pure rename (0 line changes) to alpha.5/tmp; at the alpha.4 boundary b1657d92 the file is still 281 lines, still the WSL2 title. The \"clean-sheet, all-Linux\" harness the doc describes is a LATER redesign — commit 8c02d415 (#563, \"redesign alpha.4 test-env as complete all-Linux harness\"), committed 2026-06-16 01:20, AFTER b1657d92 (2026-06-15 17:22). git merge-base confirms 8c02d415 is NOT an ancestor of b1657d92 (not alpha.4 work) but IS an ancestor of alpha.5 (098ca361). That 227-line redesign is where install.ps1/install.sh, Docker Compose golden-image, containerized/ephemeral, qmd, chaos/security/performance, \"topology parity,\" ADR-64, and the \"16 command-palette commands\" all appear. So the doc pulled the alpha.5 redesign's content and mis-attributed it to the alpha.4 authoring+move commits, placing it under \"authored here [in alpha.4].\" Additionally, GGUF and --jinja appear ZERO times in EITHER version, and neither uses llama.cpp as the server (authored=vLLM; redesign=Ollama, with llama.cpp named only as a \"perf/MCP alt\") — so \"GGUF Q4_K_M served by llama.cpp (--jinja)\" is fabricated even against the redesign. The doc author was almost certainly misled by sources/versions/0.1.0-alpha.4/tmp__test-env.md, a 263-line clean-sheet snapshot filed under alpha.4, but git at every cited commit refutes the claim. A minority of embedded sub-facts are individually accurate and worth preserving in a rewrite: the note was authored in alpha.4 and moved to alpha.5 via #530 (7dd0d09f is a git rename), the 281-line count matches the WSL2 version, it uses Gemma 4 12B at Q4_K_M, it carries the process-risk note (supersedes the prior test-env model decision; the MEMORIA_ENV ADR does not yet exist), and it refines ADR-29 (L2b placement) with ADR-31 transport. But these do not rescue the claim: its load-bearing characterization — a clean-sheet all-Linux Docker/two-installer harness with chaos/security/performance suites and bridge-deletion, authored in alpha.4 — is a confirmed misattribution of a later (alpha.5-window) redesign. The first verifier's evidence, term audit, and proposed correction are all sound; I found no context that reverses the flag."}
{"final":"CONFIRMED-ERROR","claim":"Line 1475: the superseded-claim filter 'landed in `bafa92e2`/#851 \"filter superseded claims from qmd\"'.","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. The cited SHA `bafa92e2` does not exist: `git rev-parse bafa92e2` → unknown revision, `git cat-file -t bafa92e2` → \"Not a valid object name\", and `git rev-parse --disambiguate=bafa92e2` returns zero objects (so it is not a truncated-but-valid prefix). The real commit for PR #851 is `baa92e61` (full: baa92e6138b9ba2efc7c746fb709231f8d09aa30), subject exactly `fix(alpha9): filter superseded claims from qmd (#851)` — matching the doc's verbatim quote and PR number. It is the unique commit referencing #851 in all history. It also correctly lands in the alpha.10 change window (830ecd31..383034b0): present in `git log --oneline 830ecd31..383034b0`, ancestor of tip 383034b0 but not of 830ecd31. Its diff adds `src/.memoria/mcp/qmd_filter_mcp.py` (+154) and edits `src/.memoria/tool-registry.yaml` and the copi `config.yaml`, matching the surrounding \"What:\" prose. I attempted to defend the doc via disambiguation, transposition tolerance, and an alternate \"underlying filter\" commit; none resolve `bafa92e2` to any object. The SHA is the only wrong token — everything else (PR #851, subject quote, the fact it landed the qmd superseded filter) is accurate. Flag upheld; correction `bafa92e2` → `baa92e61` is exactly right. Scope of the defect is narrow (single mistyped SHA), so PARTIAL was considered, but the specific cited fact — the commit SHA — is genuinely false, so the flag is a CONFIRMED-ERROR."}
{"final":"CONFIRMED-ERROR","claim":"Line 1444: the `index` document type was removed — cited as 'index.yaml deleted, `24540227`/ADR-119'.","reasoning":"I independently re-derived the ground truth and the flag stands. (1) `git rev-parse 24540227` fails with 'unknown revision'; `git rev-parse --disambiguate=24540227` and `git rev-list --all | grep 24540227` return nothing — no full hash and no abbreviated object prefix matches the cited SHA anywhere in the repo, and it appears nowhere in the sources. My attempt to defend the original SHA fully exhausted every avenue and found nothing. (2) The only commit in ALL history that deletes src/.memoria/schemas/types/index.yaml is cb81a255 (#924, 'fix: implement updated ADR-119 schema contract'). Its diff deletes index.yaml (12 lines) and templates/index.md (10 lines), and drops both `index: notes/indexes` and the skeleton `- notes/indexes` from folders.yaml — exactly matching the doc's described change. (3) cb81a255 is inside the alpha.10 window: descendant of alpha.9 baseline 830ecd31 and ancestor of alpha.10 baseline 383034b0; the index files exist at 830ecd31 and are gone at 383034b0, confirming removal within the window. (4) ADR-119 attribution is correct — cb81a255 is part of the window's ADR-119 cluster (#920-#925). So the design fact, the ADR reference, and the window are all accurate; the sole error is the citation SHA, which is invalid and should be cb81a255 (#924). Note the first verifier's guessed typo target 28540227 is a different real commit (#888, 'Document sample vault load and removal', unrelated), so this is not a single-digit slip but a fabricated/wrong SHA — the correction target cb81a255 is nonetheless unambiguously correct. The flagged defect (the SHA is wrong) is confirmed."}
{"final":"CONFIRMED-ERROR","claim":"Line 1344 (alpha.9 delta): 'New first-class claim.md template with an action-oriented body and a [!suggestions] callout + QuickAdd buttons (...), schema_version: 2, explicit links.supports/contradicts, and a superseded_by pointer field (src/system/templates/claim.md, #819).'","reasoning":"I re-ran the git evidence against /home/eranr/memoria-vault/main and every component of the first verifier's flag holds. (1) src/system/templates/claim.md is NOT new to alpha.9: `git cat-file -e` confirms it exists at dcfa5938 (alpha.7), b4d775ad (alpha.8), and 830ecd31 (alpha.9). So \"New ... claim.md template\" is false in this delta section. (2) The frontmatter block (schema_version: 2, links.supports/contradicts, superseded_by: \"\") is byte-for-byte identical at the alpha.8 boundary b4d775ad and the alpha.9 boundary 830ecd31 (diff of lines 1-14 = no output). It was therefore NOT introduced in the alpha.9 window; `git log -S 'schema_version: 2'` traces schema_version:2 back to f216a95a \"Implement alpha.6 correctness gates (#651)\" — three versions earlier. Attributing these fields to alpha.9 is false. (3) `git diff b4d775ad 830ecd31 -- src/system/templates/claim.md` shows the ONLY change in the window is +30 appended lines: the `> [!suggestions] Claim actions` callout plus four QuickAdd buttons (Link claim / Capture source / Delegate task / Archive claim). `git log b4d775ad..830ecd31` on that path confirms a single commit, d56038e7 \"feat: refine Obsidian capture workflows (#819)\" — matching the doc's own #819 citation. I tried to defend the doc: (a) the file path and PR #819 citation ARE correct; (b) the four button names ARE correct and ARE genuinely new this version; (c) the [!suggestions] callout IS genuinely new this version. But the doc's own cited PR (#819) only adds the callout+buttons and touches no frontmatter, so even the doc's citation contradicts its \"schema_version:2 / links / superseded_by\" novelty framing. No wrong-window, wrong-file, or paraphrase escape hatch rescues it: the window mapping (b4d775ad..830ecd31) is correct and the frontmatter demonstrably pre-existed it. The flag correctly preserves the accurate parts (buttons/callout, path, PR#) while faulting the false \"new template + new frontmatter\" attribution, so it is not an overreach — it is fully upheld. Recommended fix matches the first verifier's: reword to \"claim.md template gains an action-oriented [!suggestions] callout + QuickAdd buttons,\" and either drop schema_version:2 / links / superseded_by or explicitly note them as pre-existing fields."}
{"final":"CONFIRMED-ERROR","claim":"Line 1308: 'ADR-83 Direct PI relate control accepted ... (commit 0bbfe1cf)'","reasoning":"The doc's SHA attribution is genuinely false, so the flag stands — but the first verifier's proposed correction (89588b3d) is also wrong; the real commit is 4f524fce (#696).\n\nIndependent re-derivation against /home/eranr/memoria-vault/main, file docs/adr/83-direct-pi-relate-control.md:\n- status was `deferred` when authored in 4e593a4c (#692, 'docs: add direct PI relate control ADR').\n- The deferred→accepted flip happened in commit 4f524fce (#696) 'docs: retire deferred ADR status': `git show 4f524fce~1:<file>` = `status: deferred`, `git show 4f524fce:<file>` = `status: accepted`, and the diff shows exactly `-status: deferred` / `+status: accepted`.\n- By the time 0bbfe1cf (#732) ran, ADR-83 was ALREADY accepted. 0bbfe1cf's diff does not touch ADR-83 at all (`git show 0bbfe1cf --stat` lists only docs/adr/100, docs/adr/84, docs/adr/README.md). Its subject is 'docs: accept ADR-76 (vault-release spine), ADR-84, ADR-100 (#732)'. So line 1308's '(commit 0bbfe1cf)' attribution for ADR-83's acceptance is false, and it contradicts the doc's own line 1266 parenthetical 'commit 0bbfe1cf accepts 76/84/100' (which correctly omits 83). => doc claim is a CONFIRMED-ERROR.\n\nDefense attempt for the doc failed: there is no file/window under which 0bbfe1cf accepts ADR-83 — it never edits that file, and the status was already 'accepted' at its parent.\n\nCorrecting the first verifier: 89588b3d (#738) did NOT accept ADR-83. status was 'accepted' at both 89588b3d~1 and 89588b3d; its message explicitly states 'no status changes, no files moved', and its sole ADR-83 edit was fixing the stale 'Files affected' path `src/gates/knowledge.md`→`src/spaces/knowledge.md`. The correct acceptance commit is 4f524fce (#696).\n\nWindow check: 4e593a4c, 4f524fce, 89588b3d, and 0bbfe1cf are all ancestors of b4d775ad (alpha.8 boundary) but not of dcfa5938 (alpha.7), i.e. all within the alpha.8 change window — so the version-level attribution (ADR-83 accepted during the alpha.8 cycle) is correct; only the cited SHA is wrong. Recommended fix: change '(commit 0bbfe1cf)' on line 1308 to '(commit 4f524fce)', not 89588b3d."}
{"final":"PARTIAL","claim":"Line 1288: 'ADR-74 (pinned Obsidian plugin provenance) accepted — repo now ships ... src/.obsidian/plugin-provenance-lock.json ... (commit 0bbfe1cf; ADR-74 diff)'","reasoning":"Independent re-derivation against /home/eranr/memoria-vault/main confirms the first verifier's core evidence, but the doc's underlying design facts are true, so a flat \"FALSE\" on the whole claim overshoots.\n\nCITATION (the defect — CONFIRMED wrong): Commit 0bbfe1cf is a single-parent (non-merge) commit whose subject is \"docs: accept ADR-76 (vault-release spine), ADR-84, ADR-100 (#732)\". `git show --stat` (and `git show -m`) show it touches exactly 4 files — docs/adr/100-…, docs/adr/76-…, docs/adr/84-…, docs/adr/README.md — and contains NO ADR-74 diff at all. The doc's parenthetical \"(commit 0bbfe1cf; ADR-74 diff)\" is a genuine mis-citation. ADR-74's status was actually flipped deferred→accepted in commit ffed6dab (#687, \"docs: clarify ADR implementation mappings\"): its diff on docs/adr/74-pinned-obsidian-plugin-supply-chain.md changes `status: deferred` → `status: accepted` and sets `date_resolved: 2026-06-18`. I confirmed no merge/squash could hide the diff in 0bbfe1cf.\n\nDESIGN SUBSTANCE (TRUE): ADR-74 genuinely was accepted in the alpha.8 window (dcfa5938..b4d775ad) — it is `deferred` at the alpha.7 boundary dcfa5938 and `accepted` at the alpha.8 boundary b4d775ad, via in-window commit ffed6dab. The file src/.obsidian/plugin-provenance-lock.json is real and present in current state (exists at b4d775ad) at exactly that path. So the doc's assertions \"ADR-74 accepted\" and \"repo ships plugin-provenance-lock.json\" are both correct.\n\nTIMING NUANCE: The provenance lock file was created earlier, in c79f659c (\"Implement alpha.6 docs template provenance gate (#649)\"), an ancestor of the alpha.6 boundary — i.e. it predates this cycle. The doc implicitly attributes both the acceptance and the lock-file shipping to 0bbfe1cf; that attribution is wrong on both counts (acceptance is ffed6dab, in-window; lock file is c79f659c, alpha.6).\n\nVERDICT: The flag correctly identifies a real, confirmed defect — the commit citation is wrong and 0bbfe1cf does not touch ADR-74. The first verifier missed no context; its proposed correction (attribute acceptance to ffed6dab; note the lock file predates the cycle via c79f659c) is exactly right. But because the claim's design substance (ADR-74 accepted this cycle; provenance-lock.json ships) is TRUE and only the SHA/timing attribution is FALSE, the accurate verdict is PARTIAL: a confirmed mis-citation inside an otherwise-correct design statement, rather than a wholly false claim."}
{"final":"PARTIAL","claim":"Lines 869 and 929: 'Commander places the 16 QuickAdd command-palette macros'; 'a pytest orchestrator over the 16 command-palette commands'.","reasoning":"Re-derived from the repo and sources. The first verifier's CENTRAL EVIDENCE is false. Its claim \"no source states 16 (the only 16 in test-env.md is 'RTX 4060 Ti 16 GB')\" is wrong: the alpha.4 test-env.md — the exact note the doc's line-929 bullet is summarizing (\"Authored test-env.md\") — says verbatim \"16 command-palette commands (QuickAdd macros; see docs/reference/obsidian-command-palette.md)\" (line 84), \"all 16 commands on Gemma\" (line 199), and \"Wire the Obsidian CLI driver over the 16 commands\" (line 258), plus \"pytest orchestrator\" (lines 61/209/256). The same \"16 command-palette commands (QuickAdd macros)\" framing recurs across the alpha.4/5/8 sources. So LINE 929 is ACTUALLY-CORRECT: it is a faithful report of its cited source note, whose own number is 16. The proposed correction (force \"17\") would make the design-history doc contradict the note it summarizes — that is a worse error, not a fix.\n\nHowever, LINE 869 carries a genuine but DIFFERENT imprecision than the flag alleged. Verified counts: alpha.3 baseline 7581edb1 = 16 palette choices (2 Template + 14 Macro); alpha.4 baseline b1657d92 = 17 palette choices (2 Template + 15 Macro). The 16→17 bump is #513 create-linked-claim (commit c5010254), which landed in-window on 2026-06-14, before test-env.md was authored (0145dc3f, 2026-06-15) — so the shipped alpha.4 checkpoint count is 17, and \"16\" is stale-by-one there. Additionally, Commander (cmdr/data.json) places only 12 (8 leftRibbon + 4 pageHeader), and the reference doc obsidian-command-palette.md says Commander \"mirrors the highest-frequency entries,\" i.e. a subset — not \"the 16\" (all of them). So \"Commander places the 16 ... macros onto toolbars/ribbons\" is loose on both the number (16 vs shipped 17) and coverage (all vs subset of 12).\n\nNet: the flag's blanket \"16 is unsupported / no source states 16\" is substantially wrong — 16 is well-attested design-era terminology and line 929 correctly quotes it — so this is not a clean CONFIRMED-ERROR; but line 869's \"16\" is stale versus the end-of-window shipped state and overstates Commander's coverage, so it is not fully ACTUALLY-CORRECT either. PARTIAL."}
{"final":"CONFIRMED-ERROR","claim":"Line 1259: 'Retired/disposed all carried-forward tmp/ scratch across alpha.4-7 release folders (deletions under docs/releasing/0.1.0-alpha.{4,5,6,7}/tmp/; commits be840217, aa02089f, 2593076e)'","reasoning":"I re-ran the git evidence against /home/eranr/memoria-vault/main and the flag is correct.\n\nCITED COMMITS (all in the alpha.8 window dcfa5938..b4d775ad, verified via merge-base):\n- be840217 \"docs: dispose alpha8 tmp scratch (#723)\": deletes 4 files, EVERY one under docs/releasing/0.1.0-alpha.8/tmp/ (install-a-real-package-alpha5.md, test-env-design-alpha5.md, ui-architecture-design-history-alpha7.md, ui-architecture-future-alpha7.md). Zero deletions under alpha.{4,5,6,7}/tmp/.\n- aa02089f \"docs: prune resolved alpha8 tmp notes (#717)\": deletes 2 files, both under docs/releasing/0.1.0-alpha.8/tmp/ (alpha7-docs-audit-report.md, deferred-adr-implementability-alpha6.md). Zero deletions under alpha.{4,5,6,7}/tmp/.\n- 2593076e \"docs: align ADR issue routing guidance (#715)\": diff-filter=D returns NOTHING and diff-filter=R returns nothing — it deletes/renames zero files, only modifies 8 files (.agents playbooks, AGENTS.md, ADR-65, releasing READMEs). It is not a deletion commit at all.\n\nSo the doc's explicit, checkable assertion — that these three commits produced \"deletions under docs/releasing/0.1.0-alpha.{4,5,6,7}/tmp/\" — is false. The files are named for alpha5/6/7 but they LIVED IN and were deleted FROM docs/releasing/0.1.0-alpha.8/tmp/, and 2593076e is miscited (deletes nothing).\n\nTRIED TO DEFEND: (1) The prose \"carried-forward across alpha.4-7 release folders\" could be read as describing the scratch's subject, but the parenthetical makes a hard path claim (alpha.{4,5,6,7}/tmp/) that does not match the cited commits. (2) The doc's own \"Why\" examples (ui-architecture-future-alpha7.md -> ADR-102/103; install-a-real-package-alpha5.md -> ADR-76; test-env-design-alpha5.md -> ADR-80) are correctly supported by be840217 (it adds ADR-102/103 and modifies ADR-76/80 while deleting those exact files) — but those files are the alpha.8/tmp/ ones, which CONFIRMS the true subject is alpha.8/tmp/, not alpha.4-7/tmp/. So the doc's own rationale undercuts its path claim.\n\nADDED NUANCE (the first verifier slightly understated one point but reached the right conclusion): a genuine disposal under alpha.{4,5,6,7}/tmp/ DID occur in the alpha.8 window — but in an UNCITED commit, 30075fb3 \"Finish alpha7 QA docs and local model overlay (#679)\", which broadly deleted across alpha.4/tmp, alpha.5/tmp, alpha.6/tmp AND alpha.7/tmp (exec-plans, project-starter, ws-0-spikes, gap-analysis, dashboards/*, portals-design, workspaces-design, ui-architecture-alpha7, etc.) — not merely alpha.7/tmp/dashboards/* as the first verifier wrote. This means the doc conflated two distinct events: it described the real alpha.4-7/tmp disposal (30075fb3) but attached the commit SHAs of the separate alpha.8/tmp disposal (be840217, aa02089f) plus a non-deletion commit (2593076e).\n\nCORRECTION: The cited commits disposed docs/releasing/0.1.0-alpha.8/tmp/ scratch, not alpha.{4,5,6,7}/tmp/. If the intended subject is the alpha.8/tmp close-out sweep, cite be840217 and aa02089f (relabel path to alpha.8/tmp/) and drop 2593076e. If the intended subject is the alpha.4-7/tmp carry-forward disposal, the correct commit is 30075fb3 (#679). Either way the sentence as written is false. Verdict: CONFIRMED-ERROR."}
{"final":"PARTIAL","claim":"Line 847: \"Finalized alpha.4 scope in the ExecPlan (§2.2), b1657d92. Deferred out: #527 HTTPS transport, #379/#559 calibration spec + score-* skills, #344 diversity reserve, #412 (ADR-62 harnesses, split to #560/#561), #370 (ADR-38 ratchet — alpha.4 keeps only shadow/report-only; enforcement → #562).\"","reasoning":"The flag's central accusation is FALSE. The split issue numbers #559/#560/#561/#562 are NOT fabricated — they appear verbatim throughout the FINALIZED alpha.4 ExecPlan: §2.2 (sources tmp__exec-plan-alpha.4.md lines 155-158: \"#379 + #559 (calibration spec and score-* map skills), #344 (diversity reserve), #560/#561 (ADR-62 harnesses), and #562 (calibrated/enforcing ADR-38 ratchet)\"), the section-G table (line 111 \"#381 ... score-* moved to #559 behind #379\"; line 140 \"#412 ... split out of alpha.4 to #560/#561\"; line 141 \"#370 ... alpha.4 is shadow/report-only; tuning/enforcement moved to #562\"), and the interfaces/log (lines 273, 383-384, 442-444, 448). The first verifier's \"git grep ... returns zero hits anywhere in the alpha.4 release dir or the exec-plan\" is simply wrong: they inspected only the b1657d92 tree and the b1657d92 commit body, missing (a) the designated sources snapshot, which the task names as ground truth, and (b) git commit ece4c569 (2026-06-16 02:54, PR #570) which introduced exactly these numbers. Thus #412 IS split to #560/#561 and #370 IS narrowed to shadow/report-only with enforcement→#562 in the final plan, and the first verifier's proposed correction (delete #559/#560/#561/#562, revert to \"#379/#381/#344\" + \"#412/#370 deferred whole\") would DELETE real content and reinstate the superseded pre-revalidation snapshot — an error, not a fix.\n\nHOWEVER, the doc's claim is not flawless, which is why this is PARTIAL rather than a clean overturn. Two genuine (different) defects exist:\n1. COMMIT MIS-ATTRIBUTION. The doc cites the split-number §2.2 to \"b1657d92.\" I re-derived §2.2 AT b1657d92 (git show b1657d92:docs/releasing/0.1.0-alpha.4/tmp/exec-plan-alpha.4.md, lines 148-150, \"Last updated: 2026-06-15\") and it reads the WHOLE-deferral: \"#527 (HTTPS transport...); #379 + #381 + #344 ...; #412 (ADR-62 harnesses); #370 (... depends on the deferred calibration discipline (#379))\". No split numbers exist at b1657d92. The splits were introduced by ece4c569 on 2026-06-16, which is AFTER the alpha.4 boundary (b1657d92 is an ancestor of alpha.5=098ca361, so ece4c569 falls in the alpha.5 window). So on the exact SHA cited, the doc's split content is not present; the correct provenance is ece4c569 / the 2026-06-16 revalidation (which the doc's own \"Why\" narrates, but the citation still points at b1657d92).\n2. #527 INCONSISTENCY. The doc lists \"#527 HTTPS transport\" under \"Deferred out.\" That matches only the pre-revalidation b1657d92 state; the FINAL ExecPlan pulls #527 IN (§2.1 section B, line 87: \"Security / transport — in after 2026-06-16 revalidation\"), and the doc's own \"Why\" says Hermes ssl_verify \"unblock[ed] #527.\" So \"#527 deferred out\" contradicts the finalized plan the doc otherwise draws the split numbers from. (Note: the first verifier's proposed correction also keeps #527 in the deferred list, so this is not a point of dispute between them — both are wrong on #527.)\n\nNet: the doc's substantive content (the split issue numbers and the #412/#370 split framing) is CORRECT against the finalized ExecPlan and the flag's \"fabricated\" charge is overturned; but the doc has a real commit-timing/attribution imprecision (splits belong to ece4c569/2026-06-16, not b1657d92) plus the #527 residual — neither of which the first verifier identified. PARTIAL."}
{"final":"CONFIRMED-ERROR","claim":"Line 2012 (and table row line 2058): skill-state.md (136 lines) was 'removed with the Obsidian payload #1124'.","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. skill-state.md existed at a single path at the alpha.14 base 6c2d5b04 (vault-template/system/dashboards/skill-state.md), 136 lines (git show 6c2d5b04:...skill-state.md | wc -l = 136 — the doc's line count is CORRECT). But `git log --diff-filter=D 6c2d5b04..88b7f0f5 -- '*skill-state.md'` returns exactly ONE deleting commit: a3794fb6 \"Remove installed profile runtime surface (#1119)\", whose --name-status shows `D vault-template/system/dashboards/skill-state.md` (136 deletions) plus removal of tests/test_skill_state_dashboard.py. The cited Obsidian payload commit 5a93b142 (#1124 \"Remove shipped Obsidian workspace payload\") does NOT touch skill-state.md at all (it deletes the .obsidian tree, obsidian docs, and system/scripts). Defense of the doc fails on every angle: there is no second skill-state.md anywhere in the 6c2d5b04 tree (ruling out a same-name file deleted in #1124), and #1119's commit body has no Obsidian mention, so even the loose \"grouped with Obsidian removal\" reading is unsupported — it went with the profile-runtime removal. Cross-check confirms the companion fact fleet-health.md→#1122 (cc154cdd \"Remove fleet health dashboard surface\") is correctly attributed; only the skill-state.md→#1124 attribution is false. Verdict CONFIRMED-ERROR: the specific attribution flagged (#1124 / Obsidian payload) is false and must be corrected to #1119 (a3794fb6) at both line 2012 and the table row line 2058; the \"136 lines\" figure is accurate and needs no change."}
{"final":"CONFIRMED-ERROR","claim":"Line 1540: 'the seven old pipeline-ordered tutorials … were deleted and replaced by six (01-orient … 06-close-the-loop … 07-make-it-your-own)'.","reasoning":"Independently re-derived against the alpha.10 boundary 383034b0. `git ls-tree` shows docs/tutorials/ holds SEVEN numbered files at that commit (01-orient, 02-bring-in-your-first-source, 03-build-claims-and-connect-them, 04-draft-a-section-from-your-claims, 05-verify-it-holds, 06-close-the-loop, 07-make-it-your-own, plus README). The window diff 830ecd31..383034b0 (alpha.9→alpha.10) shows exactly 7 deleted and 7 added (name-status: 7 D / 7 A) — a 7→7 replacement, not 7→6. The shipped README states verbatim \"Seven short tutorials, grouped into three sessions\" and its path table enumerates rows 01–07; 07-make-it-your-own is a full \"Session 3 of 3\" tutorial, not an optional appendix. ADR-112 — the shipping design, whose cited commits 00978952 (prototype the destination-first arc) and b361acad (Diátaxis rewrite) both verify as real and fall in this window — lists \"### The arc\" as SEVEN beats: Orient, Capture, Distill & connect, Draft, Verify, Close the loop, Graduate. The word \"six\" traces only to the SUPERSEDED ADR-111 (\"### Spine (6 tutorials, was 7)\"; \"ADR-111's six feature-tutorials including setup\"), which the doc itself says ADR-112 supersedes. Tried to defend the doc: \"six tasks you can hand off\" in the README is a different concept (knowledge-cycle handoffs, not tutorials); 07 is not an appendix; the window is correct. Every defense fails, and the claim's own parenthetical (listing up to 07-make-it-your-own) and its own arc enumeration (orient→capture→distill&connect→draft→verify→close the loop→graduate = 7 beats) internally contradict \"six.\" The flag is correct; \"replaced by six\" should read \"replaced by seven.\""}
{"final":"CONFIRMED-ERROR","claim":"Line 1982 and table row line 2056: the Zotero-specific import surface removed in #1123 (7ad88a84) included regenerate-ai-catalog.md.","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. Three independent facts confirm the flag: (1) `git show --diff-filter=D --name-only 7ad88a84` shows #1123 deletes exactly ONE capability file, capture-zotero-source.md; it never touches regenerate-ai-catalog.md or regenerate-capability-index.md (git show 7ad88a84 -- both paths returns empty). #1123 otherwise edits cli.py/capture.py/worker.py, Zotero docs, and tests. (2) regenerate-ai-catalog.md was RENAMED to regenerate-capability-index.md by #1066 (f0da1a98, `R050 regenerate-ai-catalog.md -> regenerate-capability-index.md`), and `git merge-base --is-ancestor f0da1a98 7ad88a84` returns true — the rename landed BEFORE #1123, so at #1123's tree only regenerate-capability-index.md exists (ls-tree confirms). #1123 could not remove a file that was already renamed away. (3) The file's content at f0da1a98^ reads description \"Regenerate the tracked capability catalog projection\" / \"Render capabilities/ai-catalog.json from checked capability Concepts\", tags [alpha11, projection] — it is the capability-catalog/index projection generator, wholly unrelated to Zotero import. The doc even contradicts itself: line 2024 correctly attributes the regenerate-ai-catalog.md -> regenerate-capability-index.md rename to #1066 f0da1a98. Every defense fails: the cited commit/window is correct but simply doesn't touch the file; there is no alternate file where the fact holds; and this is a factual list-membership assertion, not a paraphrase. The rest of the Zotero-removal list on lines 1982/2056 (CLI --format zotero-export, worker operation, runtime helper, capture-zotero-source.md) is supported by #1123's diff; only regenerate-ai-catalog.md is mis-cited. Proposed correction (drop regenerate-ai-catalog.md from both line 1982 and the line 2056 table row) is correct."}
{"final":"CONFIRMED-ERROR","claim":"\"The type vocabulary collapsed from 27 types to 13\" (line 1629), cited to vault-template/.memoria/schemas/types/*.yaml vs src/.memoria/schemas/types/*.yaml","reasoning":"I independently re-derived both counts against the cited file glob and the release-boundary map. Before side: git ls-tree of src/.memoria/schemas/types/ at the alpha.10 boundary 383034b0 = 25 .yaml files (one type each, no subdirs): alert, candidate, claim, code-note, dataset, eval-task, flag, fleeting, gap, hub, maintenance, organization, paper, pattern, person, project, queue, repository, source, space, thesis, venue, work-prompt, worker-card, worklist-item. After side: git ls-tree of vault-template/.memoria/schemas/types/ at the alpha.11 boundary 1b24453b = 12 .yaml files (digest, hub, mcp, note, operation, organization, person, project, skill, source, venue, workflow), no subdirectories. So the cited authoritative glob yields 25 -> 12, not 27 -> 13.\n\nThe doc's own itemized lists corroborate this and expose a second defect: Added (6: digest,note,operation,mcp,skill,workflow) + Kept (6: hub,organization,person,project,source,venue) = 12, matching the vault-template count exactly. The Removed list has only 18 entries and Kept 6 = 24, one short of the actual 25 because the enumeration omits `repository` (which the doc's own line 1760 admits was collapsed into source.item_type). True removed = 19; 19 + 6 = 25.\n\nI tried hard to defend \"27 -> 13\" and found no support. Grepping all of sources/ for \"27 type / twenty-seven / 27-type\" returns nothing; the alpha.11 design note 0.1.0-alpha.11-design.md contains no \"27\" (only 'Date: 2026-06-27') and no 13-type count. The only real \"13\" in the corpus is the ALPHA.12 SQLite concepts.concept_type CHECK enum (doc line 1780) which lists 13 values by adding the supertype `capability` to the 12 — a different artifact in a different version, not the alpha.11 types/*.yaml vocabulary; that appears to be where the erroneous 13 leaked in. The same wrong count is also propagated at line 1603 (\"the 27-type schema\").\n\nConclusion: the first verifier's flag is correct. Recommended fix: change \"27 types to 13\" to \"25 types to 12\" (equivalently 19 removed + 6 kept -> 6 added + 6 kept), fix the twin phrasing \"27-type schema\" at line 1603, and add the omitted `repository` to the Removed enumeration at line 1629 so it lists 19 removed rather than 18."}
{"final":"CONFIRMED-ERROR","claim":"§1 (line 2082): 'work.yaml has `work_id` required, `citations`/`evidence_set` optional'","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. `git show 5b1ea8bc:vault-template/.memoria/schemas/types/work.yaml` yields required: {type, id, title, tags, links, work_id} and optional: {aliases, archived, description, x}. So `work_id` required is TRUE, but `citations`/`evidence_set` do NOT appear anywhere in work.yaml. The alpha.15 window diff (88b7f0f5..5b1ea8bc) is decisive: work.yaml was created new at PR #1164 (71ec0dc4) with optional = aliases/archived/description/x only, while digest.yaml was DELETED in the same window — and it was digest.yaml (not work.yaml) whose optional block contained `evidence_set: list` and `citations: list`. A grep for citations/evidence_set across every alpha.15 schema file returns nothing. Attempts to defend the doc failed: no later commit in the window re-adds the fields (5b1ea8bc final state = same 4 optionals), they are not folded into `x`/`links`, and there is no schema representation of them. The doc conflated the deleted digest's optional set with the new work type when it wrote 'digest materialization moved onto work'; in reality those two fields were dropped, not migrated. The first verifier's evidence, commit window, and surgical correction (drop the '`citations`/`evidence_set` optional' clause, keep `work_id` required) are all correct — the flag is fully upheld. The `work_id`-required portion being true does not make the flag partial: the flag already isolated and preserved that true part and correctly identified only the citations/evidence_set clause as the defect."}
{"final":"PARTIAL","claim":"Lines 1770 & 1784 of the design-history doc claim the alpha.12 DESIGN specifies an entities/catalog_links graph, field-provenance, enrichment runs, recursive-CTE cascade-rollback, a git-tracked journal-heads.jsonl anchor, and a Turso backup adapter (line 1770), and that 'the design's entities, catalog_links, field-provenance, and enrichment tables did not ship in this range' (line 1784) — i.e., that field-provenance and enrichment are alpha.12 design-only items.","reasoning":"I re-derived ground truth against scratch/releases/0.1.0-alpha.12/ (design, exec-plan, and the whole tree). Four/five of the enumerated design-only items are genuinely present in 0.1.0-alpha.12-design.md, so the doc's overall thesis (design > shipped) holds: entities and catalog_links (line 84), recursive-CTE cascade-rollback (lines 101, 148, 163, 225, 265), the git-tracked .memoria/audit/journal-heads.jsonl anchor (line 173), and the Turso/libSQL backup adapter (lines 19, 92, 110, 165, 178, 213, 234, 257) are all in the design. BUT the two specifically flagged items are not: grep for enrich/enrichment returns ZERO hits across the design, the exec-plan, and the entire releases/0.1.0-alpha.12/ tree; grep for field-provenance / per-field / field provenance returns ZERO hits in the design. Line 84's only provenance construct is a single row-level 'provenance refs' column on the sources table (and 'evidence/run refs' on catalog_links) — that is not a per-field-provenance table/mechanism, and 'run refs' is not an 'enrichment run'. Both concepts are alpha.13 features: 'DOI catalog enrichment' with 'per-field provenance' is the alpha.13 theme (doc lines 1838-1840), and per-field provenance traces to the alpha.9 enrichment ADR (sources/notes/docs-exports/adr-full.md:1999 'the merge is per-field, best-source-wins, with provenance'). No alpha.12 source (design, exec-plan, notes layer, versions/) supports attributing field-provenance or enrichment to the alpha.12 design. The first verifier's flag is correct and its proposed correction is exact: strike 'field-provenance' and 'enrichment runs/tables' from the design-only list on both lines 1770 and 1784; keep entities, catalog_links, recursive-CTE cascade-rollback, journal-heads.jsonl, and Turso. The claim is PARTIAL — its verified backbone is contaminated by two alpha.13 concepts retrojected into the alpha.12 design."}
{"final":"CONFIRMED-ERROR","claim":"Line 1768: alpha.12 was 'driven by ... four disposable pre-implementation spikes whose three FAIL verdicts forced concrete contract decisions.'","reasoning":"Independently re-derived and the flag holds. There are two distinct alpha.12 spike/test artifacts. (A) scratch/releases/0.1.0-alpha.12/preimpl_spikes/run_spikes.py is literally titled \"Pre-Implementation Spikes 1-4\" and defines exactly FOUR spikes — \"1. lifecycle/crash fixture\", \"2. references.bib durability\", \"3. SQLite journal integrity\", \"4. rollback graph fixture\". Every one hardcodes status=\"PASS\" (a FAIL is only reachable via an exception), and the recorded run-20260704T221100Z/results.json shows all four PASS with \"failures\": []. So the four-spike, \"pre-implementation\" set had ZERO failures. (B) alpha12_test_harness.py → alpha12-test-results.md is a SEPARATE artifact with SEVEN results and summary \"PASS: 4, FAIL: 3\"; the three FAILs are \"Persistence authority and crash replay\", \"Catalog recovery from committed artifacts\", and \"Cross-store same-operation citation lifecycle\". The doc's own body (lines 1776, 1788, 1790, 1794, 1796, 1802) cites the exact seven titles from artifact B via \"test-results §FAIL/§PASS\" — not the four run_spikes.py names — so its verdict throughline is entirely built on the seven-result harness. Line 1768 therefore fuses the COUNT from artifact A (\"four ... pre-implementation spikes\") with the FAIL count from artifact B (\"three FAIL verdicts\"). No single source has \"four spikes\" with \"three FAILs\": the four-spike set failed nothing, and the three-FAIL set has seven results, not four. The relative clause \"whose three FAIL verdicts\" grammatically attributes three failures to the four all-PASS spikes, which is false. Tried the strongest defenses — a loose two-artifact reading, or that each number independently traces to a real artifact — but even then the sentence as a unit misrepresents the evidence and is internally contradictory. The first verifier neither overstated nor misattributed anything; the proposed correction (\"seven disposable spike results, 4 PASS / 3 FAIL\" or separating the four all-PASS run_spikes.py spikes from the seven-result harness) is correct. Note the underlying substance (\"three FAILs forced concrete contract decisions\") is itself well-supported, but the specific quantitative pairing flagged is a confirmed error."}
{"final":"ACTUALLY-CORRECT","claim":"Line 1466: the `memory` toolset \"became profile-scoped to `memoria-copi` only (enforced in `policy_hook.py` via `PROFILE_SCOPED_DIRECT_TOOLS = {\"memory\": {\"memoria-copi\"}}`)\" — cited to commit 450f659d, test_policy_gate_completeness.py, and copi config.yaml.","reasoning":"The flag is overturned: the constant is real and exists exactly as the doc describes at the commit the doc actually cites. `git grep PROFILE_SCOPED_DIRECT_TOOLS 450f659d -- src/.memoria/mcp/policy_hook.py` returns line 142: `PROFILE_SCOPED_DIRECT_TOOLS = { \"memory\": frozenset({\"memoria-copi\"}), }`, plus the enforcement block at lines 261-262 inside evaluate_pre() (`if base in PROFILE_SCOPED_DIRECT_TOOLS and profile not in PROFILE_SCOPED_DIRECT_TOOLS[base]`). `git show 450f659d` confirms that commit (#886, \"Close alpha.10 and harden direct history tools\") is precisely what INTRODUCED both the constant and its enforcement — so the cited commit's diff directly supports the claim (fact-check criterion #1 fully met). 450f659d is within the alpha.10 window: 830ecd31 (alpha.9) is its ancestor and it is an ancestor of 383034b0 (alpha.10 boundary). The doc's rendering `{\"memory\": {\"memoria-copi\"}}` is a faithful paraphrase of the actual `{\"memory\": frozenset({\"memoria-copi\"})}` (set-literal shorthand for the frozenset). The first verifier searched only the release boundary 383034b0, where a LATER in-window commit — 0e89d023 (#910, \"Resolve audit refactor findings\") — had already removed the constant and replaced it with the tool-registry allowlist (_allowed_registry_tools/_tool_candidates/_registry_block). That is why the boundary grep came up empty and why the verifier saw the registry mechanism instead. Both mechanisms are real, but they are sequential states within the window; the doc names the one that existed at the commit it cites. The verifier's central assertion \"No such constant exists\" is factually false — it checked the wrong commit. Everything else the doc asserts also holds: 450f659d exists with the stated subject, and the first verifier already conceded the substantive scoping (memory allowed for memoria-copi, blocked for librarian/writer, asserted in test_policy_gate_completeness.py; copi config memory write_approval:true, provider:'') is true."}
{"final":"ACTUALLY-CORRECT","claim":"Line 1784 (alpha.12 section): the alpha.12 state.py schema is \"initialized by an inlined `executescript` schema (`SCHEMA_VERSION` not yet externalized — the standalone `schema.sql` file arrives in a later release).\"","reasoning":"Independently re-derived against the repo. In-window (alpha.12, 7765040d): state.py `_init` calls `conn.executescript(\"\"\"<literal SQL>\"\"\")`, so the schema is genuinely inlined; there are zero `.sql` files in the tree; and there is no SCHEMA_VERSION constant in state.py. All three in-window facts the doc rests on are true. The disputed forward-looking parenthetical is also SUPPORTED, and this is exactly what the first verifier missed. Their grep used the literal token \"schema.sql\", whose regex does not match \"schema_v1.sql\", so they overlooked that (a) at alpha.15 (5b1ea8bc — a later release, still inside the doc's scope) state.py introduces `SCHEMA_VERSION = 3` and loads the DDL from a standalone external file `src/memoria_vault/runtime/schema_v1.sql` via `_schema_sql()`; this externalization landed at commit be2ea1b9 \"alpha15: load sqlite schema from ddl\", confirmed inside the alpha.14..alpha.15 window; and (b) that same standalone file is renamed to exactly `src/memoria_vault/runtime/schema.sql` at commit 7dff27d7, present at HEAD (post-alpha.15, i.e. an even later release). So both halves of the parenthetical hold: SCHEMA_VERSION and the externalized schema file do not exist at alpha.12 and both genuinely arrive in a later release (they in fact arrive together at alpha.15), and a file literally named `schema.sql` does exist later. The first verifier's decisive evidence (\"no schema.sql through alpha.15 ⇒ 'arrives in a later release' unverified\") is factually wrong. The only residual imperfection is minor wording: rendering \"`SCHEMA_VERSION`\" in code font at the alpha.12 point could be read as implying the constant already exists there, when it does not appear until alpha.15 — but the qualifier \"not yet externalized … arrives in a later release\" makes the forward-looking intent unambiguous and the identifier is real later. That is a phrasing nit, not a false claim. The proposed correction would delete accurate, verifiable forward-looking content, so the flag should be overturned; the doc's claim stands."}
{"final":"CONFIRMED-ERROR","claim":"§7 (line 2172) and removals table (line 2226): '`calibration.yaml` **deleted**' / 'The static calibration.yaml (entity_resolution/classify/clustering/hybrid_scores floors) was retired'","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. (1) `git ls-tree -r 5b1ea8bc` (alpha.15) shows vault-template/.memoria/schemas/calibration.yaml STILL PRESENT — identical presence at alpha.14 (88b7f0f5); `git diff --diff-filter=D 88b7f0f5 5b1ea8bc` lists no deleted calibration file. It is the only file named calibration.yaml in the tree at either boundary, so there is no alternate file the doc could mean. (2) `git show 5b1ea8bc:…/calibration.yaml` still contains top-level sections entity_resolution:, classify:, and hybrid_scores: — three of the four enumerated 'floors' survive at release, so 'was retired' is false. (3) The only in-window commit touching the YAML is 97a819af (PR #1204 'docs: remove stale clustering config surface'); its diff removes ONLY the `clustering:` block. The other in-window match, 94255ec7 (#1231), edited docs/reference/calibration.md (removed the generated reference table), not the YAML. (4) The doc attributes the deletion to ADR-129, but docs/adr/129-layered-machine-judgment.md says calibration.yaml's triage-scoring discipline (ADR-66) 'survive[s] and [is] restated here' against the alpha.15 engine — it never states the file was deleted. Every defense angle (alternate file, wrong window, different file, paraphrase) fails: no deletion exists anywhere in the window, and the cited PR + ADR support only the clustering-surface removal, which the doc itself separately acknowledges. The doc is internally contradictory (says 'deleted' while its own citation removed just one section). The load-bearing verbs 'deleted' and 'was retired' are false; the first verifier's flag and proposed correction are correct."}
{"final":"CONFIRMED-ERROR","claim":"§5 (line 2138): enrichment merge landed via 'PR-F #1173, `ingest_paper.py` +114'","reasoning":"Independently re-derived against /home/eranr/memoria-vault/main. PR #1173 = commit 82d0a6a9 (\"Implement alpha15 enrichment merge gates (#1173)\"), the sole commit referencing #1173, correctly inside the alpha.15 window (88b7f0f5..5b1ea8bc). So the PR number and its enrichment-merge topic are right. But the \"`ingest_paper.py` +114\" fragment is false on both file and count: `git show 82d0a6a9 --numstat` gives ingest_paper.py = +1/-1 (not +114); across the entire alpha.15 window ingest_paper.py is +12/-102 (net -90). `awk '$1==114'` finds NO file with a +114 insertion count in either PR #1173 or the whole window, so +114 is not a per-PR figure, a whole-window net, nor a multi-commit total. The enrichment merge logic actually landed in runtime/enrichment.py at +133/-13 in PR #1173 (+159/-22 across the window); the commit body (\"Replace metadata_status with provider_coverage…\") and git grep at the tip (5 provider_coverage/degraded/union hits in enrichment.py vs. 1 incidental hit in ingest_paper.py) confirm enrichment.py is the merge file. I actively tried to rescue the doc via alternate commit windows/multi-commit PR span/net-line interpretations and none support +114 on ingest_paper.py. The first verifier's evidence and proposed correction (runtime/enrichment.py ~+133, not ingest_paper.py +1) are both accurate. Note the remainder of the bullet is fine — the three cited tables field_provenance/enrichment_runs/provider_payloads all exist at the alpha.15 tip — but the flagged fragment is a genuine wrong-file-and-wrong-count error."}
{"final":"ACTUALLY-CORRECT","claim":"PR-number citations #1158/#1159 (§11 line 2212, ADR consolidation) and PR-A #1160/#1161/#1162 (§2 lines 2096/2098, §6 line 2152) in verify:alpha.15.","reasoning":"I re-derived ground truth against /home/eranr/memoria-vault/main. The first verifier's observations about the COMMITS are accurate: the five earliest alpha.15-window commits (1a8664da, a3b3f1f4, be2ea1b9, afa83639, d502ac20) are real, in the 88b7f0f5..5b1ea8bc window, correctly attributed by SHA, and — uniquely among the 78 window commits (only 5 of 78) — carry NO '(#NNNN)' suffix in their subjects or bodies; the first PR-numbered commit is c1d42690=#1163. That is why the verifier's subject/body grep returned nothing. But grepping commit subjects is the WRONG test for whether a GitHub PR exists. Querying the live GitHub API via `gh pr view` against origin (git@github.com:eranroseman/memoria-vault.git) shows PRs #1158–#1162 are all REAL and MERGED, and `gh pr view N --json mergeCommit` returns merge-commit OIDs matching the doc's co-cited SHAs EXACTLY: #1158→1a8664da (consolidate alpha15 architecture ADRs = §11 six-ADR consolidation), #1159→a3b3f1f4 (document subsystem ADR default = §11 README-directs-to-subsystem-ADRs), #1160→be2ea1b9 (load sqlite schema from ddl = §2 schema_v1.sql), #1161→afa83639 (route console script through typer = §2 typer entrypoint), #1162→d502ac20 (record retrieval substrate verdict = §6 qmd/retrieval). Every PR number maps to precisely the SHA and the design fact the doc attributes to it, and the numbers slot into the #1157→#1163 gap in exact chronological order. The doc author had accurate ground truth; the citations are correct. The proposed correction would delete correct, independently verifiable PR references — a regression. The flag is overturned."}
{"final":"PARTIAL","claim":"Line 847 'Why': the 2026-06-16 revalidation against live Hermes docs changed the facts — Hermes now documents ssl_verify for HTTP/SSE MCP (unblocking #527) and native Windows support (accepting ADR-64) — so items previously deferred on missing-capability grounds became buildable, while calibration/enforcement work was split out to keep alpha.4 \"free of blocked/deferred omnibus scope.\"","reasoning":"The first verifier fact-checked against the wrong artifact and their central charge is overturned, but line 847 does contain a genuine (different) defect.\n\nWHY THE FLAG IS WRONG: There are two states of the alpha.4 exec-plan. (a) The GIT commit b1657d92 (Mon Jun 15 2026), docs/releasing/0.1.0-alpha.4/tmp/exec-plan-alpha.4.md, \"Last updated 2026-06-15\" — §2.1 B line 80 defers #527 (\"keep ADR-31's documented loopback-HTTP residual ... not a Hermes-only blocker\"), §2.2 dated 2026-06-15, no #559-562, no ssl_verify, no revalidation. This is the ONLY file the first verifier searched, so their observations are true of that file. (b) The SOURCES version at /home/eranr/memoria-vault/scratch/design-history/sources/versions/0.1.0-alpha.4/tmp__exec-plan-alpha.4.md — a LATER state, \"Last updated 2026-06-16\", which the first verifier never opened. It contains the flagged narrative near-verbatim: §2.1 B line 83 \"Security / transport — in after 2026-06-16 revalidation\"; line 87 \"#527 ... implement Hermes ssl_verify with the plugin's exported PEM cert/CA bundle\"; execution log lines 379-384 \"2026-06-16 — Revalidation pass: live Hermes MCP docs document ssl_verify for HTTP/SSE MCP servers, so #527 is in scope ... Split #381->#559, #412->#560/#561, #370->#562 to keep alpha.4 free of blocked/deferred omnibus scope\"; §9 line 442 \"The 2026-06-16 revalidation pulled #414/#296 and #527 into implementation\"; §10 line 458 \"#527 uses Hermes ssl_verify with the plugin's exported PEM cert/CA bundle.\" Independently corroborated by ADR-64's cadence review (adr-full.md): \"Cadence review (2026-06-16, v0.1.0-alpha.4): accepted. The live Hermes Windows Native guide documents native Windows support...\", and by the actual outcome shipping in alpha.5/alpha.6 (doc lines 1003, 1083, HTTPS + ssl_verify). So the narrative is NOT fabricated, and the flag's proposed correction (\"#527 stayed deferred as an ADR-31 residual, drop the narrative entirely\") is itself FALSE — #527 was explicitly un-deferred and HTTPS/ssl_verify shipped. The three pieces of the first verifier's evidence (no 2026-06-16 date; no revalidation event; zero ssl_verify) are all present in the sources file. Flag overturned.\n\nWHY NOT FULLY ACTUALLY-CORRECT: The doc misattributes/mis-scopes the event. It pins the whole item to commit b1657d92 (§2.2), but the revalidation is a 2026-06-16 event that postdates that Jun-15 commit and is NOT in it (b1657d92's §2.2 defers #527). Worse, line 847's own \"Deferred out: #527\" bullet flatly contradicts its \"Why: ...unblocking #527\" — the doc merged the Jun-15 deferred \"What\" with the Jun-16 unblocked \"Why\", producing an internal contradiction. This is a real error, but the OPPOSITE of the one flagged: the correct fix is to drop the stale \"#527 deferred-out\" line item (and, ideally, date/attribute the revalidation to the post-boundary 2026-06-16 state), NOT to delete the revalidation/ssl_verify narrative. The split-issue detail on line 847 (#559, #560/#561, #562) is itself drawn correctly from the 2026-06-16 sources version, confirming the doc was summarizing that later state. Net: the flagged \"Why\" narrative is true and well-sourced (flag refuted), while the line's #527-deferred-vs-unblocked self-contradiction is a genuine but distinct defect — hence PARTIAL."}
```
