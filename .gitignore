# Python bytecode (docs-doctor + profile scripts)
__pycache__/
*.pyc

# Vault-internal secrets and per-machine files are gitignored in vault/.gitignore
# (alongside the existing REST-API / workspace / .env entries).
