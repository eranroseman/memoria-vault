# Memoria beta.1 — folded scope consolidation

Date: 2026-07-12. Status: **draft for owner approval** (nothing filed or built from it yet).

## Why this document exists

The `docs/superpowers/` corpus (a governing requirements doc, a design-of-record,
two mechanism analyses, decision ledgers, an empirical-use plan, and a stack of
2026-07-09/10 audits + a tiered roadmap) carries a large amount of invested
design thinking that was **never implemented**. This document **folds all of it
into one place** so nothing is lost, deduplicates the ~250 raw units the corpus
describes, marks each against what alpha.20 already ships, and assigns each a
release.

### The rule (owner-set)

> Everything in the corpus is in scope and gets **implemented**, not archived.
> **Default is beta.1.** A unit moves to **beta.2 only when there is a proven
> blocker** — after analysis it is genuinely impossible to land in beta.1. A
> deferred unit is never dropped: it carries a named blocker, its beta.1
> *precursor* (the substrate/instrumentation we still build now), and a home in
> a beta.2 spec.

So beta.2 is the rare, justified exception. Where a capability is data- or
security-gated, we split it: the **substrate + instrumentation + shadow mode**
is beta.1; only the **gated activation/decision** defers.

### What got folded (source corpus)

| Role | Files |
| --- | --- |
| Governing | `specs/0.1.0-beta.1-requirements.md` (what must be true) |
| Design-of-record | `specs/0.1.0-beta.1-design.md` (how) |
| Mechanism analyses | `specs/data-structure-analysis.md`, `specs/query-mechanism-analysis.md` |
| Decision ledgers | `specs/0.1.0-beta.1-decisions.md` (canonical), `specs/0.1.0-alpha.17-decisions.md` |
| Empirical plan | `specs/0.1.0-beta.1-empirical-use-action-plan.md` (v2, canonical) |
| Feeders (audits) | `plans/architecture-review.md`, `plans/promise-audit.md`, `plans/schema-analysis.md`, `plans/blind-spots-review.md` |
| Roadmap | `plans/roadmap.md` (Tier 0→4) |
| Notes/doctrine | `plans/okf-note.md`, `plans/user-workflow.md`, `plans/autoresearch-note.md`, `plans/insights.md`, `product-statement.md` |

Excluded as already-done process/harness records: `plans/superpowers-alignment-plan.md`,
`plans/harness-goal-audit.md`. Duplicate/junk files handled in §7.

---

## 1. The alpha.20 baseline reframes the work

The requirements + design were written **2026-07-05 against alpha.16**; the code
is now **alpha.20**, and four releases shipped a great deal the design assumes it
must build. A code baseline pass confirms **much of the substrate already
exists** — beta.1 is closer than the corpus implies.

**Already built (do not rebuild):** the full writing pipeline (argument-analyze →
canvas → slice → draft → promote), verify + export-refusal gate, `evidence_sets`
(marker-driven, ~alpha.17), `passages` + FTS5 + BM25 + RRF fusion, a
code-execution warrant substrate (`code_artifacts`/`code_runs`), Guard-brokered
egress, the checked-file read barrier, single-writer enforcement (workspace
lock + one trusted-writer path), the seeded-error verdict harness, `memoria init`
scaffolding, and the append-only hash-chained `event_log` (write side).

**The real gaps (narrower than the corpus suggests):**

- `concept_edges` exists but is a **permanently empty stub** (`indexing.py`
  returns `[]`; wiped on every reindex) — the designed graph join point is dead.
- **No forward DB-migration path** (`state._init` hard-fails on any
  `user_version` except 0/8) — blocks every schema change.
- **No journal-chain verifier** (chain is written, never validated).
- **sqlite-vec / embeddings are a stub** (16-dim hash test-embedder; dense
  retrieval non-functional for real content).
- **No cost ledger**, **no feedback instrumentation** module at all.
- "Wired-at-edges-dead-in-middle" bugs: `list --type work` and `note --mode work`
  are advertised but unreachable; `_emit` prints `ok` on FAILED ops.
- Provenance `actor` is split (`operation_requests` defaults `'pi'` with no
  CHECK; `derivations` CHECK forbids `'agent'`); context threading reaches only
  ~3 of ~40 operations, so several ops hardcode `actor:"pi"`.

Legend used below — status: ✅ built · ◑ partial · ○ missing/stub. Release:
**B1** = beta.1 · **B2** = beta.2 (deferred, see §4).

### Owner corrections (2026-07-12) — applied

1. **Blast-radius propagation fires on _any_ knowledge-base change or addition**
   (new/edited claim, new/changed edge, retraction), not only a "decided-wrong"
   disposition — that is one trigger among many. (G5)
2. **Derive-on-write is primary; derive-on-read is the fallback.** Write-time
   derivation keeps the KB always-current and gives the user immediate feedback;
   reads can come weeks or months later, and even a few hours' staleness can
   matter. Read-time derivation covers only what write-time did not. (G5, S1)
3. **Prior ADRs, designs, and implementations are reference only** — never a
   justification or evidence. History is a teacher, not a prison; the
   "reverses ADR-N" notes carried from the feeders are context, not blockers. (§6)
4. **Living design-history replaces ADRs as the _history-preserving_ mechanism**
   — not a "decide once, never change" lock. The decisions in §6 are honored but
   revisable on merit. (§6)
5. **beta.1 includes the coding SRD document and excludes code execution.** (C1, §4)
6. **OKF export = copy-paste of the bundle folder; OKF import = the normal
   source-import path** (permissive, re-enters as unchecked). **Merging two
   Memoria bundles is an exception and is out of beta.1** (beta.2). (K1, §4)
7. **"Zones are states, not places" is a foundational axiom that predates OKF**
   — not an OKF-derived rule. (X1 / foundations)

### Re-verification addendum (fuller corpus, 2026-07-12)

The first fold read a copy-in-**shortened** `roadmap.md`/`user-workflow.md`/`okf-note.md`. Re-run against the restored fuller committed versions, these units/decisions fold in (nothing already folded changed materially; the shortened versions held no units the fuller ones lack):

**New/expanded buildable units:**

- **Vault `AGENTS.md` generated projection** [→K1, beta.1] — `memoria init` writes and upgrades regenerate a thin, engine-generated `AGENTS.md` at the vault root (read-contract projection, the `bibliography.bib` pattern, never PI-edited); covers plugin-less agents and detached bundles with a "how to read me safely" note.
- **Seeded-config lifecycle — two classes** [→K1/K4, beta.1] — every seeded file declares a class: *view preferences* (`.base`, `graph.json`, `types.json` — seeded once, then PI-owned like `steering.md`; upgrades must not clobber) vs *data projections* (`bibliography.bib`, canvases, vault `AGENTS.md` — regenerated always, never PI-edited).
- **Surface program — three rings** [→U3, beta.1; the native ring is cheap-while-empty seed work → lands early] — Ring 1 *vault as UI*: seeded `.base` views (catalog/claims/questions/inbox via `projection: attention` frontmatter → native Bases cards), wikilink-wrapped typed-link targets, `graph`+`properties` core plugins + seeded `graph.json`/`types.json`, CSS per type home; Ring 2 *canvas spatial voice* (below); Ring 3 *plugin as `view-spec.v1` renderer* — engine composes layouts from the closed block catalog, never emits components.
- **Canvas program** [→W1/U3, beta.1] — Toulmin-shaped layout (colors carry *role*, never verdict), layout-preserving regeneration (stable `n-<sha>` node ids keep the PI's x/y; grid-place only new nodes), blast-radius canvas (`impact-<claim>.canvas`), question text nodes adjacent to their claim; includes the `argument.canvas` projection-drift coverage fix (#1351).
- **Software products as works + version-as-required-qualifier** [→O2/S1, beta.1] — a `software` work is product identity, hub-like, no single fulltext; its evidence surface is captured artifacts edged via `work_graph_edges` + first-party usage notes; **version is a required qualifier on claims about software**; drift detection flags divergence from a claim's pinned version.
- **Model selection rides the eval harness** [→E1, beta.1] — the model is an instrument variable evaluated like prompts: pin model IDs never aliases, choose per call-site via the operation manifest, shadow-first promotion against the call-site gold set on structural metrics (never truth-agreement), incumbent stays until beaten.
- **Warrant-vs-grounds terminology collision** [→G4, beta.1] — the beta.1 design (§1.4) names a typed evidence-set a "warrant," but under Toulmin that is *grounds*; warrant is the inference license. Rename one before the six-role vocabulary ships (resolve in the warrant brainstorm).

**Corrections:** F2's goal is *one authoritative trust-read path* within the multi-log plane design (per-machine JSONL kept sync-ready + the git-tracked `journal-head` anchor), **not** a "single authoritative journal." The roadmap does have a **Tier −1** (record-sweep absorption); its gates — disposition-telemetry-before-import, the seeded-error license gate, bulk-admission flood mechanics — are already folded into I1/X1/O2. Fulltext v2 (K2) is cheaper than feared: `work_id#^pNNNN` already resolves via `passages.anchor` (design §12.1), so retirement is `folders.yaml`/`CONCEPT_HOMES` + index code, evidence-gated on the empirical plan's fulltext-consultation + staleness rows.

---

## 2. Work packages — beta.1 (grouped by sequencing tier)

Every corpus unit appears under exactly one package (that is the fold). Packages
are the milestone-trackable sub-projects; each gets its own spec → plan when we
reach it.

### Foundation — "make the delivered machinery true & visible" (roadmap Tier 0) · B1

Mostly repairs to already-shipped code; unblocks trust before anything is built on top.

- **F1 · Provenance & actor integrity** ◑ — `actor-vocabulary-unification` (one enum + CHECK on both tables, drop `DEFAULT 'pi'`); `operation-context-threading` (build context once in the worker, consume in `trusted_writer`; kill ~20 leaf kwargs and the `actor:"pi"` hardcodes); `reject-marker-trace` (record an integrity event when a `%%ev%%` marker is stripped on reject). *Load-bearing: falsified provenance is the design's worst defect class.*
- **F2 · Journal trust** ◑ — `journal-chain-verifier` (add the missing verify pass; make `event_log` the substrate trust paths read, or reconcile with JSONL each sweep; keep the git-tracked `journal-head` anchor); `event_log-indexes`.
- **F3 · Durability of grounds** ◑ — `blobs-backup-restore` (stop gitignoring `.memoria/blobs/` as "regenerable" — back up as data; operational ledgers likewise); `durable-writes` (temp-write + fsync + atomic replace).
- **F4 · Surface honesty** ◑ — `emit-failure-honesty` (`_emit` stop printing `ok`/`True` on failure); `catalog-enumeration` (real `list --type work` / `works.list`); `note-mode-work-creatable` (CLI `--mode work`); `dead-knob-removal` (`calibration.yaml`, `gated:`); `doc-code-contradiction-fixes`; `enforcement-claim-corrections`; `tutorial-fixes` (01/02/04/05).

### Graph substrate (roadmap Tier 1) · B1  *(warrant-node reification → B2)*

- **G1 · Migration machinery** ○ — `migration-machinery` (minimal ALTER path so `state._init` stops hard-failing; numbered migrations rule). *Blocks every G/schema package — build first.*
- **G2 · Single edge module + live concept_edges** ◑ — `single-edge-module` (one owner of the relation roster + all edge parsing, replacing 5 substrates/4 parsers/3 rosters); `concept_edges-fill-and-persist` (fix the stub, stop wiping on reindex); `concept_edges-reshape` (add `edge_id` + attribute columns); `catalog-sources-bridge` (claim→work edges resolve); `reverse-traversal-indexes` (`target_concept_id`, `target_id`); `links-mirror`; `tension-edge-primitive` / `tension-relation-write-path`.
- **G3 · Stable identity** ○ — `schema-identity-ULID-keying` (ULID internal, path OKF-facing, rename tracking, real FKs); `work-id-rename` (`source_id`→`work_id` sweep); rename map (`work_graph_edges.relation source→published_in`, `journal_events→event_log`, `source_type→item_type`).
- **G4 · Six-role argument graph (roles carried by relations)** ◑ — `toulmin-six-role-graph` baseline (earn-each-type; warrant/backing/qualifier/rebuttal via typed relations); `warrant-ontology-decision` (standing rec: hybrid, nodes-when-explicit — the *node reification* itself defers, see §4); `unstated-warrant-finding-fix` (fire only on genuine absence, not `supports==0`).
- **G5 · Typed propagation / blast-radius** ◑ — blast-radius fires on **any** KB change or addition (new/edited claim, new/changed edge, retraction), not only "decided-wrong"; **primary path is derive-and-propagate on write** (KB always-current, immediate user feedback), with derive-on-read as fallback. Re-confirmation of impacted nodes may still be impact-ranked, but consequences are *marked* at write time, not deferred to read. Units: `propagation-redesign` (traverse grounding edges, not just the derivation DAG; typed consequences: grounds-lost / warrant-lost / qualifier-regression / rebuttal-strengthened); `propagation-origin-blindness` (remove origin branches in `integrity.py:915-925/999-1003`; epistemic consequences origin-blind, write authority origin-gated); `claim-disposition-flow` (one trigger — "claim decided wrong" → blast-radius report in inbox); `structural_impact-on-substrate` (stop reading quarantined notes / raw `write_text`; wire the already-built engine).

### Concept model & schema hygiene (data-structure-analysis) · B1

- **S1 · Type roster + mode collapse** ◑ — `mode-collapse` (6→4 real modes); `fold-source-note` (delete `source-note`/`sources/`, fold into `note mode:work`); `delete-work-type` (markdown `work`); `concept-type-roster` (15→10); `certainty-hypothesized`; `required-when-grammar`; `item-type-rename`; `todo-field`; `concept-type-single-source` (derive DB CHECK from registry); `close-frontmatter-validation`; `prune-dead-schema`; `delete-survival-copies`; `delete-dead-migration`.
- **S2 · Real grounding detection** ◑ — `real-NLI-grounding-detection` (replace the lexical-negation stand-in; grounding-detection only, never truth scoring — axiom 1).

### Retrieval substrate (design §12 + query-mechanism-analysis) · B1  *(dense default → B2)*

- **R1 · Passages + freshness** ◑ — `passages-table` ✅, `passages-fts` ✅, `file-index-state` ✅, `check-status-cascade` (verdict writes update `passages` in-transaction), `mtime-lazy-reindex`, `incremental-indexing` (replace O(vault) delete-reinsert; also stops wiping `concept_edges`), `passages-mode-mirror`, `source-span-anchor` (real page numbers from PDF extraction), `span-ref-resolution` (`work_id#^pNNNN` via passages), `db-mirror` / `work-aspect` passages.
- **R2 · Modes + fusion + shapes** ◑ — `lexical-mode` ✅ (BM25), `structural-mode`, `graph-sql-primitives` (recursive CTE, co-citation, coupling, centrality), `rrf-fusion` ✅, `filter-before-rank`, `reranking` (CPU cross-encoder, per-group), `grounded-synthesis` (PI-invoked, every sentence carries a resolvable `passage_id`), `check-gate-ride-through`, `gap-visibility-primitive`, `cross-topic-juxtaposition`, `ask-retrieval-trace`, `search-honesty` (denominators, honest-empty), `shape1-targeted-lookup`, `shape2-topic-surfacing`, `anchor-locator-contract`.
- **R3 · Dense substrate (built, shadow-gated)** ○ — `real-embeddings` (replace hash test-embedder; `passage_vec` already carries `embedding_model_id`/`vector_dim`), `passages-vec`, `semantic-mode`, `dual-level-expansion`, `retrieval-fixture-preregistration` (impl-start check). *Dense/graph **default activation** is B2 — BM25 stays default until the pre-registered spike beats it. The substrate + fixture harness is B1.*

### Writing module — the WRITE half (design §1) · B1

- **W1 · Canvas → outline → draft** ✅◑ — `canvas` ✅ + `canvas-candidate-surfacing`, `outline-derivation` ✅, `draft-composition` ✅, `argument-graph-consumption`, `coherent-slice-expansion`, `deterministic-numbers-citations`, `warrant-evidence-set`, `abstain-marker`.
- **W2 · Draft write-back** ◑ — `extract-to-note` (PI-gated), `transclusion-backlink`, `seedling-maturity`, `squeeze-point-trigger`, `promote-draft-passage` ✅, `close-the-loop` (drafting gaps → gap analysis), `close-the-circle-writeback` (deliverable → catalog as a work).

### Coding-SRD — the SRD *document* (design §2) · B1  *(code execution → B2)*

- **C1 · SRD generation + contract** ◑ — `srd-generation` (`projects/<p>/srd.md` from the checked slice), `srd-contract` (problem/goal, requirements, non-requirements, acceptance criteria, risks), `srd-traceability` (source-span evidence or `evidence-incomplete`), `srd-contract-freeze` (freeze blocker), `srd-{traceability,acceptance,scope}-checks`. *Code execution / executable warrants are B2 (§4); the code substrate is already built.*

### Verification / export (design §4) · B1

- **V1 · Checks + refusal** ✅◑ — `citation-number-checks`, `export-refusal` ✅, `abstain-routing`, `graph-structural-integrity`, `verification-checks` (add negated-claim risk category), `non-draft-export-gate` (make it enforced, not opt-in behind `--ready-only`), `sparse-triage`.
- **V2 · Evidence-set contract + review UI** ◑ — `evidence-set-contract` ✅ (table) + `ev-marker-syntax` ✅, `evidence-set-review-ui` ○ (freeze blocker — PI surface for multi-hop/implicit sets + SRD gaps), `honesty-card-rows`, `evidence-first-render`, `independence-first-review`, `export-target-choice` (freeze blocker).
- **V3 · Provenance labels** ◑ — `provenance-labels` (human/machine/mc/contradicted/unchecked/tainted/stale; no label = true), `taint-monotone`, `stale-label`.

### Onboarding / seed corpus (design §5) · B1  *(1000-scale load → B2)*

- **O1 · Onboarding + seed** ○ — `onboarding-wizard`, `time-to-first-answer` (≤30 min), `real-seed-corpus` (~8 real sources), `seed-corpus-licensing` (freeze blocker **and** impl-start check — PMC Commercial-Use / arXiv CC BY·BY-SA·CC0), `steering-md-authoring`, `low-burden-implicit-feedback`, `open-source-affordance`.
- **O2 · Import pipeline** ◑ — `zotero-bulk-import` (generic BibTeX/CSL; admit to catalog, none to knowledge), `per-type-ingestion-adapters` (paper/dataset/repo/web-page/report), `textual-layer-derivation`, `bulk-admission-mode` (batch worklists, quiet loudness). *Staged 10→100 is B1; **1000-scale `seed-corpus-load` + acceptance test is B2** — needs disposition telemetry first (non-backfillable) and the real corpus.*

### Feedback / instrumentation (design §6) · B1 — **highest sequencing priority**

Non-backfillable: every day without it is permanently lost calibration baseline, and it must precede all ingestion.

- **I1 · Event plumbing + dispositions** ○ — `empirical-event-plumbing` (verify/extend `empirical_event.v1`), `disposition-capture`/`disposition-events` (accept/reject/edit/defer/override, true actor, not a rating), `disposition-telemetry`, `usage-load-instrumentation`, `staleness-instrumentation`, `attention-loudness-policy`, `loudness-taxonomy-batch-worklists`, `complementarity-calibration`, `payoff-attribution`, `honest-dashboard` (raw counts, never one "health" score), `shadow-first-discipline` (`production_enabled:false` by default), `throughput-fallback`, `diversity-channel` (measure coverage as its own objective), `three-orthogonal-signals`, `human-back-pressure` (WIP caps + consuming ritual), `stakes-based-gating`, `empirical-decision-plumbing` (route each alpha.20 deferral through pre-registered decision rules), `wire-orphan-operations` (`worklists`, `hub_handoff`, `session_summary`), `digestion-pressure`.

### LLM-eval / runtime modes (design §6, §11.7) · B1

- **E1 · Call-site ledger + gates** ◑ — `call-site-ledger` (stable `call_site_id` + versioned variables), `runtime-mode-boundary` (production/test as a write boundary), `llm-profile-selection` (test = isolated workspace + local/cheap-remote), `frozen-eval-promotion` (shadow-only until frozen eval beats baseline; impl-start preregistration), `eval-run-recording`, `grading-hierarchy` (deterministic → PI → LLM-judge as calibrated diagnostic only), `runner-resolution-clarify` (which knob enables live mode), `fenced-one-shot-operations` ✅, `deterministic-first-class-test`, `cost-discipline` / `ledger-cost-authority` ○ (token ceilings, price tables, circuit breakers, run attribution).

### OKF / storage / consistency (okf-note + design §12) · B1  *(fulltext-v2 owner-conflict, see §6)*

- **K1 · OKF conformance + export/import** ◑ — `okf-conformance` (every non-reserved `.md` has parseable frontmatter + non-empty `type`), `add-type-labels`, `export-unit` (**export = copy the bundle folder** = vault minus `.memoria/`, no transformation), `okf-import` (= the **normal source-import path**; a foreign bundle re-enters as unchecked, no special merge logic), `progressive-index`, `okf-markdown-links`, `nested-project-bundle` + `detachability-detector` (permanent knowledge never links into `projects/`), `move-machine-files` (templates/eval/journal → `.memoria/`), `bundle-root-rename`, `okf-conformance-sweep`, `okf-producer-profile`, `bib-work-id` + `bibliography-bib-projection` + `bib-portable-companion`. *(Merging two Memoria bundles → B2, §4.)*
- **K2 · Fulltext-v2** ◑ — `fulltext-v2-retirement` (retire `fulltexts/` as a bundle root; blob + catalog only; PDF is the human read surface), `item-6-migration-batch`, `fulltext-schema` (interim), `anchor-locator-contract`, `evidence-marker-migration`. **Owner conflict:** ratified `fulltexts/` plural (2026-07-07) vs later "no fulltext files" (2026-07-09) — §6.
- **K3 · Crash consistency + backup/restore** ◑ — `crash-consistency` + `crash-boundary-tests`, `intent-record`, `recovery-reconciliation`, `human-edit-boundary`, `consumable-barrier`, `materialization-saga`, `acid-trust-plane` ✅, `fail-closed-read-barrier` ✅, `single-authoritative-event-log`, `cross-store-crash-atomicity`, `restore-matrix`.
- **K4 · Config / secrets / drift** ◑ — `param-owner-buckets` (6 ownership buckets), `secret-management` (env/local only, `key_env`, fail-closed), `upgrade-reconciliation` (who owns steering.md/manifests/migrations at upgrade), `schema-doc-drift` (generate or lint reference from schema YAMLs).

### Surfaces / UX (design §3, §8; user-workflow) · B1  *(live badges via daemon → B2; rich spatial → B2)*

- **U1 · Read-API + surface contract** ◑ — `five-workspace-jobs` (Read/Knowledge/Project/Review/Upkeep), `surface-contract-registry`, `local-http-reads`, `on-demand-service` (no resident daemon), `mcp-scoped-tools` (`operation_run` the only write tool), `request-envelope-writes`, `read-api-acceptance`, `cli-console`, `status-paths-action`, `context-read-set-action` / `situated-context-read`.
- **U2 · Deep-work + review cockpit** ◑ — `deep-work-workspace`, `deep-vs-task-split`, `adaptive-fixed-interface`, `keep-test` (runs with vim/nano), `co-pi-loop`, `static-review-cockpit`, `trace-to-revert-preview`, `attention-as-projection`, `cli-voice-findings`.
- **U3 · Obsidian plugin (thin renderer)** ○ — `obsidian-plugin` (enqueue-only over loopback HTTP; load bug #1350 fixed 2026-07-12 by #1369), `inbox-triage-cards`, `context-handoff-bus`, `observe-file-event` (manifest + push), `plugin-inbox-argument-health`, `plugin-live-status-feed` (poll-based B1), `argument-canvas-toulmin`, `id-filenames`. *Live on-save badges depend on the reactive daemon → the **live** layer is B2; poll-based status is B1.*
- **U4 · Co-PI skill / MCP** ◑ — `memoria-skill` / `shipped-memoria-skill`, `copi-surface-split` (engine authors the method, user's agent voices it), `mcp-server-wiring`, `vault-scoped-sessionstart-hook`, `toulmin-question-taxonomy`, `question-generation-operation`, `argument-health-profile`, `conversational-ask`.

### Cross-cutting substrate · B1

- **X1 · Records & safety** ◑ — `snapshot-revert` (snapshot→apply→verify→commit + revert), `event-ledger`, `derivation-lineage` + `field_provenance`, `reimport-unchecked`, `drift-detection-mutable-sources`, `manifest-driven-dispatch` (registry dispatch replacing the ~800-line if/elif; enforce manifest `allowed_paths`/`allowed_tools` in the worker), `attenuation-only-delegation`, `prompt-injection-defense` (provenance-triggered checkpoint; the risk "most likely to force a gate back in"), `constraint-discipline-checks`, `work_aspects-PK-widen`, `given-facts-placement-lane`, `zones-as-states`, `contradiction-vs-supersession-types`, `cassette-verification-spine`, `seeded-error-release-gate` (recall 1.0 / false-clean 0 — the license every non-sandbox use must pass).

---

## 3. Inherited READ flow that must still pass beta.1 acceptance · B1

Not new builds, but acceptance-gating (must run end-to-end at least twice):
`multi-source-capture` (Zotero/BibTeX/DOI/PDF/GitHub/local, land-then-resolve),
`extract-develop-notes`, `gap-over-both-graphs` (catalog graph, ZK argument
graph, and the gap between them).

---

## 4. beta.2 deferral register — the only things that leave beta.1

Each carries a **proven blocker**, its **beta.1 precursor** (what we still build
now so no effort is lost), and will get a beta.2 spec.

| Deferred unit | Proven blocker | beta.1 precursor (still built now) |
| --- | --- | --- |
| **Reactive daemon Tier A/B/C + SSE** (`memoria-daemon-*`, `sse-events`, live on-save badges) | Staleness-data-gated; alpha.19 same-transaction cascade makes a daemon never load-bearing. "No daemon before staleness data" (requirements). | `staleness-instrumentation` (I1), same-transaction cascade ✅, `observe-file-event` manifest, poll-based status (U3). |
| **Dense/hybrid retrieval DEFAULT activation** (+ `community-detection`, `ann-index`) | BM25 stays default until a pre-registered spike beats it on the real corpus. | Full dense substrate + `real-embeddings` + fixture harness (R3). |
| **Code-execution lane** (`code-execution-lane`, `sandbox-backend`, executable `computed`-warrants) | Large security surface, no MVP-loop dependency; requirements + design both descope post-beta.1. | `code_artifacts`/`code_runs` schema ✅, SRD *document* (C1). |
| **Warrant-node reification** (`warrant-as-node-ontology`) | Node-vs-edge is evidence-gated on warrant touch-budget observed in real use; expensive to reverse after accumulation. | Roles-carried-by-relations + earn-each-type six-role graph (G4). |
| **Autoresearch overnight loop** (`autoresearch-instrument`, `prompt-autoresearch`, `eval_dispatch-fill`, Tier-C nightly, scheduler) | Needs a scheduler (not built) **and** gold tasks harvested from real beta.1 use. | Seeded-error battery ✅, instrument single-file extraction, `program.md`, `empirical_event` plumbing (I1). |
| **memoria-in-memoria** | Depends on Tier 0 + real corpus loaded + seeded-error license + repo-at-pinned-commit source type. | All the machinery it will dogfood (Foundation → Surfaces). |
| **1000-scale seed load + acceptance test** (`seed-corpus-load`, `staged-import-acceptance-test`) | Disposition telemetry must exist first (non-backfillable); needs the real corpus. | Import pipeline + 10→100 staging + `bulk-admission-mode` (O2). |
| **Facility-location / DPP diversity** | Fixture-gated. | MMR baseline exploration (I1 diversity-channel). |
| **Two-way canvas↔outline sync** | Excluded until measured demand proves it. | One-way outline derivation (W1). |
| **Entity-resolution catalog** (`catalog_entities`) | Specified-but-unbuilt until a real duplicate/entity-browse need appears. | Free `work_graph_edges.target_id` join for gap-discovery. |
| **Multi-device write topology** | Freeze-blocker decision; default is single-writer local-first + read-only secondaries. | Single-writer enforcement ✅. |
| **Spatial-graph-at-scale · local fine-tuned adapter** | Explicitly "legitimately out"; frozen local model is only a test fixture. | Small project canvas (W1); API engine. |
| **Merge two Memoria bundles** (`bundle-merge`) | Owner ruling: an exception path, not the common case. | OKF export = folder copy; OKF import of a foreign bundle = the normal source-import path, both B1 (K1). |

---

## 5. docs/ migration map — the second destination

Corpus content that is durable **product documentation** migrates into the
published `docs/` Diátaxis tree (not deleted after folding). Most doctrine is
**already covered**; the genuinely new pages are few.

**New pages to create:**

- `docs/explanation/knowledge/consequence-propagation.md` — typed blast-radius / "a claim falls" (product-statement, user-workflow).
- `docs/explanation/architecture/okf-and-portability.md` — vault-as-OKF-bundle, producer profile, "cat works" durability (okf-note).
- `docs/explanation/architecture/consistency-model.md` — ACID trust plane + BASE knowledge plane, fail-closed reads (okf-note, design §12.4).
- `docs/reference/data-model/okf-compliance.md` — the §9 conformance contract.
- `docs/reference/pipelines-and-io/srd.md` — SRD contract (mark *planned*).
- `docs/reference/system/backup-and-recovery.md` — restore matrix (or fold into `failure-modes.md`).

**Merge deltas into existing pages** (do not duplicate): design axioms → `design-principles.md`; master pattern → `why-write-half-is-bounded.md`; bundle/three-spaces → `architecture/vault.md`; placement doctrine → `architecture/memory-model.md`; inquiry loop → `knowledge/knowledge-cycle.md`; surface model + five jobs → `surfaces/README.md`; retrieval architecture → `reference/pipelines-and-io/search.md`; schema deltas → `reference/data-model/{document-types,frontmatter,glossary,vocabulary}.md`; on-disk layout → `reference/system/on-disk-layout.md`; crash/param/secret → `reference/system/configuration.md`; onboarding → `how-to-guides/setup/set-up-zotero.md`; compose/verify → `how-to-guides/project/{compose-a-draft,export-a-draft}.md`.

**Already covered** (merge only if a line is missing): `what-memoria-is.md`,
`intellectual-foundations.md`, `co-pi.md`, `evidence-sets.md`, and the reference
data-model/system pages the analyses were cross-checking against.

**Not for docs** (pure build/release scope): the requirements doc, `insights.md`,
design §§5–11, and every "gap-to-current / migration" section of the analyses.

Note: docs pages describing target-state (fulltext-v2, canvas, daemon badges,
SRD) must be marked **planned** until the corresponding package lands, to avoid
docs claiming un-built behavior.

---

## 6. Current decisions to honor (revisable on merit) + conflicts needing an owner ruling

These are the **current** decisions the build complies with — but they are
revisable on merit. Living design-history *preserves* history; it does not lock
choices, and prior ADRs/designs/implementations are reference only, never a
justification or evidence (history is a teacher, not a prison).

**Honor unless deliberately revisited:** pinned pre-commit lint tools; sandbox hardening (canonical-path allowlist,
`--` delimiter, adaptive red-team); coherent-slice policy (whole-note-if-budget
else section-expanded-anchor); MMR baseline exploration; negation as a dedicated
verification risk category + prose-plus-evidence-set (not extraction-first);
seed-corpus licensing mechanics; retrieval default = BM25 until the spike;
`projects/<slug>/` nested bundles; LLM eval gates + production/test runtime
modes; fenced one-shot operations (not an agent loop); the four axioms /
four pillars; "no metric-driven optimization of vault content"; trust order
schema → tests → code → docs; **digest stays a separate concept type** (decided
2026-07-10 — not folded into `note.mode`; differs on production process,
granularity, lifecycle); **the vault `AGENTS.md` is a generated projection**
(written by `memoria init`, regenerated on upgrade, never PI-edited).

**Conflict rulings:**

1. **Fulltext storage (K2) — OPEN.** Fact (alpha.20): the raw artifact (PDF
   bytes) and extracted text already live **engine-space** at
   `.memoria/blobs/source-content/<work_id>/{raw/…, content.txt}` (immutable,
   content-addressed); `fulltexts/<work_id>.md` in the bundle is a *derived
   markdown projection* (a duplicate). Since export copies the bundle folder
   (excluding `.memoria/`), today only the projection travels — not the PDF. So:
   - **A — keep `fulltexts/`:** exported bundle carries readable text (not the
     PDF); keeps the duplicate + the `fulltext` pseudo-type.
   - **B — retire `fulltexts/`:** the blob already exists, so B is *deletion* of
     the projection, not new storage; but a copied bundle then carries neither
     text nor PDF unless the raw artifact moves into the bundle.
   → Decide **B + where the PDF/artifact lives** so export-by-copy stays
   self-contained. Does not block alpha.21; needed before K2.
2. **Warrant ontology (G4/§4) — RESOLVED: Option B.** Roles carried by typed
   relations with promotion-ready edges (`concept_edges-reshape` adds
   `edge_id` + attributes so a warrant ref hangs on the edge); reify a warrant to
   a node only when explicitly authored/shared; the node-default stays a
   touch-budget-gated beta.2 call. Warrant brainstorm = first design session
   after Foundation.

---

## 7. Corpus hygiene + source-doc disposition

**Junk to remove:** ~24 `:Zone.Identifier` files (Windows mark-of-the-web from
the copy-in) — delete and add `*:Zone.Identifier` to `.gitignore`; never commit.

**Duplicates to resolve (keep the canonical, drop the other):**

- `product-statement.md` (root, v2 — canonical) vs `plans/product-statement.md` (v1 — drop).
- `specs/0.1.0-beta.1-decisions.md` (superset, canonical) vs `specs/decisions.md` (same minus the dossier annex — drop).
- `specs/0.1.0-beta.1-empirical-use-action-plan.md` (v2, canonical) vs `specs/empirical-use-action-plan.md` + `plans/empirical-use-action-plan.md` (v1 — drop).

**After the fold — where source docs live:**

- **Stay in `specs/`** (they govern the in-flight release; fold into
  `design-history/` when beta.1 closes): requirements, design-of-record, the two
  analyses, the canonical decisions ledger, the empirical-use plan.
- **Retired 2026-07-12** (content captured in §2 with live-code status;
  git-recoverable as evidence): the four audits, `roadmap.md`, `insights.md`
  (all not-for-docs per the §5 routing), the alpha.17 ledger (folded into
  `design-history/17-alpha.17.md`), and the done process records
  (`superpowers-alignment-plan.md`, `harness-goal-audit.md`).
- **Retire at #1366 close** (their doc-worthy prose still migrates via §5):
  `product-statement.md` and — moved to `specs/` 2026-07-12, since they are
  design notes, not plans — `specs/okf-note.md`, `specs/user-workflow.md`,
  `specs/autoresearch-note.md`. The two `2026-07-11-foundations-reconcile*`
  records were retired 2026-07-12: their prose is embedded verbatim in
  `plans/2026-07-12-docs-migration.md`.
- **Derived 2026-07-12**: `0.1.0-beta.2-scope.md` (redeems §4's beta.2-spec
  promise) and `warrant-ontology-brief.md` (the scheduled first
  post-Foundation design session).

---

## 8. Sequencing & next step

**Release plan (locked 2026-07-12): alpha.21-first.**

- **alpha.21** = Foundation (F1–F4) + instrumentation skeleton (I1) + docs/ migration. Milestone `0.1.0-alpha.21` (the empty `0.1.0` retires).
- **alpha.22** (optional) = G1 migration-machinery + graph substrate (G2–G5) + schema hygiene (S1) — the inherited-substrate backfill the requirements doc requires before beta.1.
- **beta.1** = the WRITE half (W, C1, V, O, U) on the backfilled substrate, with the real-corpus dogfood + adopt/redesign/kill verdict.

**Schema-before-corpus (load-bearing).** Every cheap-while-empty reshape — actor enum, `concept_edges` `edge_id`/attribute columns, `item_type` CHECK, ULID keys, reverse indexes, and the seeded `.base`/`graph.json`/`types.json` view files — must land **before** the 1000-paper bulk import (`seed-corpus-load`, beta.2). Migration-cost-is-zero expires the day durable rows land; after that every shape change ships as a numbered ALTER migration. (1000 full-text papers ≈ 50k–200k passage rows, brushing the brute-force-KNN flip — the staged import measures Shape-1/Shape-2 latency per stage.) This is the structural reason the substrate releases (alpha.21/alpha.22) precede the import.

**Dependency spine (within and across the releases above):**

1. **Foundation (F1–F4)** — trust repairs; unblocks everything, mostly bug-fixes.
2. **G1 migration-machinery** — the single hard prerequisite for all schema work.
3. **Instrumentation (I1)** — stand up **before** any ingestion (non-backfillable).
4. **Graph substrate (G2–G5) + schema hygiene (S1–S2)** — the substrate the WRITE
   half and propagation ride on.
5. **Retrieval (R1–R3) + OKF/consistency (K1–K4)** — in parallel with 4 where deps allow.
6. **WRITE half: Writing (W1–W2), SRD (C1), Verification (V1–V3)** — the MVP output.
7. **Onboarding (O1–O2), LLM-eval (E1), Surfaces (U1–U4), Cross-cutting (X1)**.
8. **beta.1 acceptance** (§3 + requirements §7) → exit verdict.

**Suggested first sub-project: Foundation (F1–F4).** It is mostly repairs to
shipped code, it removes the "wired-at-edges-dead-in-middle" and provenance
defects that undermine trust in everything above it, and it is the roadmap's own
Tier 0. It's the natural first spec → plan cycle.

**Owner-decisions — status (2026-07-12):**

- ✅ Warrant ontology → Option B (§6). ✅ Release strategy → alpha.21-first (above). ✅ beta.2 register (§4) confirmed. ✅ Corpus hygiene (§7) done.
- ⏳ **Fulltext storage (§6)** — decide Option B + where the PDF/artifact lives; not needed until K2, does not block alpha.21.
- ✅ **Milestone** — done: `0.1.0-alpha.21` / `0.1.0-beta.1` / `0.1.0-beta.2` created, empty `0.1.0` retired, alpha.21 packages filed (#1361–#1366), the 22 pre-existing issues triaged onto the three milestones (#1353 closed as decided).

---

## 9. What this preserves

Every one of the ~250 corpus units is folded into a package (§2), the beta.2
register (§4), the docs map (§5), or the decisions ledger (§6) — nothing is
dropped. The audits' invested findings survive as buildable units with live-code
status; the doctrine survives as published docs; the ratified decisions survive
as build constraints. Deletion of a source file happens **only after** its
content has a durable home here, in `docs/`, or in a tracked issue.
