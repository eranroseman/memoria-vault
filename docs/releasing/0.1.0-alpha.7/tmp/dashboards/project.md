---
title: Project
dashboard: gate
gate: project
---

# Project — steer inquiry to output

Gates: [[inbox|Inbox]] · [[library|Library]] · [[knowledge|Knowledge]] · **Project**

> [!note] Draft gate dashboard → golden copy **Project** gate. Embed token/view
> pending `workspaces-design.md` §8.

> [!tip] No projects yet? Create one with the **`project`** form (ribbon). A project
> carries its inquiry (population/intervention/comparison/outcome) and FINER scope.

## Active projects

<!-- default view: "Active" (lifecycle == current) -->
![[projects.base]]

> Other views (same base): **Saturation** (group by `saturation_state`, show
> `graph_maturity` / `computed_at` with an "as of" caveat — cached fields) ·
> **Project gaps** (group by `gap_type`, sort `impact` DESC, `on_path` first).

## Per-project page

Open a project note to see its thesis, sources, and gaps.

> [!info] The projected **argument canvas** (the link-open button on a project page)
> is **deferred** — it depends on the unbuilt Canvas projection. In alpha.7 the
> Project gate is the base above plus the authored project pages. See
> `ui-architecture-future.md` (spatial axis).

---
*Theses attach to a project (`thesis` form). Capture and actions are in the ribbon;
gate switcher is the nav row above.*
