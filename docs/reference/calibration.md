---
title: Calibration
parent: Reference
---

# Calibration

`src/.memoria/schemas/calibration.yaml` is the source of truth for shared
thresholds. Every calibrated value follows the same discipline: grounded in real
Memoria data, bounded by an explicit error budget, recalibrated on drift, and
shadow-first before it affects routing or automation.

## Production rule

A score may affect production behavior only when all of these are true:

- `production_enabled: true`
- the relevant threshold fields are non-null
- `grounding_dataset` names the reviewed dataset or log slice used to fit the
  thresholds
- `model_version` pins the model, scorer, or upstream source version whose scores
  were calibrated
- the sample-count and error-budget fields in that block are satisfied

If any threshold is `null`, the score is report-only. It may log shadow telemetry
or appear in a review surface, but it must not auto-accept, auto-defer, hide,
merge, block, or reorder canonical work.

<!-- REFERENCE:START -->
<!-- Generated by scripts/gen_reference_refs.py; edit schema sources instead. -->

Generated from `src/.memoria/schemas/calibration.yaml`.

| Path | Value |
| --- | --- |
| `entity_resolution.confidence_floor` | `0.85` |
| `entity_resolution.model_version` | `null` |
| `classify.confidence_floor` | `0.6` |
| `classify.near_tie_margin` | `0.15` |
| `classify.model_version` | `null` |
| `inbox.archive_after_days` | `30` |
| `clustering.embedding_model` | `null` |
| `clustering.umap_n_neighbors` | `15` |
| `clustering.hdbscan_min_cluster_size` | `5` |
| `clustering.full_cluster_min_documents` | `10` |
| `clustering.seed` | `42` |
| `clustering.quality_thresholds.production_enabled` | `false` |
| `clustering.quality_thresholds.silhouette_floor` | `null` |
| `clustering.quality_thresholds.topic_coherence_floor` | `null` |
| `clustering.quality_thresholds.min_reviewed_maps` | `20` |
| `clustering.quality_thresholds.max_cluster_churn_rate` | `0.15` |
| `clustering.quality_thresholds.grounding_dataset` | `null` |
| `clustering.quality_thresholds.model_version` | `null` |
| `hybrid_scores.candidate_rank.production_enabled` | `false` |
| `hybrid_scores.candidate_rank.promote_threshold` | `null` |
| `hybrid_scores.candidate_rank.defer_threshold` | `null` |
| `hybrid_scores.candidate_rank.min_labeled_decisions` | `200` |
| `hybrid_scores.candidate_rank.max_false_promote_rate` | `0.05` |
| `hybrid_scores.candidate_rank.max_top_decile_miss_rate` | `0.1` |
| `hybrid_scores.candidate_rank.grounding_dataset` | `null` |
| `hybrid_scores.candidate_rank.model_version` | `null` |
| `hybrid_scores.outline_score.production_enabled` | `false` |
| `hybrid_scores.outline_score.accept_threshold` | `null` |
| `hybrid_scores.outline_score.revise_threshold` | `null` |
| `hybrid_scores.outline_score.min_labeled_outlines` | `50` |
| `hybrid_scores.outline_score.max_false_accept_rate` | `0.05` |
| `hybrid_scores.outline_score.max_false_revise_rate` | `0.15` |
| `hybrid_scores.outline_score.grounding_dataset` | `null` |
| `hybrid_scores.outline_score.model_version` | `null` |

<!-- REFERENCE:END -->

## Drift

Recalibrate any enabled block when the scoring model, embedding model, upstream
classification source, prompt, or feature extraction changes. Until recalibration
passes the same sample and error-budget checks, set `production_enabled: false`
and return the score to shadow mode.
