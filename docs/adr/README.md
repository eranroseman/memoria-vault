# Architecture decision records

One file per decision, `NNNN-kebab-title.md`, numbered in the order they land.

Written lazily — `/domain-modeling` (reached via `/grill-with-docs` and
`/improve-codebase-architecture`) creates one when a decision actually gets
resolved, not upfront. An empty directory is the correct state until then.

Records predating this directory survive only as a frozen export at
`design-history/archive/notes/docs-exports/adr-full.md`; issue #902 cites
ADR-113 from there. Those have no live file until someone needs one.

Not published to the site — see `docs/_config.yml`.
