{
  "name": "memoria-vault-devtools",
  "version": "0.0.0",
  "private": true,
  "description": "Stub package.json required by pre-commit language: node hooks; hook tool versions live in .pre-commit-config.yaml."
}
