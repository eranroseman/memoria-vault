<!-- cspell:words SQLite OpenAlex Unpaywall CSL BibTeX pydantic allowlist Obsidian Hermes Memoria Zotero QuickAdd Dataview worktree authorships MeSH SDG -->

# Release 0.1.0-alpha.14

Draft release parent issue body. Live release state belongs in the GitHub milestone,
the "Release 0.1.0-alpha.14" parent issue, and its gate/stage sub-issues. Delete
this scratch file before the release/checkpoint closes, after durable decisions move
to ADRs, docs, issues, or release notes. Tactical detail lives in
`scratch/releases/0.1.0-alpha.14/0.1.0-alpha.14-exec-plan.md`.

## Status

Draft sandbox release plan for the alpha.14 standalone CLI + engine cut. This is a
full architecture cut, not a partial phase: Memoria becomes a standalone local CLI
tool with a SQLite engine, and Obsidian, Zotero, Hermes, MCP, installed profiles, and
lane/fleet are dropped from the baseline. The alpha.12 SQLite working-state boundary
(ADR-122) and alpha.13 DOI enrichment (ADR-123) already landed on `main`, so the
engine exists. Current `main` through `88b7f0f5` / PR #1157 has the `memoria`
console script, grouped CLI surface, request-envelope dispatch for wired mutating
commands, workspace-local qmd checks/rebuild support, `doctor --repair`,
expanded `init --dry-run --json`, journal filters, `workspace check --assert
no-legacy-work-markdown`, capability-backed `operation list`, hardened network
allowlist matching, stable work IDs for Co-PI interview runs, and DB-only product
capture operation routing. Digest/interview operation evidence, inputs, and hub
members now use Work ID refs, and integrity/provenance/quote-anchor checks resolve
those refs through SQLite catalog rows plus acquired blob text. Generated
`references.bib` and catalog-index projections now render checked SQLite catalog
rows only. Lower-level capture helper entry points now route through SQLite
catalog rows plus `.memoria/blobs/source-content/<work-id>/...` payloads and no
longer create legacy Work markdown, legacy raw/content folders, derived entity
markdown, or capture-time `references.bib`. pydantic-ai is now the sole accepted
operation runner, all checked operation manifests declare `runner: pydantic-ai`,
and the generated capability index is regenerated. Capability Concepts now use
file-per-capability operation manifests and optional `adapter` Concepts under
`capabilities/adapters/`; `mcp` is no longer a capability type. The old operation
implementation port is landed: all 26 legacy `.memoria/operations/` Python
modules now live under `src/memoria_vault/runtime/subsystems/`, tests import the
runtime package path, `eval-run` has worker dispatch and a capability manifest,
and direct Hermes transport/env assumptions were removed from the copied
reconcile/eval/ingest paths. PR #1118 removed the vacated
`vault-template/.memoria/operations/` tree, moved the template pre-commit hook to
`vault-template/.githooks/`, rewrote current callers to runtime package modules,
and added `scripts/alpha14_negative_gate.py` for operation-home references. PR
#1119 removed shipped installed profiles, lane overrides, the profile tool
registry, profile rendering, live Hermes/L2 smoke scripts, and profile/Hermes
installer modes; current docs and agent guidance now describe the standalone CLI
baseline, and the negative gate covers the deleted profile/lane/tool-registry
surfaces. PR #1120 removed the shipped `vault-template/.memoria/mcp/` adapter
home, moved policy-hook enforcement into `src/memoria_vault/runtime/policy/`,
kept optional adapter/plugin entry points around the installed runtime package,
rewrote CI/installer/docs/generated references away from `.memoria/mcp`, and
expanded the negative gate so `.memoria/mcp` cannot reappear in runtime,
installer, tests, or current docs. PR #1121 removed legacy Work Markdown source
fallbacks from runtime digest, integrity, rollback, projection, and seeded-error
source paths; checked SQLite catalog rows are now the source authority. PR #1122
removed the shipped fleet-health dashboard, Fleet nav target, and Memoria
Inspector lane-metric/Fleet trust panel, then expanded the negative gate for
those contracts. PR #1123 removed the Zotero-specific CLI format, worker
operation, runtime helper, capability file/index row, stale tests, and
current-doc references, then expanded the negative gate for the deleted
operation id/helper/format/path. PR #1124 removed the shipped `.obsidian/**`
tree and QuickAdd script/button payloads from `vault-template`, stopped
installer/refresh/smoke/provenance/golden-restore/docs tooling from expecting
plugin bundles, rewrote first-run/current docs around the CLI/editor baseline,
and expanded the negative gate so those contracts cannot return. PR #1125
removed dead installed-profile docs mirror checks, the generated profile-policy
matrix, profile/Hermes issue-template and PR-template language, and package-gate
lane-deny fixture fabrication; it also rewrote current docs around the
standalone request-control plane, operation postures, workspace memory, attention
prompts, and optional-adapter boundaries. PR #1126 removed the remaining runtime
lane-policy loader: actor policy now loads from
`<workspace>/.memoria/config/policy.yaml`, optional adapter hooks require
explicit `request_id`, audit/session consumers use `actor` + `request_id`, and
the negative gate rejects the deleted lane-policy contracts. PR #1127 repaired
the package verifier command so it builds dependency wheels into the evidence
`dist/` directory before the no-index package install. PR #1128 removed the
remaining tracked non-policy `lane`/`task_id` residue: work-prompt metadata now
uses `request_id` plus optional `posture`, worklist prompts no longer emit
lanes, vault-eval uses `eval_role`, package replay cassettes match the new
shape, stale current docs use request/operation-posture language, and the shipped
Hermes-backed `.githooks/post-commit` trigger is gone. PR #1129 added the first
PR 8 parity fixture: the visible historical `Memoria:` command-palette actions
are exercised through standalone CLI commands, startup shell restore is asserted
adapter-only rather than core CLI, `operation list` is compared against
workspace manifests, and every manifest id is dispatched through `operation run`
to catch unsupported worker branches. PR #1130 routed shipped scheduled wrappers
through public CLI commands with stable `--schedule-id` provenance, made
`workspace scan` report that schedule id, removed dead Hermes cron installer
code, and corrected the linter how-to away from Hermes cron. PR #1131 added a
thin CLI e2e fixture for init, capture/enrich/update/interview/digest, note
proposal/accept/link, generic Ask, project Ask, gap scan, project export, and
recovery, and it made project Ask pass project ids into retrieval so checked
project context appears in the Ask contract. PR #1132 made request-control
mutations keep row `args_json`, job payload, and embedded `request_envelope.args`
in sync for operation and trusted-write requests. PR #1133 made
`workspace scan --json` report observed PI edits that need checking, and added
CLI evidence that direct checked keep-set edits are marked unchecked,
non-consumable, journaled as `observed_external_edit`, and promoted only through
`operation run mark-checked`. PR #1134 added an explicit runtime-local replay
gate that exercises the alpha.14 command sequence across init, capture,
enrichment, update, interview, digest, note curation, recovery, runner/qmd
doctors, scan quarantine, scheduled queue/check commands, rebuild, Ask/project
Ask/export, journal lookup, and seeded-error verdict; `scripts/verify runtime`
now runs that replay before the older runtime suites. PR #1135 made checked
metadata-only/abstract-only digest attempts fail in the shared runtime path,
create a stable attention projection, expose the attention path in the failed
request error, and leave no consumable digest output. PR #1136 made
`memoria project gaps <project_path>` project-scoped by feeding checked project
argument/Toulmin health into the shared `analyze-gaps` runtime path, while
leaving project-less `operation run analyze-gaps` available for workspace-wide
maintenance scans. PR #1137 made gap analysis count checked SQLite catalog
source terms from `csl_json.memoria.tags`, `topics`, and `research_area`,
deduped by catalog source ID and ignoring unchecked or archived rows. PR #1138
made generated qmd graph-neighborhood documents include raw OpenAlex
topic rollups and made gap analysis count checked topic graph edge terms as
source evidence. PR #1139 made portable BibTeX/CSL import seed explicit
reference edges into SQLite `work_graph_edges` while preserving provider graph
rows. PR #1140 made enrichment try ordered Unpaywall Open Access location URLs
until one allowlisted, reachable, non-empty candidate yields extractable full
text, skipping denied, failed, extraction-failing, or empty candidates. PR #1141
extended that path to include Unpaywall `oa_locations` after `best_oa_location`,
with URL dedupe and the same ordered list used for fixture text and license
lookup. PR #1142 made OpenAlex `primary_topic` import as a first-order topic
graph edge alongside the `topics` list. PR #1143 added OpenAlex `keywords` as
first-order `keyword` graph edges and made checked keyword graph terms count in
gap analysis. PR #1144 extended discovered full-text acquisition beyond
Unpaywall-only payloads by trying OpenAlex `open_access.oa_url` and OA
`best_oa_location` / `primary_location` / `locations` URLs through the same
allowlisted retry loop. PR #1145 added `memoria doctor --check runner --live`,
an opt-in pydantic-ai live dispatch through the same model/network policy guard
used by operations, and made `scripts/verify live` run the live pytest marker.
PR #1146 made project-scoped Ask expand qmd queries with checked project
scope/facet terms and checked linked thesis terms, and exposes those terms in
`project_context`. PR #1147 made SQLite-detected source/topic gaps carry checked
source IDs into discovery candidate generation, so candidate Works can be
proposed from first-order reference/related graph edges without qmd rediscovery.
PR #1148 made project gap analysis seed checked project scope/facet terms and
count checked linked thesis terms without manual `--seed-term` input. PR #1149
made OpenAlex `concepts` import as first-order `topic` graph edges during DOI
enrichment. PR #1150 made Crossref relation rows import as first-order
`related` graph edges during DOI enrichment and feed discovery candidates
through the existing graph path. PR #1151 made Crossref `message.link[].URL`
candidates feed the existing discovered full-text acquisition loop after
Unpaywall/OpenAlex candidates. PR #1152 made embedding-enabled qmd rebuilds
persist `mode: hybrid`, `embeddings: true`, and the `qmd embed
--chunk-strategy auto` command in the checked-search manifest. PR #1153 made
gap scans write PI-reviewable full-text attention flags for catalog rows that
cannot produce checked digests or retrieval because full text is missing. PR
#1154 made OpenAlex `authorships`, nested institutions, and location Source
metadata persist as graph edges and feed qmd graph-neighborhood retrieval. PR
#1155 made OpenAlex `mesh` and `sustainable_development_goals` persist as
first-order topic graph edges through the same provider-topic path. PR #1156
made `knowledge/_views/index.md` a worker-owned tracked projection under the
final generated-path directory, wired it into projection drift checks,
direct-write scan quarantine, init dry-run targets, runtime replay, package
refresh, shipped template evidence, and current-doc projection mirrors. PR
#1157 enforced universal Concept frontmatter for `knowledge/` and
`capabilities`: path-correct `id`, approved `standing`, and map-shaped `links`;
trusted-writer validation and `note capture` default/check those fields; shipped
operation manifests, schemas, PI templates, package-gate cassettes, e2e smoke
fixtures, and reference docs use the new shape.
The main blockers are broader graph import beyond the current first-order
OpenAlex/Crossref/import fields, full-text acquisition depth beyond the current
Crossref link/OpenAlex/Unpaywall URL fields, remaining gap/discovery depth,
remaining PR 9 typed facets/steering/vocabulary/asset Concept depth if still
required, and packaging/docs cleanup. No implementation worktree is active after
#1157; the next branch should start from `origin/main` at `88b7f0f5`.

## Scope

Alpha.14 delivers, in `Memoria-test`, a grouped `memoria` CLI over the existing
SQLite engine that runs the thin end-to-end cycle - `init`, `work capture`,
`work enrich`, `work update`, `work interview`, `work digest`,
`note propose/accept/link`, `memoria ask`, `project ask`, `project export`, and
`workspace recover` - with no Obsidian, Zotero, Hermes, MCP, or profile
dependency. Capture becomes SQLite-only (no legacy Work Markdown, no OpenAlex
record Markdown). The runner becomes pydantic-ai over an OpenAI-compatible
endpoint. Operation manifests become file-per-capability with optional same-stem
asset folders only for oversized prompts, schemas, or examples. Existing
operation logic is imported into the new runtime before obsolete operation homes
are removed. The dropped-baseline surfaces are removed from runtime, installer,
current docs, and tests, and an `rg` negative gate proves it. The full
user-facing parity floor (palette actions plus the manifest-derived operation
set, qmd-backed checked-only keyword/semantic retrieval, full-text acquisition,
first-order citation/topic graph, embeddings, discovery candidates,
attention/worklists, linter/session summaries, diagnostics, and export) runs
through CLI/engine.

Dropped from the baseline (not deferred): Obsidian as required surface, Zotero as
Work authority, Hermes, MCP as required operation API, installed profiles,
lane/fleet, `direct_api` as a product runner, backward-compatibility/migration
branches, directory-only capability manifests, and legacy markdown Work support.

Later / deferred: ISBN and URL-primary depth beyond the full-text/digest
contract; provider-specific Semantic Scholar, PubMed, and arXiv hardening beyond
the OpenAlex/full-text baseline; exhaustive citation expansion; non-baseline
embedding jobs; topic expansion beyond OpenAlex/controlled facets; full Writer
agent and automated draft/verify workflow; RO-Crate/BagIt and in-toto/SLSA; any
optional Obsidian/MCP adapter or Zotero adapter around generic BibTeX/CSL files
and the stable CLI/engine.

## Implementation Slices

Maps to the exec-plan PRs. Work in small PRs off `origin/main`.

Merged implementation slices so far: #1096 and #1100-#1157. They are useful
progress inside slices 2, 3, 4, 5, 8, and 11. They close slices 4 and 5, but the
remaining touched slices still have open work: full-text acquisition depth,
graph import, remaining gap/discovery depth, residual parity/product
loop evidence, and packaging/docs cleanup remain open. Slice 3 now has product capture, generated
projections, read-barrier/integrity, and lower-level helper cleanup landed.
Slice 4 has file-per-capability manifests and `adapter` capability naming
landed. Slice 5 has the pydantic-ai runner and opt-in live proof landed. Slice
6 has the operation-subsystem port landed in #1117. Slice 7 has landed for the
tracked dropped-baseline cleanup:
#1118 removed the vacated operation home and added the first negative gate; #1119 removed shipped installed profiles,
lane overrides, the profile tool registry, profile rendering, live Hermes/L2
smoke scripts, and installer profile/Hermes modes, then expanded the negative
gate for those surfaces; #1120 removed the shipped `.memoria/mcp` adapter home,
moved policy-hook enforcement into the runtime package, removed wrapper-only MCP
tests/scripts, rewrote current docs to the standalone boundary, and expanded the
negative gate to reject `.memoria/mcp` references outside historical ADR context.
Then #1121 removed legacy Work Markdown source fallback reads, moved source
metadata/prompt-injection checks to DB source rows, added DB source rollback
quarantine markers, and updated seeded-error source targets to canonical Work IDs.
Then #1122 removed the shipped fleet-health dashboard and lane-metric Inspector/
nav/docs surface. Then #1123 removed the Zotero-specific import/operation
surface and negative-gated the deleted contracts. Then #1124 removed the shipped
Obsidian/QuickAdd payload and negative-gated the deleted contracts. Then #1125 removed
dead generated profile mirror tooling, stale profile/Hermes templates,
package-gate lane-deny fixture fabrication, and current-doc profile/lane/Hermes
baseline claims. Then #1126 removed runtime lane-policy residue and replaced it
with actor/workspace policy plus explicit `request_id` audit grouping. PR #1127
repaired the package verifier dependency-wheelhouse command. PR #1128 landed the
final tracked PR 7 pass by removing non-policy work-prompt/eval/cassette/doc
lane/task residue and the shipped Hermes-backed post-commit hook. New
dropped-surface hits should now be handled as regressions against the negative
gate or current-doc review frontier. PR #1129 started slice 8 by adding the
dedicated parity fixture for command-palette equivalents, adapter-only startup
classification, manifest-derived `operation list`, and `operation run` dispatch
coverage. PR #1130 routed scheduled worker/lint/eval wrappers through the CLI
with schedule provenance and removed the stale Hermes cron installer helper.
PR #1131 added thin end-to-end CLI loop coverage and project-aware Ask context.
PR #1132 added request-control envelope consistency. PR #1133 added
scan-observed needs-check reporting and promotion evidence. PR #1134 added the
explicit runtime replay gate and wired it into `scripts/verify runtime`. PR
#1135 landed PR 3's digest full-text gate: checked metadata-only/abstract-only
sources now create attention and fail before digest materialization. PR #1136
landed project-scoped gap analysis: the product command reads checked project
argument/Toulmin health through the shared `analyze-gaps` runtime path, while
project-less `operation run analyze-gaps` remains the workspace-wide maintenance
operation. PR #1137 made source-gap analysis count checked SQLite catalog
source topic/research-area terms without depending on legacy Work Markdown. PR
#1138 made qmd graph-neighborhood documents searchable by raw OpenAlex topic
rollups and made source-gap analysis count checked topic graph edges. PR #1139
made portable BibTeX/CSL import seed explicit first-order reference edges in
SQLite. PR #1140 made discovered Open Access full-text acquisition try fallback
Unpaywall location URLs before giving up on acquired text. PR #1141 made the
same discovery path read Unpaywall `oa_locations` after `best_oa_location`.
PR #1142 made OpenAlex `primary_topic` enter the first-order topic graph. PR
#1143 made OpenAlex `keywords` enter the first-order graph as `keyword` edges
and feed gap analysis. PR #1144 made discovered full-text acquisition use
OpenAlex OA URL/location fields after Unpaywall candidates. PR #1145 added the
opt-in live runner proof gate. PR #1146 made project-scoped Ask expand qmd
queries with checked project and linked thesis terms. PR #1147 made
SQLite-backed source gaps emit candidate Works from first-order graph edges
without qmd rediscovery. PR #1148 made project gap analysis seed checked project
scope/facet terms and count checked linked thesis terms without manual
`--seed-term` input. PR #1149 made OpenAlex `concepts` enter the first-order
graph as `topic` edges. PR #1150 made Crossref `relation` rows enter the
first-order graph as `related` edges and feed discovery candidates through the
existing graph path. PR #1151 made Crossref `message.link[].URL` candidates
feed discovered full-text acquisition after Unpaywall/OpenAlex candidates. PR
#1152 made embedding-enabled qmd rebuilds persist hybrid manifest evidence. PR
#1153 made missing-full-text catalog rows actionable by writing gap-scan
attention flags. PR #1154 made OpenAlex authorships, institutions, and location
Sources import as graph edges for generated graph-neighborhood retrieval. PR
#1155 made OpenAlex MeSH and SDG rows import as topic graph edges. PR #1156
made `knowledge/_views/index.md` part of the tracked generated projection set,
drift checks, scan quarantine, worker output contracts, tests, package refresh,
and current-doc projection mirrors. PR #1157 enforced universal Concept
frontmatter for `knowledge/` and `capabilities`, backfilled shipped operation
manifests and PI templates, added shared writer/defaulting checks, refreshed
reference docs, and proved package-gate fixtures with the new shape. No
implementation worktree is active after #1157. Good next scopes are the next
concrete graph import hole, remaining full-text acquisition depth, typed
facets/steering/vocabulary/asset Concept depth if still required, or a focused
gap/discovery depth hole.

1. Decision authority ADRs (alpha.14 as the cut; supersede Hermes/lane/profile/MCP/
   `direct_api`/Obsidian/OpenAlex-style graph markdown ADRs). New ADRs start at 124.
2. CLI spine + engine boundary: grouped `memoria <resource> <action>` console
   script, request rows before work, `--workspace`/`--json`/exit codes, and
   CLI commands, scan-observed file changes, and scheduled jobs with no
   dedicated ingress-type field.
3. SQLite-only OpenAlex graph: stop capture writing markdown Work, Author,
   OpenAlex Source, or Institution records; keep SQLite;
   `references.bib` generated from SQLite, not at capture; integrity,
   quote-anchor, references, projections, rollback, and recovery resolve Work IDs
   through SQLite. The full-text-required digest gate landed in #1135:
   metadata-only and abstract-only checked rows fail before materialization and
   leave no consumable digest output.
4. File-per-capability manifests; directory-only manifests fail closed;
   regenerate `capabilities/_generated/capability-index.json`; rename/drop the
   `mcp` type. Landed in #1116 as `adapter`.
5. Runner: single pydantic-ai runner; remove Hermes and `direct_api` dispatch; keep the
   pre-network allowlist and structured output. Landed in #1115; opt-in live
   runner proof landed in #1145.
6. Import old operation implementations into the runtime engine with no logic
   dropped before deleting obsolete operation homes. Landed in #1117.
7. Drop-baseline cleanup sweep + negative gate: operation-home deletion,
   runtime-package caller rewrites, and the first negative gate landed in #1118.
   Installed-profile/lane-override/profile-tool-registry deletion, profile/Hermes
   installer-mode removal, current-doc rewrites, and the expanded deleted-surface
   gate landed in #1119. Shipped `.memoria/mcp` adapter-home deletion,
   policy-hook runtime relocation, wrapper MCP test/doc cleanup, and the
   `.memoria/mcp` negative gate landed in #1120. Legacy Work Markdown source
   fallback removal, DB source rollback, and canonical seeded-error source targets
   landed in #1121. Shipped fleet-health/lane-metric dashboard residue landed in
   #1122. Zotero-specific import/operation residue landed in #1123; portable
   `work import --format bibtex` and `--format csl` remain the baseline. Shipped
   Obsidian/QuickAdd residue landed in #1124: no `.obsidian` tree, no QuickAdd
   command scripts, no plugin-bundle presence tests, no Obsidian workspace/
   provenance smoke assumptions, and no current docs that present Agent Client,
   Local REST, Dataview, or Obsidian launch macros as required behavior.
   Lane/profile current-doc cleanup landed in #1125: no generated profile-policy
   matrix, no installed-profile mirror checks, no profile/Hermes issue-template
   prompts, no package-gate fabricated lane-deny fixture, and current docs now
   present the request-control/operation-posture baseline. Optional-adapter
   lane-policy cleanup landed in #1126: runtime policy uses actor/workspace
   policy loaded from `<workspace>/.memoria/config/policy.yaml`, audit rows are
   keyed by `actor` and `request_id`, `LanePolicy`/`load_lane`/lane-overrides/
   tool-registry allowlist concepts are gone from current runtime policy, and
   `session_id` is no longer accepted as a request fallback. PR #1128 closed the
   tracked non-policy `lane`/`task_id` cleanup: work-prompt frontmatter moved to
   `request_id`/optional `posture`, eval routing moved to `eval_role`, replay
   cassettes and current docs were updated, and the shipped Hermes-backed
   post-commit hook was removed. Treat future obsolete non-policy `lane`/
   `task_id` occurrences as regressions unless they are explicitly historical
   ADR context.
8. Knowledge loop + parity fixture (`note capture`, `attention list/show/resolve`,
   `attention worklist`, `workspace scan`, `work update`, `work interview`,
   `work digest`, hub updates, notes, `note accept/reject/link`, `project gaps`,
   `memoria ask`, `project ask`, `project export`,
   `request answer/amend/cancel/retry/resume`, `operation list/run`,
   `workspace rebuild`, `workspace export`,
   `journal list/show`, `doctor bundle/self-test`, `workspace check`, scheduled
   jobs, and `eval seeded-error-verdict`). PR #1133 covered the scan-observed
   direct-PI-edit path: observed edits appear in `workspace scan --json` as
   needs-check paths, remain unchecked/non-consumable until promoted, and use the
   existing `mark-checked` capability rather than a new top-level command. PR
   #1134 covered the replayed runtime-local command gate and made it the first
   `scripts/verify runtime` runtime step.
9. OKF bundle validation + universal Concept frontmatter (`id`, `standing`, `links`,
   typed facets, steering, vocabulary, asset Concepts, operation `method_class`);
   final generated paths: `knowledge/index.md`, `knowledge/_views/index.md`
   under the final `knowledge/_views/` directory, `capabilities/index.md`, and
   `capabilities/_generated/capability-index.json`. The generated-path work
   through `knowledge/_views/index.md` landed in #1156. Universal-frontmatter
   enforcement/backfill for `knowledge/` and `capabilities/`, plus shipped
   capability/schema/template updates, landed in #1157; typed facets/steering/
   vocabulary/asset depth remains open if alpha.14 needs more than the current
   schemas enforce.
10. Seeded-error verdict + installed runtime-local gate on replay + crash fixtures.
11. Docs, installer, packaging: `pip install -e .`, `memoria init`/`doctor`, Node
    >=22 or bundled qmd, workspace-local qmd config/index state, CLI-first docs
    only.

## Definition Of Done

This release is done when every gate/stage sub-issue below is closed.

| Gate | Proves | Evidence home |
|---|---|---|
| Code | CLI spine, request-envelope routing, SQLite-only OpenAlex graph, pydantic-ai runner, imported operation logic, file capability manifests, dropped-baseline removal, the dropped-surface negative gate, and static checks are coherent. | CI and `scripts/verify pr` summary |
| Package | Disposable workspace assembly ships the SQLite DB, blob dirs, qmd runtime dependency or bundle, absolute qmd path, workspace-local qmd config/index state, registered `memoria-checked` collection, `knowledge/`+`capabilities/` OKF bundles, `knowledge/_views/index.md`, file capability manifests, and workspace provider policy - with no Obsidian plugin, Hermes profile, or Zotero. | `scripts/verify package` summary |
| Runtime | Installed CLI dispatch, qmd doctor/rebuild, write boundary, scheduled wrappers, generated-projection direct-write quarantine, provider allowlist, materialization recovery, diagnostics, journal summaries, and pydantic-ai/local-model dispatch work live or have explicit skip reasons. | `scripts/verify runtime` or skip evidence |
| Product | The thin CLI e2e cycle produces a checked usable work, portable `references.bib`, conflict/retraction attention, explicit note/link curation, generic/project ask, project export, the parity set, and passes the seeded-error bars with no Obsidian/Zotero/Hermes/MCP/profile dependency. | Product/manual sub-issue |
| Release | Blockers, docs, versioning, release notes, scratch cleanup, and rollout/rollback decisions are ready. | Release parent/sub-issues |

## Gate Sub-Issues To Create

- G1 Code gate: CLI spine + SQLite-only OpenAlex graph + pydantic-ai runner + imported
  operation logic + file capability manifests land with failing-first tests, including
  Work ID evidence/read-barrier coverage, lower-level capture helper cleanup, and
  the negative gate is green for the vacated operation home, shipped profile/lane
  surfaces, the removed `.memoria/mcp` adapter home, and all remaining dropped
  baseline surfaces. The #1126 cleanup pass added focused checks for deleted
  optional-adapter lane policy contracts (`LanePolicy`, `load_lane`,
  lane-overrides, and profile tool-registry allowlist semantics) without banning
  historical ADR context or optional-adapter prose. The #1128 cleanup pass
  finished the remaining non-policy `lane`/`task_id` shapes and removed the
  Hermes-backed post-commit hook; the negative gate now covers those deleted
  contracts too. The shipped fleet-health dashboard/lane-metric data source,
  deleted Zotero-specific import/operation contracts, shipped Obsidian/QuickAdd
  contracts, and generated profile mirror/template/harness contracts are already
  removed after #1122, #1123, #1124, and #1125. Portable-import reference edges
  are now part of SQLite graph evidence after #1139. Open Access fallback across
  multiple Unpaywall location URLs is now part of full-text acquisition evidence
  after #1140, and `oa_locations` payload coverage is part of that evidence
  after #1141. OpenAlex `primary_topic` import is now part of graph evidence
  after #1142. OpenAlex keyword graph edges and keyword gap evidence are now
  part of graph evidence after #1143. OpenAlex OA URL/location candidates are
  now part of full-text acquisition evidence after #1144. The pydantic-ai runner
  live-dispatch gate is part of runtime evidence after #1145. Project-scoped qmd
  retrieval expansion is part of Ask evidence after #1146. SQLite source-gap
  candidate routing is part of gap/discovery evidence after #1147. Project gap
  seeding from checked project/thesis terms is part of gap/discovery evidence
  after #1148. OpenAlex `concepts` graph import is part of graph evidence after
  #1149. Crossref relation graph import is part of graph/discovery evidence
  after #1150. Crossref full-text link acquisition is part of full-text
  acquisition evidence after #1151. Qmd embedding manifest evidence is part of
  retrieval/embedding evidence after #1152. Missing-full-text gap attention is
  part of gap/discovery evidence after #1153. OpenAlex authorship/institution/
  Source graph import is part of graph/retrieval evidence after #1154. OpenAlex
  MeSH/SDG topic import is part of graph/gap evidence after #1155.
  `knowledge/_views/index.md` tracked projection evidence is part of package/
  projection evidence after #1156. Universal `id`/`standing`/map-shaped `links`
  code evidence for `knowledge/` and `capabilities`, shared writer backfill/
  validation, and shipped operation capability conformance are part of code and
  package evidence after #1157.
- G2 Package gate: disposable workspace ships SQLite DB, blob dirs, OKF bundles,
  Node >=22 or bundled qmd, absolute qmd path, workspace-local qmd config/index
  state, registered `memoria-checked` collection, file capability manifests,
  `knowledge/_views/index.md` as a tracked generated projection, workspace
  provider config, and installs no Obsidian/Hermes/Zotero/profile material. The
  gate must validate the shipped `knowledge/` and `capabilities/` bundle roots
  and all shipped capability files against the universal frontmatter contract.
- G3 Runtime gate: attended or replay-based run proves installed dispatch, allowlist,
  qmd doctor/rebuild, scheduled wrappers, crash-before-materialization recovery,
  generated-projection direct-write quarantine, diagnostics/redaction, journal summaries, and
  checked/read-barrier behavior against a local OpenAI-compatible endpoint.
- G4 Product gate: CLI e2e evidence (`work capture` -> `work enrich` ->
  `work update` -> `work interview` -> `work digest` -> `note accept/link` ->
  `memoria ask` / `project ask` -> `project export` -> `workspace recover`),
  metadata-only/abstract-only digest blocking, project-scoped gap analysis that
  includes checked project argument/Toulmin health and checked SQLite source
  topic/research-area terms plus checked graph topic terms, portable import
  reference-edge evidence, Open Access full-text fallback evidence across
  multiple Unpaywall location URLs and `oa_locations`, parity fixture,
  OpenAlex primary-topic and keyword graph evidence, conflict/retraction
  attention, citation survival, and the seeded-error verdict.
- G5 Release gate: ADR routing, release-please readiness, and milestone cleanup.

## Known Limitations

- Limitation: sandbox-only; no installed-workspace migration target and no
  backward-compatibility mode. Impact: alpha.14 starts from fresh disposable workspaces
  only. Workaround: test only in `~/Memoria-test`. Tracking: alpha.14 parent.
- Limitation: provider breadth. Impact: alpha.14 requires qmd, full text,
  first-order citation/topic graph, embeddings, and discovery for the DOI/OA plus
  user-supplied URL/PDF/text baseline, but deeper provider-specific hardening and
  exhaustive graph expansion remain later.
  Workaround: use OpenAlex/Unpaywall/user-supplied text first; schedule provider
  hardening after the baseline gates pass. Tracking: alpha.14 follow-ups.
- Limitation: no Obsidian/Zotero/MCP product surface. Impact: the CLI is the
  only product surface this checkpoint; portable BibTeX/CSL files are the
  supported bibliography import path, and adapters are future optional edges.
  Workaround: operate via `memoria`, a plain editor, and generic bibliography
  files. Tracking: adapter follow-up.
- Limitation: re-enrichment cadence and workspace-export packaging beyond Markdown +
  `references.bib` remain open. Impact: they do not block the first checked work,
  digest, ask, project export, or recovery path. Workaround: keep volatile-field
  rechecks manual or scheduled explicitly until cadence is decided. Tracking:
  alpha.14 open questions.

## Candidate Checks

1. Required CI is green on `main`.
2. `scripts/verify pr` passes after code changes.
3. `scripts/verify package` passes after shipped workspace/manifest/projection
   changes, including `knowledge/_views/index.md`.
4. `scripts/verify runtime` or `scripts/verify rc` passes, or Runtime is skipped with a
   concrete environment reason.
5. `pip install -e .` then `memoria init`/`memoria doctor` succeeds against a fresh
   disposable workspace in `~/Memoria-test`, with Node >=22 or bundled qmd,
   absolute qmd path resolution, workspace-local qmd config/index state, and no
   personal workspace access.
6. The negative gate confirms no runtime/installer/current-doc path requires
   the vacated `.memoria/operations/` home, shipped profiles/lane overrides/tool
   registry, shipped `.memoria/mcp` adapter home, Hermes, Obsidian, Zotero-named
   import/operation surfaces, MCP, or legacy markdown Work paths. Runtime source
   authority uses SQLite catalog rows and blob content; lone
   `catalog/sources/<id>/source.md` files do not
   satisfy digest or integrity paths. The shipped fleet-health dashboard, Fleet
   nav target, and Memoria Inspector lane-metric/Fleet trust panel are absent and
   gate-protected. The Zotero-specific CLI format, worker operation, runtime
   helper, and capability path are absent and gate-protected after #1123. The
   shipped `.obsidian` tree, QuickAdd command scripts/buttons, plugin bundle,
   workspace JSON, and plugin provenance lock are absent and gate-protected after
   #1124; no package/smoke/current-doc path depends on them. Runtime policy no
   longer exposes lane/profile policy concepts: adapter policy is actor/workspace
   scoped, uses `request_id` for audit pairing, and loads from workspace-local
   policy config rather than lane overrides or a profile tool registry.
   `session_id` is not a request-id fallback. Current-doc references to Obsidian
   command palette/Agent Client/Local REST/vault launch surfaces and remaining
   dropped runtime concepts are either gone, historical ADRs, explicitly legacy
   eval data, or explicitly optional adapter notes.
7. Lower-level capture helper evidence proves `capture_source`,
   `capture_bibtex_source`, `capture_url_source`, and `capture_pdf_source` route
   through SQLite catalog/blob staging or are removed, and that capture does not
   write legacy Work files, legacy OpenAlex record markdown, or capture-time
   `references.bib`.
8. Product/manual evidence covers DOI success, metadata-only digest blocking,
   URL/PDF/user-text full-text
   capture, BibTeX/CSL DOI and ISBN imports, required-provider failure, conflict,
   retraction, qmd doctor/rebuild, full-text acquisition, abstract-only digest
   blocking, first-order citation/topic graph import, embeddings, discovery candidates,
   project-scoped `project gaps`, portable import reference edges, SQLite
   source-topic and graph-topic gap evidence, work update/interview/digest,
   note/link curation,
   generic/project ask, project export, crash-before-materialization recovery,
   checked Work ID evidence through integrity/read-barrier checks,
   generated-projection direct-write
   quarantine, universal OKF frontmatter validation for knowledge/capabilities,
   scheduled check evidence, diagnostics bundle/self-test, citation
   survival, the parity fixture, runner doctor evidence, and the seeded-error
   verdict bars.
9. Documentation integrity is complete: shipped behavior is covered, dropped surfaces
   are absent from current docs, and durable decisions are captured in ADRs.
10. `scratch/releases/0.1.0-alpha.14/` scratch is deleted from the `scratch` branch after
   durable content moves to ADRs, docs, release notes, or issues.

## Cut Or Checkpoint Close

1. No open release-milestone issue has `Readiness: Blocked`.
2. All release sub-issues are closed or explicitly rolled forward.
3. Formal release: merge the release-please PR. It owns version bump, changelog, tag, and
   GitHub Release notes.
4. Internal checkpoint: close the parent issue and milestone without tagging.
