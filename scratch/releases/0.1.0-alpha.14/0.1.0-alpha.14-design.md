# Memoria - 0.1.0-alpha.14 design (standalone CLI + engine)

Date: 2026-06-30
Status: working design for `0.1.0-alpha.14`. This design reconciles the best
parts of alpha.11, alpha.12, and alpha.13 into one CLI-first product direction.
It supersedes earlier scratch claims that make Obsidian, Zotero, Hermes, MCP, or
installed profiles part of the required architecture.

Method: carry forward the product thesis and integrity model from alpha.11, the
SQLite-backed engine and keep-test from alpha.12, the catalog enrichment and
clean concept boundary from alpha.13, and the working implementation primitives
that already prove useful behavior.

---

## 0. Decision

Memoria is a standalone local CLI tool and engine.

Required baseline:

- `memoria` CLI.
- Local workspace on disk.
- SQLite for catalog, operations, queue, journal, checks, and rollback graph.
- Markdown files for the user keep-set.
- Content-addressed blobs for raw/source/provider payloads.
- OpenAI-compatible model runner, local or remote, behind Memoria's runner API.
- Git CLI for checked file materialization and journal anchoring.

Outside the baseline:

- Obsidian.
- Zotero.
- Hermes.
- MCP servers.
- Installed profiles.
- A plugin, browser UI, companion app, hosted service, or always-on control
  plane.

Obsidian UI, Zotero import, or MCP/BYO-agent access may exist later only as
optional adapters around the CLI/engine. Hermes, installed profiles, lane/fleet
runtime, and `direct_api` as a product runner are dropped, not deferred. No
external adapter ever owns state, workflow, policy, checks, or the write
boundary.

Cutover rule:

- Alpha.14 is a full architecture cut, not another partial phase.
- Default to implementing the final baseline now.
- Defer only when alpha.14 cannot safely decide or implement the behavior with
  available evidence or local prerequisites.
- Every deferral names the temporary behavior, the gate that proves it, and the
  condition that removes it.
- No competing product paths. Old mechanisms are either ported behind the new
  CLI/engine boundary or dropped from the baseline.
- There is no production vault, so alpha.14 has no backward-compatibility mode,
  no migration path, and no legacy runtime branch. Existing compatibility code,
  tests, docs, and schema paths are removed or reclassified as historical/import
  notes before the design is called implemented.
- Current runtime, installer, schema, tests, and current docs must not carry
  alpha.11/alpha.12/alpha.13 compatibility branches. Historical scratch and ADR
  provenance may describe old layouts; product code may not support them.

---

## 1. Product Thesis

Carry forward from alpha.11:

Memoria is an opinionated personal thinking-and-writing tool. The human directs
the research program; the LLM and deterministic workers do labor under that
direction. Safety comes from traceability, checks, read barriers, and rollback,
not from requiring a human to approve every generated write.

The core loop stays:

```text
open project -> seed -> gap analysis -> capture source -> enrich source
-> guided conversation -> digest + hub compounding -> notes -> re-gap
```

The Co-PI is an engine behavior, not a UI dependency. In alpha.14 the first
surface is the CLI:

```bash
memoria ask <operation>
memoria answer <request-id> key=value
memoria resume <request-id>
```

An editor can still open the markdown files. The product must remain fully
operable if the editor is only `vim`, `nano`, or a file browser.

---

## 2. Architecture

```text
CLI       memoria commands, stdin/stdout, JSON mode, exit codes
Engine    request envelope, queue, worker, checks, read barrier, recovery
Ops       typed operation specs, pydantic-ai runner, deterministic helpers
Storage   SQLite SSOT for catalog + ops; Markdown keep-set; blob store
Export    references.bib, OKF-ish markdown takeout, optional JSONL reports
Adapters  future optional importers/UIs/APIs that call the CLI/engine
```

The engine is the product boundary. All surfaces create the same request
envelope and all writes pass through the same worker lifecycle.

Minimum request envelope:

```text
request_id
trigger_type: command | file_change | schedule
operation_id
args
idempotency_key
actor
target_path
target_hash
causal_refs
provenance
schedule_id?
```

Triggers:

- `command`: a `memoria ...` CLI invocation.
- `file_change`: `memoria scan` classifies direct edits to keep-set markdown.
- `schedule`: `memoria run-scheduled <job>` or an OS scheduler invoking CLI.

No trigger may write storage directly.

---

## 3. Storage And Source Of Truth

Carry forward alpha.12's keep-test:

> The thing the user keeps if Memoria dies must be markdown files plus a
> portable bibliography export. Memoria working state may be SQLite.

| Component | Source of truth | Projection / export |
| --- | --- | --- |
| Notes | Markdown under `knowledge/notes/` | SQLite read model |
| Digests | Markdown under `knowledge/digests/` | SQLite read model |
| Hubs | Markdown under `knowledge/hubs/` | SQLite read model |
| Projects | Markdown under `knowledge/projects/<id>/project.md` | dashboards/reports |
| Capabilities | directory-per-capability manifests under `capabilities/` | `capabilities/_generated/ai-catalog.json` |
| Catalog records | SQLite | `references.bib`, optional JSON/OKF export |
| Operations/journal/queue | SQLite | optional JSONL/report export |
| Raw/provider payloads | content-addressed blobs | hashes in SQLite |
| Search/indexes/views | derived | `knowledge/_views/` and rebuilt SQLite views |

There is no `catalog/` markdown source folder in the alpha.14 baseline. Sources,
people, organizations, venues, provider payloads, external IDs, and catalog
links are records in SQLite. The source's graph presence is its digest.

The portable takeout set is:

```text
knowledge/
capabilities/
references.bib
```

This is enough to keep thinking and writing outside Memoria for completed,
checked operations. It is not a full backup of Memoria's working state. A full
Memoria backup also needs SQLite and blobs.

---

## 4. Object Model

Use alpha.13's clean boundary, without making any UI part of the model.

| Class | Storage | Owns |
| --- | --- | --- |
| Records | SQLite | sources, entities, links, provider evidence, field provenance |
| Concepts | Markdown | digest, note, hub, project |
| Manifests | Markdown + sidecars | operations, tools, skills, workflows |
| Projections | Generated files | references, indexes, reports, catalogs |

Concept frontmatter baseline:

```yaml
id: <stable-id>
type: digest | note | hub | project
title: <human title>
check_status: unchecked | checked | quarantined
standing: current | superseded | retracted | archived
description: <one-sentence summary>
links: {}
tags: []
```

Concepts:

- `digest`: one source or source bundle's checked machine summary, keyed by
  `source_id`.
- `note`: one durable human-meaningful idea. `note_kind` remains an optional
  open string until enforcement needs diverge.
- `hub`: curated map of an area, with machine suggestions and human direction.
- `project`: output-oriented working memory: thesis, scope, decisions, gaps,
  outputs, and links.

Records:

- `sources`: identity, identifiers, metadata status, check status, CSL JSON,
  content/blob hashes, citekey aliases.
- `entities`: people, organizations, venues, and other catalog entities.
- `catalog_links`: source/entity/citation/version/dedup links.
- `external_ids`, `enrichment_runs`, `provider_payloads`,
  `field_provenance`.

Keep-set citation rule:

Any digest, note, or hub that references a source stores both `source_id` and a
compact survival citation:

```yaml
citations:
  - source_id: doi-10-example
    title: Example title
    authors: [A. Researcher]
    issued: 2026
    doi: 10.0000/example
```

SQLite owns full CSL and provenance. Markdown embeds enough to remain meaningful
without SQLite.

Capability manifest layout:

```text
capabilities/
  operations/<name>/capability.md
  tools/<name>/capability.md
  skills/<name>/capability.md
  workflows/<name>/capability.md
  _generated/ai-catalog.json
  _generated/index.md
```

Flat operation Markdown is not a compatibility path. The implementation either
loads the directory-per-capability contract or fails.

---

## 5. CLI Surface

The first complete surface is `memoria`.

Required command families:

```text
memoria init
memoria status
memoria capture-source
memoria enrich-source
memoria import-bibtex
memoria import-csl
memoria ingest
memoria ask
memoria answer
memoria resume
memoria scan
memoria compile-digest
memoria suggest-hub-updates
memoria propose-notes
memoria analyze-gaps
memoria run-pending
memoria run-scheduled
memoria recover
memoria rollback
memoria export
memoria doctor
```

CLI rules:

- Every mutating command creates a durable request row before work starts.
- Every command has `--vault`, `--json`, and stable nonzero exit codes.
- Commands import without Obsidian, Zotero, Hermes, MCP, profile env vars, or
  GUI state.
- `memoria status` is read-only.
- `memoria doctor` checks local prerequisites and never repairs without an
  explicit flag.
- The CLI is usable by a human and scriptable by future adapters.

The CLI is not "operator-only." It is the product surface for alpha.14.

The alpha.14 product loop is complete only when the CLI can run a thin end-to-end
cycle: import or capture a source, enrich it, conduct/resume the interview,
compile the digest, suggest hub updates, propose notes, run gap analysis, and
recover from an interrupted materialization.

---

## 6. Operation And Write Lifecycle

Carry forward alpha.12's lifecycle:

```text
requested -> stage -> validate -> check -> DB commit -> file materialize -> project
```

Steps:

1. `requested`: insert request, queue row, idempotency key, actor, and inputs.
2. `stage`: produce DB row candidates and durable file payloads.
3. `validate`: Pydantic shape checks plus SQLite FK/UNIQUE/CHECK/DAG checks.
4. `check`: structural and policy checks run before any machine output is
   consumable.
5. `DB commit`: write catalog/ops/journal/check rows and file payload metadata.
6. `file materialize`: write checked markdown/projections from durable payloads,
   commit/anchor them, then record `materialized`.
7. `project`: refresh indexes/views from SQLite + files.

Crash rule:

A DB commit with pending file materialization is not complete and not
consumable. `memoria recover` resumes from SQLite and the durable payload. A
hash-only pending row is invalid because it cannot replay a lost file payload.

Rollback:

- DB-backed rows roll back with inverse SQLite transactions.
- File-backed Concepts roll back with inverse materializations.
- Blast radius is a recursive graph query over catalog links, derivation edges,
  citations, and materialization records.

---

## 7. Runner Model

The runner is a Memoria implementation detail.

Baseline:

- pydantic-ai for typed LLM calls, structured output, validation, and retries.
- OpenAI-compatible endpoint shape for local models, gateways, or direct
  vendors.
- Operation policy pins model/provider/routing for checks and evals.
- Direct deterministic helpers are preferred whenever they work.

Rejected as baseline:

- Hermes runtime or Hermes-as-library.
- LangChain/LlamaIndex-style framework-owned state.
- Workflow engines.
- Agent session memory as source of truth.
- Provider auto-routing for calibrated checks.

Borrow patterns, not runtimes. The Karpathy wiki pattern is prompt/schema/workflow
material inside a Memoria operation, not a dependency on a wiki engine.

---

## 8. Catalog Capture And Enrichment

Carry forward alpha.13's capture/enrich split.

`capture-source`:

- Stores raw/content hashes, initial identifiers, minimal source row, request
  and journal rows.
- Leaves `metadata_status = partial | unverified`.
- Leaves `check_status = unchecked`.
- Does not update `references.bib` for DOI/ISBN sources.
- Does not create source/person/organization/venue markdown.

Portable seed imports:

- `import-bibtex` and `import-csl` accept local BibTeX/CSL files as standalone
  catalog seed inputs.
- They create unchecked source rows and queued enrichment work.
- They are not Zotero import paths, do not read a reference-manager database, and
  do not make citekeys the source of truth.

`enrich-source`:

- Resolves identifiers and source branch.
- Fetches required providers.
- Caches raw provider payloads as content-addressed blobs under
  `.memoria/blobs/provider-payloads/`.
- Normalizes provider records.
- Merges canonical CSL with per-field provenance.
- Reconciles identities with external IDs.
- Checks conflicts, required-provider coverage, and retraction/contested status.
- Promotes to consumable `checked` only after required checks pass.
- Stores durable `references.bib` payload/hash and materializes it before the
  operation completes.

Branches:

| Branch | Required for checked promotion | Optional / async |
| --- | --- | --- |
| DOI scholarly | Crossref; OpenAlex + Unpaywall unless explicitly degraded | S2, PubMed, arXiv, ORCID/ROR, DataCite, retraction hardening |
| ISBN book | Open Library or Google Books | other book provider, Wikidata |
| URL / primary source | URL metadata extraction | Wayback/archive, Wikidata |

Provider policy:

- `vault-template/.memoria/enrichment/providers.yaml` is data config for provider
  roles, timeouts, token buckets, required/optional sets, and env var names.
- The operation manifest is the hard ceiling for network access.
- Every HTTP call passes a shared operation allowlist check before network I/O.
- Secrets and mailto/email values live outside tracked config.

---

## 9. Integrity And Security

Carry forward alpha.11's integrity thesis, with alpha.12 mechanisms.

Guarantees:

- Attributable: every output has operation, request, input hashes, runner, model,
  prompt version, and materialization evidence.
- Reversible: rollback works by output/record, not whole run.
- Detected: structural checks run on write and on schedule.
- Read-barriered: machines consume only checked DB rows and checked +
  materialized file outputs.
- Audited: every allowed mutation records before/after evidence.

Read barrier:

```text
DB row:       check_status = checked
File output:  check_status = checked AND materialization_status = materialized
```

Anything missing a status fails closed for machine consumption.

Security controls:

- External text is untrusted data.
- Data/instructions are separated.
- Raw source text is never system prompt material.
- Network access is operation-scoped and allowlisted.
- Generation and checking are separate operation roles.
- A retracted or contested source is never silently checked.
- Capability import is supply chain and starts quarantined.
- Agent-rendered UI, if any later exists, is declarative and catalog-bound.

Residual risk:

No structural check proves semantic truth. Seeded-error evaluation remains the
gate to real use. If recall is weak for crafted injection or unwarranted claims,
add a targeted human checkpoint for that class.

---

## 10. Search, Gap Analysis, And Ask

The search/index layer is a rebuilt interface, not truth.

Baseline:

- SQLite FTS5 for text.
- Optional vector index if available.
- Hybrid retrieval over checked digests, notes, hubs, compact citations, and
  catalog read model.
- Reasoning-trace query expansion and offline enrichment only after a cheap
  baseline fails.

Gap analysis reads both:

- Catalog coverage.
- Knowledge graph coverage.
- Mismatch between source density and note/claim density.

Gap states:

- sources thin / notes thin: early area, propose seed.
- sources dense / notes thin: undigested corpus.
- sources thin / notes dense: under-warranted claim cluster.
- sources dense / notes dense: mature area.

Ask answer contract:

- cite sources;
- state unknowns;
- state stale/superseded evidence;
- state contradictions;
- state missing evidence.

A confident answer with no edges is a defect.

---

## 11. Packaging And Install

Alpha.14 install target:

```bash
python -m pip install -e .
memoria init --vault /path/to/workspace
memoria doctor --vault /path/to/workspace
memoria --help
```

Installer requirements:

- Create workspace skeleton.
- Create SQLite DB.
- Create blob directories.
- Create `knowledge/`, `capabilities/`, and generated projection targets.
- Install no Obsidian plugin.
- Install no Hermes profiles.
- Require no Zotero setup.
- Seed no external provider secrets.
- Make local model/provider config explicit and inspectable.

Runtime-local gate:

```bash
memoria doctor --vault /home/eranr/Memoria-test/vault-alpha14
memoria capture-source --vault /home/eranr/Memoria-test/vault-alpha14 --doi <doi>
memoria enrich-source --vault /home/eranr/Memoria-test/vault-alpha14 --source-id <id>
memoria ask --vault /home/eranr/Memoria-test/vault-alpha14 status
memoria seeded-error-verdict --vault /home/eranr/Memoria-test/vault-alpha14
```

The gate proves installed CLI wiring, local model dispatch when configured, write
boundary, denied direct write, audit rows, recovery, and the seeded-error verdict
bar before any non-sandbox use. It must not require generated prose to match.

Codebase cleanup requirements:

- Remove runtime lookup, schema, installer, docs, and tests for
  `catalog/sources/*/source.md` and catalog entity Markdown as supported state.
- Remove migration/backward-compatibility commands, flags, fallbacks, and tests
  for alpha.11/alpha.12/alpha.13 layouts.
- Remove Hermes/profile/Zotero/MCP assumptions from default install, verification,
  package metadata, and current user docs.
- Keep old-layout discussion only in scratch/provenance/ADR history, never as a
  runnable or documented current path.

---

## 12. What Carries Forward

From alpha.11:

- Opinionated personal thinking-and-writing identity.
- Direction, not authorship.
- Traceability, not per-write approval.
- Ingest loop: capture -> conversation -> digest/hub -> notes -> gaps.
- One graph with machine and human roles.
- Read barrier, cascade rollback, seeded-error verdict, baseline discipline.
- Human gate only at irreversible boundaries or empirically weak error classes.

From alpha.12:

- Keep-test for what must be markdown.
- SQLite SSOT for catalog and operations.
- `references.bib` as tracked bibliography takeout.
- Request envelope and uniform worker lifecycle.
- Cross-store materialization and recovery.
- pydantic-ai typed operation runner.
- Git CLI as boring file-materialization primitive.
- No event-sourcing or workflow engine.

From alpha.13:

- Capture/enrich split.
- No source/person/org/venue markdown Concepts.
- Records vs Concepts vs Manifests vs Projections.
- Provider payload cache, field provenance, required-provider policy.
- `source_id` link model plus compact citation survival payload.
- Directory-per-capability manifests with generated mirrors separated from
  authored capability truth.
- Universal Concept frontmatter for the clean workspace: `id`, `standing`, and
  map-shaped `links`.
- Final generated view locations such as `knowledge/_views/`; no compatibility
  path for legacy generated locations.
- DOI scholarly baseline first: Crossref, OpenAlex, Unpaywall.

From the current implementation:

- Trusted-writer mechanics: stage unchecked output, run checks, promote checked
  output, record before/after evidence, and commit only touched materializations.
- Operation manifests as data contracts and hard ceilings for tools, paths,
  network, runner, model, schema, risk, and required checks.
- OpenAI-compatible runner constraints: explicit endpoint/config, allowlist before
  network I/O, deterministic local test endpoint, and no framework-owned state.
  These constraints move into the pydantic-ai runner; `direct_api` does not stay
  as a competing product runner.
- Generated projection discipline for `references.bib`, workspace indexes,
  `knowledge/_views/`, and `capabilities/_generated/ai-catalog.json`.
- Deterministic ingest, extraction, identity-merge, degradation, and retraction
  logic, ported out of MCP/profile/Zotero assumptions.
- Existing worker/projection/policy test coverage, rewritten only where the new
  CLI + SQLite engine changes the contract.

Dropped from the baseline:

- Obsidian as required surface.
- Zotero as required source authority.
- Hermes as runtime, runner, adapter, profile system, or library contingency.
- MCP as required operation API.
- Installed profiles and lane/fleet runtime model.
- `direct_api` as a separate product runner.
- Backward-compatibility and migration branches for previous scratch designs.
- Legacy `catalog/sources/*/source.md` lookup or schema support in the runtime.
- Flat operation manifests as a supported runtime path.
- CLI-as-operator-only language.
- PI catalog actions waiting for an Obsidian UI.

---

## 13. Non-goals

- Obsidian plugin or UI.
- Zotero integration beyond future optional import.
- Hermes integration.
- MCP/BYO-agent API.
- Multi-device sync or multi-writer coordination.
- Turso/libSQL, pygit2/dulwich, or verifiable-log servers as baseline
  dependencies.
- ISBN/URL-primary depth beyond the contract.
- Semantic Scholar, PubMed, arXiv, citation graph, embeddings, topics, and
  full-text enrichment unless scheduled.
- Backward compatibility, production-vault migration, or compatibility mode for
  alpha.11/alpha.12/alpha.13 layouts.
- RO-Crate or BagIt archive wrappers.
- in-toto or SLSA attestations.

Deferred-boundary posture:

- Export stays deterministic and plain: keep-set Markdown, `references.bib`,
  optional JSON/JSONL reports, SQLite backup, and blobs. Add RO-Crate or BagIt
  only when there is an actual archive/deposit consumer.
- Capability supply-chain metadata stays small: source URL, SPDX license,
  content hashes, signed/unsigned status, and quarantine state. Add in-toto/SLSA
  only when third-party capability import becomes real.
- Broad citation graph, embeddings, topic expansion, and full-text acquisition
  are not alpha.14 product claims. Alpha.14 stores the hooks: provider payloads,
  external IDs, field provenance, OA/full-text locators, and enough checked source
  text for the first digest path. Turning any broad enrichments on requires a
  scheduled gate and a baseline eval that proves they beat simple retrieval.
- Local SQLite and the git CLI are the boring baseline. Turso/libSQL, pygit2,
  dulwich, and verifiable-log servers are considered only after a concrete
  failing baseline gate proves they are needed.

---

## 14. Pre-implementation Gates

Write small failing tests before implementation code:

- `memoria --help` imports without Obsidian, Zotero, Hermes, MCP, profiles, or
  GUI env vars.
- `memoria init` creates a usable workspace from scratch.
- DOI capture creates an unchecked SQLite source and no source markdown.
- DOI capture does not update `references.bib`.
- Enrichment required-provider success materializes `references.bib` before
  completion.
- Required-provider failure leaves source unchecked and creates attention.
- Provider endpoint enabled in config must be covered by operation
  `allowed_network`.
- Adapter HTTP bypassing shared allowlist fails closed.
- Provider payloads are content-addressed and deterministic remerge is stable.
- High-authority DOI/title conflict blocks checked promotion.
- Retraction/contested evidence blocks checked promotion.
- Keep-set source reference without compact `citations[]` fails validation.
- Crash after DB commit but before file materialization recovers from durable
  payload.
- Lookup, digest compilation, references, attention, rollback, and recovery work
  by `source_id` without `catalog/sources/*/source.md`.
- Directory-per-capability manifests load; flat operation Markdown does not.
- Universal Concept frontmatter requires `id`, `standing`, and map-shaped `links`.
- `regenerate-tracked-projections` emits final generated paths:
  `knowledge/_views/` and `capabilities/_generated/ai-catalog.json`.
- `import-bibtex` and `import-csl` seed unchecked source rows without Zotero and
  without source Markdown.
- The installed/runtime gate includes the seeded-error verdict before non-sandbox
  use.
- Runtime, installer, schemas, current docs, and tests contain no supported
  migration/backward-compatibility path for alpha.11/alpha.12/alpha.13 layouts.
- `rg` checks fail the gate if current runtime or installer paths still require
  Hermes profiles, Obsidian, Zotero, MCP, or `catalog/sources/*/source.md`.
- Export emits the plain alpha.14 takeout without RO-Crate/BagIt.
- Capability metadata records source URL, SPDX license, content hash, and
  quarantine/signed status without requiring in-toto/SLSA.
- Broad citation graph, embeddings, topic expansion, and broad full-text
  acquisition are absent unless a scheduled gate and baseline eval are added.
- Runtime-local installed gate proves CLI + local provider wiring without
  Obsidian, Zotero, Hermes, or external provider dependency.

---

## 15. Open Questions

- Exact first CLI syntax for multi-turn `ask` / `answer` / `resume`.
- First seeded-error bar values by class.
- Title-match confidence threshold for DOI resolution.
- Minimal checked source-text policy for the first digest path; broad full-text
  acquisition remains out of baseline.
- Re-enrichment cadence for volatile provider fields.
- Minimal export format beyond `references.bib` and markdown keep-set.

---

## Provenance

- Alpha.11 supplied the product identity, research loop, integrity thesis,
  knowledge model, security posture, and evaluation discipline.
- Alpha.12 supplied the keep-test, SQLite catalog/ops SSOT, worker lifecycle,
  read barrier mechanics, cross-store recovery, and typed operation runner.
- Alpha.13 supplied DOI capture/enrichment, provider provenance, retraction
  blocking, record/concept/projection separation, and catalog markdown removal.
- The current implementation supplied proven write-boundary mechanics, operation
  manifests, projection regeneration, deterministic ingest/retraction logic, and
  small runner constraints to port into the alpha.14 engine.
- This alpha.14 design resolves their surface/runtime drift: Memoria is a
  standalone CLI + engine first. External tools are optional edges, not the core.
