formulas:
  note: link(file.path, title)
properties:
  formula.note:
    displayName: Note
views:
  - type: table
    name: Sources
    filters:
      and:
        - file.inFolder("catalog/sources")
        - type == "source"
    order:
      - formula.note
      - check_status
      - description
      - resource
