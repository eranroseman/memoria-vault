type: source
category: catalog
gated: false
initial_check_status: unchecked
enums:
  check_status: [unchecked, checked, quarantined]
  lifecycle: [current, retracted, archived]
  item_type: [article, book, webpage, dataset, repository, software, audio, video, note, report]
  metadata_status: [verified, partial, unverified, not-indexed]
  text_status: [full-text, abstract-only, metadata-only]
required:
  type: literal:source
  check_status: enum:check_status
  title: str
  description: str
  source_id: str
optional:
  resource: str
  lifecycle: enum:lifecycle
  citekey: str
  item_type: enum:item_type
  identifiers: map
  csl_json: map
  raw_copy_path: str
  content_path: str
  raw_text_sha256: str
  normalized_text_sha256: str
  metadata_status: enum:metadata_status
  text_status: enum:text_status
  links: map
  tags: list
