type: space
category: spaces
gated: false
initial_lifecycle: current
enums:
  lifecycle: [current, archived]
  space: [library, knowledge, project]
required:
  type: literal:space
  lifecycle: enum:lifecycle
  title: str
  space: enum:space
optional:
  sample: bool
  created: date
