type: organization
category: catalog
gated: false
initial_lifecycle: current
enums:
  lifecycle: [current, archived]
required:
  type: literal:organization
  lifecycle: enum:lifecycle
  name: str
optional:
  subtype: str # lab · university · company · funder …
  location: str
  relationships: map
  sample: bool
  created: date
