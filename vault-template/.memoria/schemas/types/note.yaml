type: note
category: notes
gated: false
enums:
  mode: [claim, question]
  question_status: [open, resolved]
  certainty: [reported, contested, unknown]
required:
  type: literal:note
  id: ulid
  title: str
  tags: list
  links: links
optional:
  aliases: list
  annotation_ref: map
  archived: bool
  citations: list
  claim_text: str
  description: str
  evidence_set: list
  extraction_confidence: str
  mode: enum:mode
  question_status: enum:question_status
  quote: str
  source_id: str
  temporal_scope: str
  tense: str
  topics: list
  qualifier: str
  certainty: enum:certainty
  superseded: bool
  reading: str
  anchors: list
  x: map
