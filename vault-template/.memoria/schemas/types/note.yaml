type: note
category: knowledge
gated: false
initial_check_status: unchecked
enums:
  check_status: [unchecked, checked, quarantined]
  note_status: [candidate, accepted, rejected, superseded, needs_review]
required:
  type: literal:note
  check_status: enum:check_status
  title: str
optional:
  description: str
  resource: str
  links: map
  quote: str
  text_sha256: str
  source_id: str
  annotation_ref: map
  claim_text: str
  qualifier: str
  tense: str
  temporal_scope_start: date
  temporal_scope_end: date
  status: enum:note_status
  evidence_set: list
  extraction_confidence: str
  tags: list
