type: note
category: knowledge
gated: false
enums:
  mode: [claim, question]
  question_status: [open, resolved]
required:
  type: literal:note
  id: ulid
  title: str
  tags: list
  links: map
optional:
  aliases: list
  archived: bool
  description: str
  mode: enum:mode
  question_status: enum:question_status
  temporal_scope: map
  tense: str
  qualifier: str
  anchors: list
  x: map
