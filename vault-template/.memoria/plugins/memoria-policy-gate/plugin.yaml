name: memoria-policy-gate
version: 0.1.0-alpha.1
description: Route vault writes through the Memoria policy gate (fail-closed); block deny/dry_run zones in every run mode.
provides_hooks:
  - pre_tool_call
  - post_tool_call
