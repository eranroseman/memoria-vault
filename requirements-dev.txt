# Python dependencies for Memoria contributor tooling and CI.
# Runtime/vault dependencies live in src/.memoria/mcp/requirements.txt.

pre-commit==4.6.0
pytest==9.0.3
PyYAML==6.0.3
ruff==0.15.15
yamllint==1.38.0
