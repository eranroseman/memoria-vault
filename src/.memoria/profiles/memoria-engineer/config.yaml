# Generated by scripts/render_profile_configs.py; edit tool-registry.yaml.
_config_version: 30
context_file_max_chars: 30000
model:
  provider: '{{MODEL_PROVIDER}}'
  base_url: '{{MODEL_BASE_URL}}'
  default: '{{MODEL_DEFAULT}}'
  # {{MODEL_LOCAL_CONTEXT}}
mcp_servers:
  policy:
    command: '{{PYTHON}}'
    args: ['{{VAULT_PATH}}/.memoria/mcp/policy_mcp.py', --vault, '{{VAULT_PATH}}']
    timeout: 30
    tools:
      include: [check_permission, complete_write]
  obsidian:
    url: https://127.0.0.1:${OBSIDIAN_MCP_PORT}/mcp
    ssl_verify: ${OBSIDIAN_MCP_SSL_VERIFY}
    timeout: 30
    headers:
      Authorization: Bearer ${OBSIDIAN_API_KEY}
tools:
  tool_search:
    enabled: auto
memory:
  memory_enabled: false
  user_profile_enabled: false
  write_approval: true
  provider: ''
skills:
  write_approval: true
  guard_agent_created: true
curator:
  enabled: false
platform_toolsets:
  cli: [skills, kanban, policy, obsidian]
  cron: [skills, kanban, policy, obsidian]
  api_server: [skills, kanban, policy, obsidian]
  telegram: [skills, kanban, policy, obsidian]
  discord: [skills, kanban, policy, obsidian]
  slack: [skills, kanban, policy, obsidian]
  whatsapp: [skills, kanban, policy, obsidian]
  whatsapp_cloud: [skills, kanban, policy, obsidian]
  signal: [skills, kanban, policy, obsidian]
  bluebubbles: [skills, kanban, policy, obsidian]
  email: [skills, kanban, policy, obsidian]
  homeassistant: [skills, kanban, policy, obsidian]
  mattermost: [skills, kanban, policy, obsidian]
  matrix: [skills, kanban, policy, obsidian]
  dingtalk: [skills, kanban, policy, obsidian]
  feishu: [skills, kanban, policy, obsidian]
  wecom: [skills, kanban, policy, obsidian]
  wecom_callback: [skills, kanban, policy, obsidian]
  qqbot: [skills, kanban, policy, obsidian]
  yuanbao: [skills, kanban, policy, obsidian]
  teams: [skills, kanban, policy, obsidian]
  google_chat: [skills, kanban, policy, obsidian]
agent:
  tool_use_enforcement: true
  disabled_toolsets: [browser, clarify, code_execution, computer_use, cronjob, delegation, file, homeassistant, image_gen, memory, messaging, moa, session_search, spotify, terminal, tts, video, video_gen, vision, web, x_search, yuanbao]
terminal:
  cwd: '{{VAULT_PATH}}'
plugins:
  enabled: [memoria-policy-gate]
