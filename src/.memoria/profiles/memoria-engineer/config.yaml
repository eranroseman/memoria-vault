# Generated by scripts/render_profile_configs.py; edit tool-registry.yaml.
_config_version: 30
context_file_max_chars: 30000
model:
  provider: '{{MODEL_PROVIDER}}'
  base_url: '{{MODEL_BASE_URL}}'
  default: '{{MODEL_DEFAULT}}'
  # {{MODEL_LOCAL_CONTEXT}}
mcp_servers:
  policy:
    command: '{{PYTHON}}'
    args: ['{{VAULT_PATH}}/.memoria/mcp/policy_mcp.py', --vault, '{{VAULT_PATH}}']
    timeout: 30
    tools:
      include: [check_permission, complete_write]
  obsidian:
    url: https://127.0.0.1:${OBSIDIAN_MCP_PORT}/mcp
    ssl_verify: ${OBSIDIAN_MCP_SSL_VERIFY}
    timeout: 30
    headers:
      Authorization: Bearer ${OBSIDIAN_API_KEY}
tools:
  tool_search:
    enabled: auto
memory:
  memory_enabled: false
  user_profile_enabled: false
  write_approval: true
  provider: ''
skills:
  write_approval: true
  guard_agent_created: true
curator:
  enabled: false
platform_toolsets:
  cli: [skills, policy, obsidian]
  cron: [skills, policy, obsidian]
  api_server: [skills, policy, obsidian]
  telegram: [skills, policy, obsidian]
  discord: [skills, policy, obsidian]
  slack: [skills, policy, obsidian]
  whatsapp: [skills, policy, obsidian]
  whatsapp_cloud: [skills, policy, obsidian]
  signal: [skills, policy, obsidian]
  bluebubbles: [skills, policy, obsidian]
  email: [skills, policy, obsidian]
  homeassistant: [skills, policy, obsidian]
  mattermost: [skills, policy, obsidian]
  matrix: [skills, policy, obsidian]
  dingtalk: [skills, policy, obsidian]
  feishu: [skills, policy, obsidian]
  wecom: [skills, policy, obsidian]
  wecom_callback: [skills, policy, obsidian]
  qqbot: [skills, policy, obsidian]
  yuanbao: [skills, policy, obsidian]
  teams: [skills, policy, obsidian]
  google_chat: [skills, policy, obsidian]
agent:
  tool_use_enforcement: true
  disabled_toolsets: [browser, clarify, code_execution, computer_use, cronjob, delegation, file, homeassistant, image_gen, memory, messaging, moa, session_search, spotify, terminal, tts, video, video_gen, vision, web, x_search, yuanbao]
terminal:
  cwd: '{{VAULT_PATH}}'
plugins:
  enabled: [memoria-policy-gate]
