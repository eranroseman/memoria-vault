type: candidate
category: inbox
gated: false
enums:
  lifecycle: [proposed, current, archived] # proposed = awaiting the PI (ADR-50/D18)
  certainty: [confident, likely, unsure] # 3-level, calibrated (D35/D49)
  loudness: [quiet, notice, alert, block]
required:
  type: literal:candidate
  lifecycle: enum:lifecycle
  title: str
  action: str # what you'd be accepting
  argument_for: str
  argument_against: str # the agent's honest self-rebuttal
  what_tipped_it: str
  certainty: enum:certainty
optional:
  citekey: str
  url: str
  raised_by: str
  loudness: enum:loudness
  created: date
