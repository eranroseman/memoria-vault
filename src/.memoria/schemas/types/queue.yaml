type: queue
category: spaces
gated: false
initial_lifecycle: current
enums:
  lifecycle: [current, archived]
required:
  type: literal:queue
  lifecycle: enum:lifecycle
  title: str
optional:
  sample: bool
  created: date
