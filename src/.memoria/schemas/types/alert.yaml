type: alert
category: inbox
gated: false
initial_lifecycle: proposed
enums:
  lifecycle: [proposed, current, archived]
  agent_recommendation: [inconclusive, issues-found, clean]
  loudness: [quiet, notice, alert, block]
required:
  type: literal:alert
  lifecycle: enum:lifecycle
  title: str
  finding: str
optional:
  target: str
  agent_recommendation: enum:agent_recommendation
  raised_by: str
  loudness: enum:loudness
  sample: bool
  created: date
