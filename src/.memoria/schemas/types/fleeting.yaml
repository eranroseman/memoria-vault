type: fleeting
category: notes
gated: false
enums:
  lifecycle: [proposed, archived]
  origin: [human, agent, chat]  # chat = ACP-pane session export (notes/fleeting/chats/)
required:
  type: literal:fleeting
  lifecycle: enum:lifecycle
  origin: enum:origin
optional:
  title: str
  sample: bool
  created: date
