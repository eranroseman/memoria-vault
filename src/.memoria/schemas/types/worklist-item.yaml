type: worklist-item
category: system
gated: false
enums:
  lifecycle: [proposed, current, archived]
  decision: [proposed, include, exclude, maybe, archived]
required:
  type: literal:worklist-item
  lifecycle: enum:lifecycle
  title: str
  decision: enum:decision
  worklist: str
  item_ref: str
optional:
  source_report: str
  group: str
  rank: int
  reason: str
  sample: bool
  created: date
