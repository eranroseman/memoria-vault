type: paper
category: catalog
gated: false
enums:
  lifecycle: [current, retracted, archived]
  ingest_status: [tier0, enriched, complete, needs-human]
required:
  type: literal:paper
  lifecycle: enum:lifecycle
  citekey: str
  title: str
optional:
  ingest_status: enum:ingest_status
  doi: str
  authors: list
  year: int
  venue: str
  url: str
  relationships: map # given edges — cited_by, authored_by, published_in… (ADR-52)
  research_area: list
  methodology: list
  sample: bool
  created: date
