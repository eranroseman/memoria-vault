type: space
category: spaces
gated: false
enums:
  lifecycle: [current, archived]
  space: [inbox, library, knowledge, project]
required:
  type: literal:space
  lifecycle: enum:lifecycle
  title: str
  space: enum:space
optional:
  sample: bool
  created: date
