---
title: "{{VALUE:thought}}"
type: fleeting
lifecycle: proposed
origin: human
created: {{DATE:YYYY-MM-DD}}
---

# Capture

The thought, quote, or idea — one item per note. Raw is fine; distill later or archive.

Origin values are `human`, `agent`, or `chat`; `chat` is reserved for ACP session exports stamped by the sweep.
