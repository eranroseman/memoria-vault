# Drift Watch

Active and imminent drift — the Linter's and the sweeps' open findings (`flag` /
`alert` cards still in `proposed`). HIGH findings also surface in the queue. [Dashboard rationale](https://eranroseman.github.io/memoria-vault/explanation/dashboards/structural-health/drift-watch).

![[inbox.base#Drift watch]]
