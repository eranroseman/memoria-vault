type: code-artifact
category: projects
concept_type: project
required:
  type: literal:code-artifact
  id: str
  title: str
  tags: list
  links: links
  artifact_id: str
  purpose: str
  approved_command: list
optional:
  archived: bool
  declared_inputs: list
  declared_outputs: list
  dependency_notes: str
  generated: map
  output_dir: str
  source_dir: str
  sources: list
  usage_window: map
  verified: list
  x: map
