# Implementation status

The design docs in `docs/` describe the **intended** Memoria system. The starter vault (under `vault/` in this repo) is a **v0.1 scaffold** — some components are shipped and present in the vault, and a significant portion of the v0.1 design is pending implementation. Nothing is approved yet. This ledger is the single source of truth for the build state of every artifact, so a gap reads as a known status — not a documentation error. For the phase-by-phase activation sequence — when each component is wired, configured, or first run — see [release-plan-v0.1-spillover.md](release-plan-v0.1-spillover.md).

When a design doc describes an artifact, tag it with one of the status values below (inline, e.g. *(status: pending)*). When in doubt, this ledger wins.

## Status values

| Value | Meaning |
| --- | --- |
| **approved** | In `vault/`, tested, and verified to work end-to-end. |
| **shipped** | In `vault/`, but not yet verified end-to-end (dependencies missing or testing not done). |
| **pending** | Designed and required — but not yet built. A build gap; the requirement stands. |
| **deferred** | Consciously excluded from this release. A scope decision; absence is intentional. |

Authoritative runtime signal: `scripts/install.ps1` requires `SOUL.md` + `config.yaml` + `mcp.json` + `distribution.yaml` per profile. All four now ship for all seven profiles (the profile wiring landed — it is part of v0.1, per the glossary's "complete system" definition; it was never a separate version), so the installer no longer skips them — it will stage, substitute `{{VAULT_PATH}}`, and call `hermes profile install`. If the installer and a design doc disagree, the installer is right and the doc needs a status tag.

## Profiles and control plane

| Artifact | Status | Notes |
| --- | --- | --- |
| 7 profile `SOUL.md` prompts (`.memoria/profiles/memoria-*/`) | shipped | Present for all seven (`coder/librarian/linter/mapper/socratic/verifier/writer`). `config.yaml` + `mcp.json` + `distribution.yaml` now ship alongside (below), so profiles are installable. Not yet approved: needs an end-to-end run against a registered profile + the `obsidian` MCP bridge (below). |
| Per-profile `cron/` and `skills/` placeholder dirs | shipped | Empty (`.keep`) for all 7 profiles. Linter and Mapper also ship `cron/scheduled.yaml` with real content. |
| `cron/scheduled.yaml` for Linter and Mapper | shipped | Actual scheduled task definitions present in `.memoria/profiles/memoria-linter/cron/` and `memoria-mapper/cron/`. Other profiles have empty `.keep` only. |
| Per-profile `config.yaml` (×7) | shipped | Full `model` block only (Hermes replaces config sections wholesale — a partial block would wipe the global `agent`/toolset settings, so everything else inherits from `~/.hermes/config.yaml`). `provider: kilocode` + `base_url: https://api.kilo.ai/api/gateway` (matches the user's gateway); `default: ~anthropic/claude-{sonnet,opus,haiku}-latest`. Tiers: Linter/Librarian/Coder→Haiku, Mapper/Writer→Sonnet, Socratic/Verifier→Opus. Recommended `reasoning_effort` per profile noted in comments. Also carries a `hooks.pre_tool_call` block registering the policy gate (`policy_hook.py`); `scripts/install.ps1` now substitutes `{{VAULT_PATH}}` in `config.yaml` as well as `mcp.json`. Model alias strings confirmed against the KiloCode catalog (2026-05-31). Not yet verified live: per-profile `hooks` honoring; end-to-end run. Auxiliary models (split: `z-ai/glm-4.7-flash` for light slots, `deepseek/deepseek-v4-flash` for compression's 1M-ctx headroom) are set in the global `~/.hermes/config.yaml`, not per-profile — see [hermes/configuration.md](../../docs/how-to-guides/using-hermes-agent/configuration.md). |
| Per-profile `mcp.json` (×7) | shipped (partial) | Top-level `mcp_servers` (per Hermes docs). Wires `policy` (stdio: `python {{VAULT_PATH}}/.memoria/mcp/policy_mcp.py --vault {{VAULT_PATH}}`) and `obsidian` (`uvx mcp-obsidian`, env `OBSIDIAN_API_KEY`/`OBSIDIAN_HOST=127.0.0.1`/`OBSIDIAN_PORT=27124`). Valid JSON after substitution. `tasks` server intentionally absent (the native Hermes `kanban_*` tools cover it — see `tasks_mcp.py` row). **`${OBSIDIAN_API_KEY}` in the `env` block:** the Tier-4 "does NOT substitute" finding was **reversed on re-investigation (2026-06-01)** — current Hermes *does* interpolate `${VAR}` in MCP `env` values (`tools/mcp_tool.py`, main). The shipped form is correct and **unchanged**; the 401 is a version/`.env`-shadow issue, re-test pending — see the `obsidian` MCP server row + [#39](https://github.com/eranroseman/memoria-vault/issues/39). Still unverified: `mcp-obsidian` bridge choice, HTTPS self-signed cert, per-profile read-only `tools` filters; plus the write-gating gap (below, [#51](https://github.com/eranroseman/memoria-vault/issues/51)). |
| Per-profile `distribution.yaml` (×7) | shipped | Manifest per the live Hermes profile-distribution schema: `name`/`version: 0.1.0`/`description`/`author`/`hermes_requires: ">=0.12.0"`/`env_requires`. Verified parse + `name` matches dir. **Version convention:** the profile package `version` tracks the Memoria release version (both `0.1.0`) — they are kept in lockstep, not versioned independently, since the seven profiles ship and install together (`--force`). The separate `hermes_requires` is the Hermes *runtime* dependency, not a Memoria version. |
| Per-profile `.env.EXAMPLE` | shipped | **Generated by `hermes profile install` from `distribution.yaml` `env_requires`** (not hand-authored), then copied to `.env` by `scripts/install.ps1`. Required secrets: `KILOCODE_API_KEY` (model access — the shipped `provider: kilocode`), `OBSIDIAN_API_KEY` (all); `OPENALEX_API_KEY` (Librarian — required by OpenAlex since 2026-02); optional `ANTHROPIC_API_KEY` (only if you swap to `provider: anthropic`), `S2_API_KEY`/`NCBI_API_KEY`/`NCBI_EMAIL` (Librarian; Zotero needs no key — `pyzotero` uses the local desktop API), `SCITE_API_KEY` (Verifier), `GITHUB_TOKEN`/`OPENAI_API_KEY` (Coder) per profile. |
| `obsidian` MCP server (vault-access bridge) | **approved** (live write confirmed, Tier-4 — [#39](https://github.com/eranroseman/memoria-vault/issues/39) RESOLVED) | Wired in every `mcp.json` as `uvx mcp-obsidian` against coddingtonbear's Local REST API plugin (port 27124). **Tier-4 (2026-06-01, live Hermes v0.14.0 on WSL2):** a `memoria-writer` agent run wrote `10-inbox/02-answers/…` through the bridge — HTTP 204, file on disk, read back OK. The earlier 401 was **neither** an interpolation defect **nor** a version issue: **Hermes profile runs read ONLY the profile's own `.env`, never the global `~/.hermes/.env`** (docs: "each profile has its own `.env`"; no global inheritance — `hermes -p <p> config env-path` resolves to the profile file). `${OBSIDIAN_API_KEY}` interpolated to empty because the key sat only in the global `.env`. **Fix landed:** the installer now seeds shared keys into each profile `.env` (`seed_profile_env`, see the `install.sh` row). The shipped `"${OBSIDIAN_API_KEY}"` form is correct and unchanged. Still unverified: HTTPS self-signed cert handling, read-only `tools` filters. |
| `.memoria/mcp/policy_hook.py` | shipped | The **shared decision/audit core** for the write gate (reuses `policy_mcp.PolicyEngine`): maps tool→action, blocks `deny`/`dry_run`, passes reads, normalizes `file`-tool paths to vault-relative (`to_vault_relative`; writes outside the vault proceed — the gate governs vault zones only), and handles both events (`evaluate_pre` gates; `evaluate_post` appends the paired `write_complete` after_hash record). `python policy_hook.py --self-test` passes (**all checks green**, incl. file-toolset gating + pre→post roundtrip); the self-test stands and remains the **Phase 1 gate** (must pass before any profile runs against the production vault). **The live gate is the `memoria-policy-gate` Python plugin ([ADR-28](../decisions/28-write-gate-as-plugin.md))**, which calls this module's `evaluate_pre`/`evaluate_post`, is enabled per lane via `plugins.enabled`, deployed by the installer's `deploy_policy_plugin`, and is **fail-closed** (any gate error blocks). It gates two write families via `classify`: the `obsidian` MCP writes (all lanes) and the Hermes `file` toolset writes (`write_file`/`patch`) on **Coder + Linter** (the two lanes that keep terminal+file). The capability layer ([ADR-27](../decisions/27-hermes-native-config-and-gate-enforcement.md)) backstops it: the installer disables the `terminal`+`file` toolsets (`agent.disabled_toolsets`) for the 5 non-terminal lanes per `tool-registry.yaml`, leaving obsidian the only write path. **The old `pre_tool_call` shell-hook entrypoint is retired** — it never fired on live writes: Hermes registers the obsidian tool as `mcp_obsidian_obsidian_append_content`, the shell-hook matcher uses `re.fullmatch`, so `"obsidian.*"` matched nothing (and shell hooks are consent-gated + fail-open). **Validated live (installer-deployed plugin, librarian + writer, `hermes -z`):** allowed write → `allow`+`write_complete`; review-gated/denied write → blocked, no file on disk, no filesystem fallback; simulated policy outage → fails **closed**. The gate fires in oneshot/gateway/cron/ACP (all share `run_agent.AIAgent` + plugin dispatch); **#58 resolved.** |
| `.memoria/mcp/policy_mcp.py` | shipped | Policy write-gate implemented against [policy.md](../../docs/reference/policy-mcp.md): four-decision protocol, eight-action vocabulary, default-deny, review-gated `dry_run` degrade, Linter `auto_fix` class-gating, SHA-256 audit hashing, and skill-conditional one-way narrowing. Dependency-light decision core passes `python policy_mcp.py --self-test` (34 checks) and `--decide` one-shots against all seven shipped lane-overrides. **Phase 1 gate: both self-tests (34 + 32 checks) must pass before any profile runs against the production vault.** Not approved: needs the `mcp` SDK installed, live `hermes` MCP registration (via `mcp.json`), and a real worker exercising it end-to-end. |
| `.memoria/mcp/requirements.txt` | shipped | `mcp>=1.2.0` (FastMCP server wrapper) + `PyYAML>=6.0` (lane-override parsing). `scripts/install.ps1` pip-installs it before profile registration. |
| `.memoria/mcp/tasks_mcp.py` | deferred (not needed) | The board is the **native Hermes Kanban**; workers use the built-in `kanban_*` tools + `hermes kanban` CLI (`reference/kanban-board.md` rejects a separate task registry). Board *visibility* is handled by `board_export.py`. No `tasks` MCP server is needed. |
| `.memoria/mcp/board_export.py` | shipped | Projects the live Hermes board to `99-system/board/<task_id>.md` (per-card frontmatter) + the operational logs `board-state.jsonl` (per-lane counts), `board-transitions.jsonl` (card status/review changes), `disposition.jsonl` (accept : edit : reject — un-backfillable), `cost.jsonl` (API spend + tokens at completion). Diffs against an internal `.board-state-cache.json` so first-sight cards seed silently and only real movements log. Reads `hermes kanban list --json` (`--from-json` for tests). `--self-test` passes (26 checks). Schemas: [reference/telemetry.md](../../docs/reference/telemetry.md). **Cron wired (G5, 2026-06-02):** the installer's `wire_telemetry_cron` deploys `board-export-cron.sh` to `~/.hermes/scripts/` and runs `hermes cron create --no-agent` (1-min cadence); validated live (forced tick → fresh `board-state.jsonl` snapshot). Not approved: full `transitions`/`disposition`/`cost` emission needs live card activity to exercise, and the Hermes scheduler/gateway must be running for the cron to fire. |
| `.memoria/mcp/metrics_aggregate.py` | shipped | Rolls `audit.jsonl` (deny rate) + the board (success/retry rate) + `lint-findings.jsonl` (drift) + `disposition.jsonl` (accept-ratio) + `board-transitions.jsonl` (median decision time) + `cost.jsonl` (spend/tokens) into `99-system/metrics/lane-<lane>-<period>.md` trust-score notes (bands 90+/70-89/<70). `--self-test` passes (20 checks). Not approved: `skill-metric` + CRS `pass^k` need repeated-run data (`consistency_passk` ships as a `null` placeholder); needs cron + real weekly volume. **Phase 3: cron wiring waits for sufficient run volume; trust-score bands are meaningless on sparse data.** |
| `.memoria/lane-overrides/{lane}.yaml` (×7) | shipped | All seven lane-override YAMLs present (`coder/librarian/linter/mapper/socratic/verifier/writer`). `policy_mcp.py` parses and enforces all seven (verified via `--decide`). Writer's review-gated *draft* paths (`30-synthesis/02-reference/**`, `50-deliverables/**`) moved from `deny.write` → `allow.write` so they degrade to `dry_run` per the matrix; human-owned `01-claims`/`03-moc` stay hard-`deny` (verified). Full approval needs live enforcement against a registered profile. |
| `.memoria/project-hints.yaml.example` | shipped | Present in `.memoria/` root. Copy-on-install semantics analogous to Hermes's `.env.EXAMPLE` (which stays uppercase — upstream-generated; Memoria-authored examples use lowercase `.example`). |
| `.memoria/csl/` | shipped | Empty placeholder (`.keep`) for CSL citation style files. |
| `.memoria/memoria.bib` | shipped (empty stub) | Ships as a comment-only `.bib` so the obsidian-citation-plugin loads without a "file not found" warning. Better BibTeX overwrites it on first auto-export; the Librarian + citation plugin read it at runtime. |
| `.memoria/tool-registry.yaml` | shipped | Per-profile tool allowlist (default-deny) with capability groups (`vault_read`/`vault_write`/`web`/`policy`); schema Memoria-defined. Cross-checked: `vault_write` capability matches each lane's write-scope (only Socratic lacks both). **Partial machine enforcement landed ([#51](https://github.com/eranroseman/memoria-vault/issues/51)):** the installer now realizes the registry's terminal/file capability split via `agent.disabled_toolsets` (only Coder + Linter keep `terminal`+`file`). Still a follow-up: enforcing the rest of the registry (web/browser per lane) and the Linter drift-check of each profile's effective toolset against its registry entry. |
| Per-profile skills (the `lane-overrides` allowlists name 28) | shipped | **Verified 2026-05-31** against K-Dense + the official Hermes registry (`NousResearch/hermes-agent/skills/`). Only **7 are installable**: 5 K-Dense (`paper-lookup`, `pyzotero`, `citation-management`, `literature-review`, `scientific-writing`) via `git clone …K-Dense-AI/scientific-agent-skills` → `~/.hermes/skills/` (not `hermes skills install`), plus official `research/arxiv` + `research/llm-wiki` (lane-overrides name them `arxiv-search`/`llm-wiki-draft` — drift to reconcile). The other **20 are Memoria-coined and do not exist in any registry.** Decision (2026-05-31): **adapt the design — no wrappers.** Lane-overrides point at the real skill IDs and orchestration lives in `SOUL.md`; only deterministic pipelines stay real skills. Breakdown: **3 prompt-only** (→ `SOUL.md`); **6 direct-use** (lane-override names the real skill, e.g. `obsidian-markdown`, `autonomous-ai-agents/codex`, `github-repo-management`, `scikit-learn`+`umap-learn`, `qmd`); **2 different mechanism, both done 2026-05-31** (`scaffold-code-note` → the `Memoria: scaffold code note` QuickAdd choice; `graph-analyze` → the `graph_analyze` `detectors.py` function, pure stdlib, no `networkx`); **3 already in `detectors.py`** (`schema-check`, `health-report`, and now `graph-analyze` — Linter `allow.skills: []`, runs via terminal); **4 Memoria logic** as `SOUL.md` procedures (`session-log`, `scope-project`, `gap-report`, `cite-check`; the per-source `comparative-brief`/`[!brief]` is folded into the Librarian's `obsidian-paper-note` skill, since only the Librarian writes `20-sources/`); **2 authored as real (thin) skills, done 2026-05-31** — `obsidian-paper-note` (in `memoria-librarian/skills/`, now also composing the `[!brief]`) + `retraction-check` (in `memoria-verifier/skills/`). So **all skill work is complete**: lane-override + `SOUL.md` edits in place, the 2 skills authored, the rest provisioned by the bootstrap's skills step. See [bootstrap-installer § skills](../../docs/reference/installer.md). |
| ADR-30 ingest pipeline (`memoria-librarian/skills/obsidian-paper-note/scripts/`) | shipped (not operable) | **5 deterministic scripts landed #93–#100 under [ADR-30](../decisions/30-deterministic-ingest-pipeline.md) (still `proposed`):** `ingest_paper.py` (Tier-0 — `captured` stub + un-gated `capture-intake.jsonl` durability anchor, 330 ln), `resolve_merge.py` (Tier-1 — S2→OpenAlex→Crossref fallback-chain resolve + multi-source merge, 379 ln), `extract.py` (Tier-1 full-text extraction, 155 ln), `link.py` (Tier-1 deterministic linking, 187 ln), `pipeline.py` (orchestrator, 141 ln). All five pass `python <script> --self-test`. **Not operable (release-plan G9 ingest value loop, `in-progress`):** the two LLM steps (classify against `vocabulary.md`; the `[!brief]`) are unbuilt, `pipeline.py` writes nothing (gated writes not wired), and the capture glue (`capture-from-zotero.js`) + one end-to-end run remain. Hardening owed before reliance — see the round-2 red-team (R2-1 multi-source merge correctness, R2-2 serialization on the manual re-ingest path). ADR-30 stays `proposed` until that spike. |
| Linter `detectors.py` + `structural-detectors.md` | shipped | `detectors.py` ships **9 deterministic stdlib (zero-LLM) checks** (`DETECTORS` list): orphan-working-files, stale-fleeting, stale-answer-drafts, extract-path-broken, frontmatter-schema-check, broken-wikilinks, dashboard-field-drift, graph-analyze, fama-exposure (`--self-test` passes 15/15). `structural-detectors.md` documents the Linter's **8 *structural* drift checks** — a **different set**: only 3 (dashboard-field-drift, orphan-working-files, extract-path-broken) are `detectors.py` functions; the other 5 (profile-install-drift, vault-hash-drift, skeleton-drift, command-vocab-drift, plugin-config-drift) are Linter **agent procedures** (git / SHA-256 / audit-log), not in `detectors.py`. Full verification needs a live Hermes-Linter run. |
| `scripts/install.sh` (bootstrap — Ubuntu/Debian + WSL2) | shipped | **Rewritten 2026-05-31 from profile-installer → full bootstrap** per [bootstrap-installer.md](../../docs/reference/installer.md). 9-step flow: consent → prereqs (git/pandoc) → clone-or-use-clone → Hermes + ACP extra → copy vault to a target off OneDrive (user sets up their own git — installer guides, does not `git init`) → pip MCP deps → deploy 7 profiles → provision skills (K-Dense clone + official/kepano/qmd) → guide Obsidian/Zotero → secrets + next-steps. Safety rails: `main()`-guard, up-front consent, `/dev/tty` prompts (survives `curl\|bash`), no silent sudo, `--dry-run`/`--yes`/`--no-apps`/`--vault`. The old profile-install logic survives as `--profiles-only` (the maintenance path). After each profile installs, it applies the **capability layer** (#51): writes `agent.disabled_toolsets: [terminal, file]` as a real YAML list into the 5 non-terminal lanes' deployed `config.yaml` via `disable_write_toolsets()` (PyYAML), preserving the model + hooks blocks (Coder/Linter keep the toolsets). **Tier-3 verified (this session, live Hermes on WSL2):** `bash -n` clean; full install EXIT=0; all 7 profiles register at 0.1.0; the list lands typed correctly (was a `config set` string-coercion bug — `config set` stores a scalar and cannot emit a list, so the file is edited directly); idempotent `--profiles-only` re-run clean. **Env-key seeding (Tier-4 fix):** because Hermes profile runs read only their own `.env` (no global inheritance), the installer now seeds each profile's `.env` with the shared keys it declares (`seed_profile_env`, copied from `~/.hermes/.env`; idempotent — re-run `--profiles-only` after adding keys); next-steps guidance updated to match. Verified live: `memoria-mapper` runs with no manual `.env` editing after a clean install. **Resolved confirm-at-build:** `config set` list syntax (→ replaced by the PyYAML edit). **Open Tier-3 findings:** 2 official skills fail to fetch — `official/productivity/ocr-and-documents` + `official/github/github-repo-management` ("Could not fetch from any source"); installer warns-not-fails, so those 2 capabilities are absent — needs a corrected skill path or removal from `OFFICIAL_SKILLS`. Still pending: live model run (Tier-4) to confirm Hermes honors `disabled_toolsets` + the `hermes -p memoria-socratic acp` smoke-test. |
| `scripts/install.ps1` (thin Windows launcher) | shipped | **Rewritten 2026-05-31 from full PowerShell profile-installer → thin WSL2 launcher.** Does only the three Windows-only things: WSL2 gate (absent → Microsoft how-to link + exit, install nothing), winget the GUI apps (Obsidian, Zotero — guided), then hands off to `scripts/install.sh` inside WSL with `--no-apps` and the vault path translated via `wslpath` to `/mnt/c/…` (one vault both Windows-Obsidian and WSL-Hermes can reach). No install logic of its own — `scripts/install.sh` is the single source of truth. Forwards `-ProfilesOnly`/`-DryRun`/`-Yes`/`-Vault`. Parses clean. Not approved: not run end-to-end against a live WSL2. |

## Vault structure and notes

| Artifact | Status | Notes |
| --- | --- | --- |
| Folder skeleton `00-meta`…`95-archive` | shipped | Complete structure ships with `.keep` placeholders. Matches design spec. Includes `10-inbox/03-candidates/` (see note below). |
| `10-inbox/03-candidates/` | shipped | Folder **and** its `candidate-note.md` template both ship (ADR-17 adopted 2026-06-01). Populated by the Librarian (`find`/dead-letter) and Verifier (`gap`). |
| `99-system/logs/` and `99-system/logs/sessions/` | shipped | Both dirs ship as `.keep` placeholders. `audit.jsonl` is created at runtime by the policy MCP (pending). |
| 16 note templates (`99-system/templates/`) | shipped | All 16 present: answer/canvas/claim/code/deliverable/draft/fleeting/item/moc/organization/paper/person/project/reference/venue/candidate. |
| `candidate-note.md` template (16th type) | shipped | **ADR-17 adopted 2026-06-01** — part of v0.1. Unifies discovery candidates + Verifier gap-cards (`source: gap`) + ingestion dead-letters; registered in note-types / frontmatter; `weekly-review` query wired. |
| `relations:` block on `claim-note` | shipped | ADR-8 (typed-relations-frontmatter); opt-in `supports`/`contradicts` block in template (human-set). |
| `superseded_by:` on `claim-note` | shipped | ADR-10 (claim-supersession); top-level field in template (human-set; currency derives from it). |
| `00-meta/vocabulary.md` (controlled classification vocabulary) | shipped | Seeded #101 — the controlled tag vocabulary the [ADR-30](../decisions/30-deterministic-ingest-pipeline.md) classify step (Tier-1, unbuilt) ranks against, and the human source of truth behind `manage-vocabulary.md`. Present; not yet exercised by any agent. Tag-shortlist precision is unvalidated (round-2 red-team R2-4). |
| `lifecycle: captured` + `ingest_status:` frontmatter | shipped | Added #102 ([ADR-30](../decisions/30-deterministic-ingest-pipeline.md)). New `captured` lifecycle state (the pre-`proposed` ingest stub) + `ingest_status` field with a terminal `needs-human` floor; registered in `reference/frontmatter.md` + note-types. The retry-sweep over stalled `captured` notes and the gated transition out of `captured` are unbuilt (release-plan G9). |
| `home.md` (vault-root front door) | shipped | ADR-13 (homepage-front-door); ships at vault root. obsidian-homepage auto-open is a post-clone step (see plugins). |
| `.obsidian/workspaces.json` | deferred | Designed in `obsidian-ui/workspaces.md`; out of v0.1 scope. |
| Vault-resident reference notes (`00-meta/`) | shipped | `04-reference/` dissolved (2026-06-02): reference docs belong on the website (single source of truth), not duplicated in the vault. **Only the troubleshooting note stays in-vault** (renamed from `safe-mode`, moved to the **vault root** 2026-06-02, and broadened to fold in the former `system-status` health-check — now dropped as a standalone note), drift-checked by `skeleton-drift`. `schema-reference` went website-only too — it duplicated `reference/frontmatter.md`, and offline field-lookup didn't outweigh the duplication + drift-upkeep cost (its one unique bit, the MOC `scope` vocab, was migrated to `frontmatter.md` first). Relocated: `design-system` → `.memoria/` (machine tokens read by CSS-gen/Pandoc); `screening-protocol` → `99-system/templates/` (Templater template). Deleted (now website-only, vault links out): `agent-roles`, `profile-policies`, `system-map`, `getting-started`, `obsidian-config`, `vocabulary-example` (seed lists → `manage-vocabulary.md`), `dataview-cheatsheet` + `performance-checklist` (unique bits → `reference/dashboards.md`). |
| `research-focus.md` | shipped | At the **vault root** (moved from `00-meta/` and renamed from `research-directions` 2026-06-02). Librarian's session-start input. Shares the root cockpit with `home.md` and `troubleshooting.md`; `system-status.md` was folded into the latter. |
| `screening-protocol.md` | shipped | Fillable PRISMA template at `99-system/templates/screening-protocol.md` (ADR-16 — adopt-on-demand systematic-review tooling cluster). |

## Dashboards

| Artifact | Status | Notes |
| --- | --- | --- |
| 10 dashboards + `daily-health.md` (`00-meta/01-dashboards/`) | shipped | Files present: audit-log, board-state, contradictions, discuss-queue, drift-watch, fleet-health, loose-ends, open-questions, reading-pipeline, weekly-review. Dataview is now bundled and configured (dataviewjs enabled); approval still pending query verification against a live corpus. |
| `skill-lifecycle` dashboard | deferred | Out of v0.1 scope. Ships when skill-governance is stood up (Phase 3). |
| `99-system/eval/` | shipped | `.keep` placeholder present. Eval task content (ADR-11 — vault-eval-integration) is pending. |
| `99-system/skills/` | shipped | `.keep` placeholder present. Skill-governance overlay is deferred. |
| `99-system/metrics/` | shipped | `.keep` placeholder present. Metrics aggregator now ships (`.memoria/mcp/metrics_aggregate.py`); needs a cron entry + real weekly volume to populate. **Phase 3: cron wiring waits for sufficient run volume.** |
| `99-system/board/` (markdown board export) | shipped | `.keep` placeholder present. The board exporter ships (`.memoria/mcp/board_export.py`); **the cron is now wired (G5)** by the installer's `wire_telemetry_cron`. Needs a live board (cards) to populate `99-system/board/`, and the Hermes scheduler/gateway running for the cron to fire. |

## Board and runtime

| Artifact | Status | Notes |
| --- | --- | --- |
| Hermes built-in Kanban board (`kanban.db`) | pending (runtime) | **Phase 1 / v0.1**, but the **native Hermes** Kanban: created at `~/.hermes/kanban.db` by `hermes kanban` at runtime, not authored in the vault. Workers use native `kanban_*` tools; Memoria adds only the `metadata` review overlay (convention) + the board exporter. Nothing for Memoria to author — it exists once Hermes runs. |
| Policy-MCP write-gate enforcement | shipped | `policy_mcp.py` implements the gate (see control-plane); decision logic verified by self-test + `--decide`. Not approved: enforcement is only live once the `mcp` SDK is installed and a profile registers the server via `mcp.json` (pending). Until then, nothing routes writes through the gate. |
| `99-system/logs/audit.jsonl` | pending | Created at runtime by the policy MCP on its first logged decision (the code to write it now exists in `policy_mcp.py`). Does not exist until the gate runs live. |
| Board exporter / metrics aggregator | shipped | Both now implemented: `.memoria/mcp/board_export.py` (board-state) and `.memoria/mcp/metrics_aggregate.py` (fleet-health). See their rows above. `board_export.py` cron is Phase 1; `metrics_aggregate.py` cron is Phase 3 (needs real weekly volume first). |
| Six-signal capture (v0.1 minimal measurement) | shipped | The un-backfillable capture from [publication-path-report §5](../../_notes/publication-path-report.md) now emits in full: state-transition timestamps + decision time (`board-transitions.jsonl`), cost per card (`cost.jsonl`), deny-reasons (`audit.jsonl`), disposition / accept : edit : reject (`disposition.jsonl`), FAMA exposure (`fama-exposure` detector → `lint-findings.jsonl`). Schemas pinned in [reference/telemetry.md](../../docs/reference/telemetry.md). The *analysis* harnesses (CiteME fixture, claim taxonomy, fleet dashboard, propagation debts, LLM-judge, reflection-on-retry) stay **deferred** per [measurement-and-verification.md](../proposals/measurement-and-verification.md). Not approved: needs the board_export cron (Phase 1) running for the logs to populate. |

## Obsidian plugins

| Plugin | Status | Notes |
| --- | --- | --- |
All eight required plugins are now **bundled** in `vault/.obsidian/plugins/` (build files committed) and configured to spec — the distribution model changed from post-clone install to a batteries-included starter vault. Per-machine secrets/paths still need a manual pass (see notes).

| Plugin | Status | Notes |
| --- | --- | --- |
| obsidian-local-rest-api | shipped | Bundled. HTTPS port 27124; insecure HTTP port 27123 off. Real `data.json` (apiKey + TLS cert + RSA key) is **gitignored**; ships as `data.json.example`. Per-machine: regenerated on first launch. |
| agent-client | shipped | Bundled. Real `data.json` is **gitignored** (per-machine agent command paths via `{{HOME}}`, provider apiKeys); ships as `data.json.example`. `defaultAgentId: memoria-socratic`, `autoMentionActiveNote: true`. Not verified end-to-end without a registered profile. |
| callout-manager | shipped | Bundled and **configured** — `data.json` defines the three custom callouts (`brief`, `suggestions`, `verification`) with RGB color + Lucide icon. `data.json.TODO` retired. Rendering unverified against a live theme. |
| obsidian-citation-plugin | shipped | Bundled and configured. Uses an **inline** `literatureNoteContentTemplate` (this plugin has no external-template setting); kept in sync with `99-system/templates/paper-note.md`. Requires `.memoria/memoria.bib` (shipped, empty stub) to produce notes. |
| dataview | shipped | Bundled and **configured**: `enableDataviewJs` + `enableInlineDataviewJs` set `true` (off by default), `refreshInterval` 2500. Dashboards now have their prerequisite. Queries unverified against a live corpus. |
| templater-obsidian | shipped | Bundled and **configured**: `templates_folder` = `99-system/templates`, `trigger_on_file_creation` + `enable_system_commands` `false`. |
| quickadd | shipped (partial) | Bundled. **2 of ~20 catalog commands wired** as Template choices (`Memoria: capture fleeting`, `Memoria: write claim note`). The rest are a **build gap (pending)**: they need QuickAdd Macros + user `.js` scripts that POST to the Hermes API (none exist yet) and, for lens reads, a Hermes profile arg `agent-client:open-chat-view` can't pass. See [reference/obsidian-command-palette.md](../../docs/reference/obsidian-command-palette.md) for the full catalog. |
| obsidian-git | shipped | Bundled and **configured**: `commitMessage`/`autoCommitMessage` = `vault: {{date}} {{numFiles}} files`, `autoSaveInterval` 30, `autoPullOnBoot` true, `autoBackupAfterFileChange` false, `pullBeforePush` true. |
| obsidian-homepage | pending | **Recommended, not bundled.** Auto-opens `home.md` on startup (ADR-13). `home.md` ships at vault root; the auto-open plugin is an optional post-clone install. |

## How to use this ledger

- Adding or moving an artifact? Update its row here in the same change.
- Writing a design doc for something not yet built? Tag it *(status: pending)* and link this ledger.
- Writing a design doc for something intentionally out of scope? Tag it *(status: deferred)*.
- A `plugin-config-drift` / structural-detector pass can read this file to distinguish pending (build gap) from deferred (scope decision) from shipped (present, unverified) from approved (present, verified).
