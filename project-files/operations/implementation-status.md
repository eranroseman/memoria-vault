---
topic: general
---

# Implementation status

The design docs in `docs/` describe the **intended** Memoria system. The starter vault (under `vault/` in this repo) is a **v0.1 scaffold** — some components are shipped and present in the vault, and a significant portion of the v0.1 design is pending implementation. Nothing is approved yet. This ledger is the single source of truth for the build state of every artifact, so a gap reads as a known status — not a documentation error. For the phase-by-phase activation sequence — when each component is wired, configured, or first run — see [timeline.md](timeline.md).

When a design doc describes an artifact, tag it with one of the status values below (inline, e.g. *(status: pending)*). When in doubt, this ledger wins.

## Status values

| Value | Meaning |
| --- | --- |
| **approved** | In `vault/`, tested, and verified to work end-to-end. |
| **shipped** | In `vault/`, but not yet verified end-to-end (dependencies missing or testing not done). |
| **pending** | Designed and required — but not yet built. A build gap; the requirement stands. |
| **deferred** | Consciously excluded from this release. A scope decision; absence is intentional. |

Authoritative runtime signal: `install.ps1` requires `SOUL.md` + `config.yaml` + `mcp.json` + `distribution.yaml` per profile. All four now ship for all seven profiles (the v0.2 wiring landed), so the installer no longer skips them — it will stage, substitute `{{VAULT_PATH}}`, and call `hermes profile install`. If the installer and a design doc disagree, the installer is right and the doc needs a status tag.

## Profiles and control plane

| Artifact | Status | Notes |
| --- | --- | --- |
| 7 profile `SOUL.md` prompts (`.memoria/profiles/memoria-*/`) | shipped | Present for all seven (`coder/librarian/linter/mapper/socratic/verifier/writer`). `config.yaml` + `mcp.json` + `distribution.yaml` now ship alongside (below), so profiles are installable. Not yet approved: needs an end-to-end run against a registered profile + the `obsidian` MCP bridge (below). |
| Per-profile `cron/` and `skills/` placeholder dirs | shipped | Empty (`.keep`) for all 7 profiles. Linter and Mapper also ship `cron/scheduled.yaml` with real content. |
| `cron/scheduled.yaml` for Linter and Mapper | shipped | Actual scheduled task definitions present in `.memoria/profiles/memoria-linter/cron/` and `memoria-mapper/cron/`. Other profiles have empty `.keep` only. |
| Per-profile `config.yaml` (×7) | shipped | Full `model` block only (Hermes replaces config sections wholesale — a partial block would wipe the global `agent`/toolset settings, so everything else inherits from `~/.hermes/config.yaml`). `provider: kilocode` + `base_url: https://api.kilo.ai/api/gateway` (matches the user's gateway); `default: ~anthropic/claude-{sonnet,opus,haiku}-latest`. Tiers: Linter→Haiku, Librarian/Writer/Coder/Mapper→Sonnet, Socratic/Verifier→Opus. Recommended `reasoning_effort` per profile noted in comments. Also carries a `hooks.pre_tool_call` block registering the policy gate (`policy_hook.py`); `install.ps1` now substitutes `{{VAULT_PATH}}` in `config.yaml` as well as `mcp.json`. Not approved: `~anthropic/claude-{opus,haiku}-latest` strings should be confirmed against the KiloCode catalog (`hermes model`); per-profile `hooks` honoring not yet verified live; not yet run end-to-end. |
| Per-profile `mcp.json` (×7) | shipped (partial) | Top-level `mcp_servers` (per Hermes docs). Wires `policy` (stdio: `python {{VAULT_PATH}}/.memoria/mcp/policy_mcp.py --vault {{VAULT_PATH}}`) and `obsidian` (`uvx mcp-obsidian`, env `OBSIDIAN_API_KEY`/`OBSIDIAN_HOST=127.0.0.1`/`OBSIDIAN_PORT=27124`). Valid JSON after substitution. `tasks` server intentionally absent (the native Hermes `kanban_*` tools cover it — see `tasks_mcp.py` row). Unverified: `mcp-obsidian` bridge choice, `${OBSIDIAN_API_KEY}` env substitution, HTTPS self-signed cert, and per-profile `tools.include` read-only filters; plus the write-gating gap (below). |
| Per-profile `distribution.yaml` (×7) | shipped | Manifest per the live Hermes profile-distribution schema: `name`/`version: 0.2.0`/`description`/`author`/`hermes_requires: ">=0.12.0"`/`env_requires`. Verified parse + `name` matches dir. |
| Per-profile `.env.EXAMPLE` | shipped | **Generated by `hermes profile install` from `distribution.yaml` `env_requires`** (not hand-authored), then copied to `.env` by `install.ps1`. Required secrets: `ANTHROPIC_API_KEY`, `OBSIDIAN_API_KEY` (all); `OPENALEX_EMAIL` (Librarian); optional `OPENROUTER_API_KEY`/`SEMANTIC_SCHOLAR_API_KEY`/`ZOTERO_API_KEY`/`SCITE_API_KEY`/`GITHUB_TOKEN` per profile. |
| `obsidian` MCP server (vault-access bridge) | shipped (partial) | Wired in every `mcp.json` as `uvx mcp-obsidian` against coddingtonbear's Local REST API plugin (port 27124). Write-gating now enforced by the `policy_hook.py` `pre_tool_call` hook (below) — obsidian writes route through `policy_mcp.check`. Unverified: `mcp-obsidian` bridge choice, `${OBSIDIAN_API_KEY}` env substitution, HTTPS self-signed cert. |
| `.memoria/mcp/policy_hook.py` | shipped | Hermes `pre_tool_call` hook that gates `mcp-obsidian` writes through the policy decision core (reuses `policy_mcp.PolicyEngine`). Maps obsidian tool→action, blocks `deny`/`dry_run`, passes reads. Registered per profile in `config.yaml` (`matcher: "obsidian"`, `--profile memoria-<name>`). Now handles BOTH events (branches on `hook_event_name`): `pre_tool_call` gates (block deny/dry_run, pass reads, stash before_hash); `post_tool_call` appends the paired `write_complete` after_hash record for reversibility. `python policy_hook.py --self-test` passes (18 checks incl. pre→post roundtrip) + verified end-to-end via stdin. **Phase 1 gate: self-test must pass before any profile runs against the production vault.** **Caveat:** Hermes fails *open* on hook errors, so this is best-effort enforcement — fully fail-closed needs a custom bridge that calls policy internally. |
| `.memoria/mcp/policy_mcp.py` | shipped | Policy write-gate implemented against [policy-mcp.md](../reference/architecture/policy-mcp.md): four-decision protocol, eight-action vocabulary, default-deny, review-gated `dry_run` degrade, Linter `auto_fix` class-gating, SHA-256 audit hashing, and skill-conditional one-way narrowing. Dependency-light decision core passes `python policy_mcp.py --self-test` (34 checks) and `--decide` one-shots against all seven shipped lane-overrides. **Phase 1 gate: both self-tests (34 + 18 checks) must pass before any profile runs against the production vault.** Not approved: needs the `mcp` SDK installed, live `hermes` MCP registration (via `mcp.json`), and a real worker exercising it end-to-end. |
| `.memoria/mcp/requirements.txt` | shipped | `mcp>=1.2.0` (FastMCP server wrapper) + `PyYAML>=6.0` (lane-override parsing). `install.ps1` pip-installs it before profile registration. |
| `.memoria/mcp/tasks_mcp.py` | deferred (not needed) | The board is the **native Hermes Kanban**; workers use the built-in `kanban_*` tools + `hermes kanban` CLI (`reference/kanban-board.md` rejects a separate task registry). Board *visibility* is handled by `board_export.py`. No `tasks` MCP server is needed. |
| `.memoria/mcp/board_export.py` | shipped | Projects the live Hermes board to `00-meta/board/<task_id>.md` (per-card frontmatter) + `00-meta/02-logs/board-state.jsonl` (per-lane counts). Reads `hermes kanban list --json` (`--from-json` for tests). `--self-test` passes (15 checks). Not approved: needs a live board + a cron entry on the ~60s dispatch cadence. **Phase 1: cron must be wired before dashboards show board data.** |
| `.memoria/mcp/metrics_aggregate.py` | shipped | Rolls `audit.jsonl` (deny rate) + the board (success/retry rate) + `lint-findings.jsonl` (drift) into `00-meta/08-metrics/lane-<lane>-<period>.md` trust-score notes (bands 90+/70-89/<70). `--self-test` passes (12 checks). Not approved: `skill-metric` + CRS `pass^k` need repeated-run data (TODO); needs cron + real weekly volume. **Phase 3: cron wiring waits for sufficient run volume; trust-score bands are meaningless on sparse data.** |
| `.memoria/lane-overrides/{lane}.yaml` (×7) | shipped | All seven lane-override YAMLs present (`coder/librarian/linter/mapper/socratic/verifier/writer`). `policy_mcp.py` parses and enforces all seven (verified via `--decide`). Writer's review-gated *draft* paths (`30-synthesis/02-reference/**`, `50-deliverables/**`) moved from `deny.write` → `allow.write` so they degrade to `dry_run` per the matrix; human-owned `01-claims`/`03-moc` stay hard-`deny` (verified). Full approval needs live enforcement against a registered profile. |
| `.memoria/project-hints.yaml.EXAMPLE` | shipped | Present in `.memoria/` root. Copy-on-install semantics analogous to `.env.EXAMPLE`. |
| `.memoria/csl/` | shipped | Empty placeholder (`.keep`) for CSL citation style files. |
| `.memoria/library.bib` | pending | User-generated by Better BibTeX auto-export; not shipped. Required at runtime for ingest and citation lookup. |
| `.memoria/tool-registry.yaml` | shipped | Per-profile tool allowlist (default-deny) with capability groups (`vault_read`/`vault_write`/`web`/`policy`); schema Memoria-defined. Cross-checked: `vault_write` capability matches each lane's write-scope (only Socratic lacks both). Not approved: machine enforcement (policy reading it to gate tool calls; Linter drift-check against it) is a follow-up. |
| K-Dense scientific-agent-skills (per-profile `skills/`) | pending | Required by v0.1. Skills dirs ship as empty `.keep` placeholders for all seven profiles. K-Dense skill files must be installed via `hermes skills install` before any profile can use them. **Phase 1 setup step.** |
| Linter `detectors.py` + `M-detectors.md` | shipped | Eight structural detectors present in `.memoria/profiles/memoria-linter/`. Verification requires a full Hermes-Linter run against a live vault. |
| `install.ps1` (Windows) | shipped | Runs; now substitutes `{{VAULT_PATH}}` in both `mcp.json` (MCP server commands) and `config.yaml` (policy-gate hook command). All seven profiles now carry the required file set, so the installer registers them instead of skipping. Full end-to-end install still unverified against a live Hermes + Obsidian. |
| `install.sh` (macOS/Linux/WSL2) | shipped | POSIX port of `install.ps1` (same logic: prereq checks, pip install, stage + `{{VAULT_PATH}}` substitution in `mcp.json`+`config.yaml`, `hermes profile install`, `.env` bootstrap). Honors `$HERMES_HOME`. `bash -n` clean; substitution verified. Especially relevant since the Hermes runtime is Linux/WSL2. Not approved: not yet run against a live Hermes. (Supersedes the earlier "Windows-only / deferred" scope call.) |

## Vault structure and notes

| Artifact | Status | Notes |
| --- | --- | --- |
| Folder skeleton `00-meta`…`95-archive` | shipped | Complete structure ships with `.keep` placeholders. Matches design spec. Includes `10-inbox/03-candidates/` (see note below). |
| `10-inbox/03-candidates/` | shipped | Folder ships as `.keep`. The `candidate-note.md` template that would populate it is **deferred** (proposal-21 *shared-candidate-frontmatter*, still `proposed`) — the folder is ahead of its template. |
| `00-meta/02-logs/` and `00-meta/02-logs/sessions/` | shipped | Both dirs ship as `.keep` placeholders. `audit.jsonl` is created at runtime by the policy MCP (pending). |
| 15 note templates (`00-meta/03-templates/`) | shipped | All 15 present: answer/canvas/claim/code/deliverable/draft/fleeting/item/moc/organization/paper/person/project/reference/venue. |
| `candidate-note.md` template (16th type) | deferred | proposal-21 (*shared-candidate-frontmatter*) is still `proposed` — slated for baseline adoption but not yet landed (see ADR-16). Not part of v0.1. |
| `relations:` block on `claim-note` | shipped | ADR-8 (typed-relations-frontmatter); opt-in `supports`/`contradicts` block in template (human-set). |
| `superseded_by:` on `claim-note` | shipped | ADR-10 (claim-supersession); top-level field in template (human-set; currency derives from it). |
| `Home.md` (vault-root front door) | shipped | ADR-13 (homepage-front-door); ships at vault root. obsidian-homepage auto-open is a post-clone step (see plugins). |
| `.obsidian/workspaces.json` | deferred | Designed in `obsidian-ui/workspaces.md`; out of v0.1 scope. |
| Human-facing reference notes (`00-meta/04-reference/`) | shipped | All ten present and verified against design docs by the Linter's `skeleton-drift` detector: `agent-roles`, `dataview-cheatsheet`, `design-system`, `getting-started`, `obsidian-config`, `performance-checklist`, `profile-policies`, `safe-mode`, `schema-reference`, `system-map`. |
| `00-meta/index.md`, `research-directions.md`, `system-status.md` | shipped | Present at `00-meta/` root (vault home / strategy / status). |
| `screening-protocol.md` | shipped | Fillable PRISMA template at `00-meta/04-reference/screening-protocol.md` (ADR-16 — adopt-on-demand systematic-review tooling cluster). |

## Dashboards

| Artifact | Status | Notes |
| --- | --- | --- |
| 10 dashboards + `index.md` (`00-meta/01-dashboards/`) | shipped | Files present: audit-log, board-state, contradictions, discuss-queue, drift-watch, fleet-health, loose-ends, open-questions, reading-pipeline, weekly-review. Cannot be approved until Dataview is installed and queries verified against a live corpus. |
| `daily-health.md` | n/a | The entry point is `index.md`. Any doc naming `daily-health.md` is wrong. |
| `skill-lifecycle` dashboard | deferred | Out of v0.1 scope. Ships when skill-governance is stood up (Phase 3). |
| `00-meta/05-eval/` | shipped | `.keep` placeholder present. Eval task content (ADR-11 — vault-eval-integration) is pending. |
| `00-meta/07-skills/` | shipped | `.keep` placeholder present. Skill-governance overlay is deferred. |
| `00-meta/08-metrics/` | shipped | `.keep` placeholder present. Metrics aggregator now ships (`.memoria/mcp/metrics_aggregate.py`); needs a cron entry + real weekly volume to populate. **Phase 3: cron wiring waits for sufficient run volume.** |
| `00-meta/board/` (markdown board export) | shipped | `.keep` placeholder present. The board exporter now ships (`.memoria/mcp/board_export.py`); needs a cron entry + a live board to populate. **Phase 1: cron must be wired on the ~60s cadence before dashboards show board data.** |

## Board and runtime

| Artifact | Status | Notes |
| --- | --- | --- |
| Hermes built-in Kanban board (`kanban.db`) | pending (runtime) | **Phase 1 / v0.1**, but the **native Hermes** Kanban: created at `~/.hermes/kanban.db` by `hermes kanban` at runtime, not authored in the vault. Workers use native `kanban_*` tools; Memoria adds only the `metadata` review overlay (convention) + the board exporter. Nothing for Memoria to author — it exists once Hermes runs. |
| Policy-MCP write-gate enforcement | shipped | `policy_mcp.py` implements the gate (see control-plane); decision logic verified by self-test + `--decide`. Not approved: enforcement is only live once the `mcp` SDK is installed and a profile registers the server via `mcp.json` (pending). Until then, nothing routes writes through the gate. |
| `00-meta/02-logs/audit.jsonl` | pending | Created at runtime by the policy MCP on its first logged decision (the code to write it now exists in `policy_mcp.py`). Does not exist until the gate runs live. |
| Board exporter / metrics aggregator | shipped | Both now implemented: `.memoria/mcp/board_export.py` (board-state) and `.memoria/mcp/metrics_aggregate.py` (fleet-health). See their rows above. `board_export.py` cron is Phase 1; `metrics_aggregate.py` cron is Phase 3 (needs real weekly volume first). |

## Obsidian plugins

| Plugin | Status | Notes |
| --- | --- | --- |
| obsidian-local-rest-api | shipped | Installed. HTTPS port 27124; insecure HTTP port 27123 off by default. |
| agent-client | shipped | Installed. Not independently verified end-to-end without a registered profile. |
| callout-manager | shipped | Installed. Config ships as `data.json.TODO` — callout styling unverified until the TODO is resolved. **Phase 1 manual step: copy `data.json.TODO` → `data.json` and configure callout styling before the dashboard and agent callout types (`[!brief]`, `[!suggestions]`, `[!verification]`) will render.** |
| obsidian-citation-plugin | shipped | Installed. Requires `.memoria/library.bib` (pending) to function. |
| dataview | pending | Required for all dashboard queries. Not in `.obsidian/plugins/`; must be installed as a post-clone step before any dashboard query runs. **Phase 1 prerequisite.** |
| templater | pending | Required for template insertion. Post-clone step. **Phase 1 prerequisite.** |
| quickadd | pending | Required for quick-capture flows. Post-clone step. **Phase 1 prerequisite.** |
| obsidian-git | pending | Required for version-controlled vault commits and profile prompt versioning. Post-clone step. **Phase 1 prerequisite.** |
| obsidian-homepage | pending | Required to auto-open `Home.md` on startup (ADR-13). Post-clone step. **Phase 1 prerequisite.** |

## How to use this ledger

- Adding or moving an artifact? Update its row here in the same change.
- Writing a design doc for something not yet built? Tag it *(status: pending)* and link this ledger.
- Writing a design doc for something intentionally out of scope? Tag it *(status: deferred)*.
- A `plugin-config-drift` / structural-detector pass can read this file to distinguish pending (build gap) from deferred (scope decision) from shipped (present, unverified) from approved (present, verified).
