# memoria-linter — deterministic detectors + logs
# Per-profile Hermes config. Hermes reads model / mcp_servers / agent / terminal /
# checkpoints / hooks from THIS file (ADR-27). The installer substitutes the
# {{PYTHON}} interpreter and {{VAULT_PATH}} at deploy time.
# Recommended: set agent.reasoning_effort per lane via `hermes -p memoria-linter config set`.
model:
  provider: kilocode
  base_url: https://api.kilo.ai/api/gateway
  default: ~anthropic/claude-haiku-latest

# Vault access + policy gate run as MCP servers — loaded ONLY from config.yaml
# mcp_servers (a standalone mcp.json is never read by Hermes; ADR-27 finding 1).
mcp_servers:
  policy:
    command: "{{PYTHON}}"
    args: ["{{VAULT_PATH}}/.memoria/mcp/policy_mcp.py", "--vault", "{{VAULT_PATH}}"]
    timeout: 30
  obsidian:
    command: uvx
    args: ["mcp-obsidian"]
    timeout: 30
    env:
      OBSIDIAN_API_KEY: "${OBSIDIAN_API_KEY}"
      OBSIDIAN_HOST: "127.0.0.1"
      OBSIDIAN_PORT: "27124"

# Capability allowlist (ADR-27): keep toolsets = file, skills, terminal, todo (+ obsidian/policy via
# MCP). disabled_toolsets = every Hermes toolset NOT in the keep-set, so the only
# vault-write path is the gated obsidian MCP. Update if Hermes adds toolsets.
agent:
  disabled_toolsets: [browser, clarify, code_execution, computer_use, cronjob, delegation, homeassistant, image_gen, memory, messaging, moa, session_search, spotify, tts, video, video_gen, vision, web, x_search, yuanbao]

# Profiles do not sandbox (ADR-27); anchor stray file ops inside the vault.
terminal:
  cwd: "{{VAULT_PATH}}"

# Shadow-git snapshot before destructive writes — mode-independent reversibility.
checkpoints:
  enabled: true

# Route writes through the policy gate. matcher is an re.fullmatch pattern, so
# "obsidian.*" (not "obsidian") is required to catch obsidian_* tools.
hooks:
  pre_tool_call:
    - matcher: "obsidian.*"
      command: "{{PYTHON}} {{VAULT_PATH}}/.memoria/mcp/policy_hook.py --profile memoria-linter"
      timeout: 10
    - matcher: "write_file|patch"
      command: "{{PYTHON}} {{VAULT_PATH}}/.memoria/mcp/policy_hook.py --profile memoria-linter"
      timeout: 10
  post_tool_call:
    - matcher: "obsidian.*"
      command: "{{PYTHON}} {{VAULT_PATH}}/.memoria/mcp/policy_hook.py --profile memoria-linter"
      timeout: 10
    - matcher: "write_file|patch"
      command: "{{PYTHON}} {{VAULT_PATH}}/.memoria/mcp/policy_hook.py --profile memoria-linter"
      timeout: 10
